---
type: RoundReport
routine: cobogo
date: 2026-09-01T01:58:13-04:00
focus: franklinbaldo/intuit
candidates: [franklinbaldo/intuit, franklinbaldo/the-lab, franklinbaldo/causaganha]
outcome: inspection-only
prs: []
merged: []
captures: []
blocked: [franklinbaldo/cobogo#407, franklinbaldo/the-lab#128, franklinbaldo/workspace#27]
---

## Foco e alternativas

As três skills operacionais e os três RoundReports mais recentes foram relidos antes da seleção. `franklinbaldo/cobogo#407` continua aberta no head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`, `mergeable: false`, portanto não decide por follow-up causal. `franklinbaldo/the-lab#128` continua aberta com a mesma causa já registrada (`Deploy Site` desabilitado manualmente) e não apresenta mudança causal que justifique repetir a intervenção. CausaGanha foi o foco imediatamente anterior e permaneceu sem gap.

Intuit foi escolhido pela pressão explícita do RoundReport anterior e porque não existe urgência/follow-up superior nesta passagem. O foco é inspeção: verificar se o fechamento de #27 continua representando o produto publicado e se surgiu algum gap novo D1/D2/D3.

## Leitor de fora — antes

**D1 — o que a pessoa pode fazer diretamente no site.** A superfície incorporada permite editar/colar HTML, renderizar no preview, copiar link, atualizar URL, limpar editor, alternar scripts e tema. A relação editor → preview é semanticamente nomeada; foco de teclado e reduced-motion foram incorporados pela #27. **Gap:** nenhum material observado.

**D2 — o que o projeto faz por trás e por que existe.** README, source e ConsumerCard continuam descrevendo Intuit como renderer HTML/visual-testing client-side, com `srcdoc`, sandbox estrito por default e scripts opt-in. O `ProjectProfile` do workspace ainda está factual e arquiteturalmente compatível com o produto, mas não incorporou a capacidade de captura visual adicionada por #26/#27; isso já está corretamente rastreado por `workspace#27`. **Gap de superfície:** nenhum. A divergência restante pertence à memória factual do workspace, fora da autoridade desta rotina.

**D3 — o que a pessoa pode fazer sozinha com os artefatos publicados.** O estado pode ser transportado por `?data=`/`?b64=`, carregado por Gist, e o checkout estático pode ser executado diretamente. O README continua documentando esses caminhos. **Gap:** nenhum material observado; não existe release/dataset/API adicional incorporado que a superfície esconda.

## Gap escolhido e critério

**Nenhum gap fechável nesta rodada.** Critério verificável: se `main`, README, ProjectProfile, Pages e captura visual continuarem coerentes com o ConsumerCard e não houver capacidade publicada nova invisível ao leitor, não abrir PR de UI. O resultado correto é `inspection-only`, mantendo `gap_score: 0`; qualquer divergência de inventário operacional do workspace permanece na issue dona já existente.

## Capturas

- Before novo: nenhum. Não há mudança visual proposta.
- Evidência visual vigente revalidada: Visual capture run `33417946698`, head `8178f348600f231e567589e45174f4fc507f6986`, success; artifact pós-merge já registrado no card.
- After: não aplicável; nenhuma byte da superfície do consumer foi alterada.
- Save Page Now: nenhum mecanismo registrado no consumer.

## Ações

- Relidas `brazilian-web-design`, `cobogo-design-review` e `cobogo-consumer-synergy`.
- Revalidada `franklinbaldo/cobogo#407`: aberta, head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`, `mergeable: false`; sem bypass.
- Revalidada `franklinbaldo/the-lab#128`: aberta e sem mudança causal de escopo; nenhuma nova intervenção.
- Lidos ConsumerCard, `workspace/knowledge/projects/intuit.md`, README e `main` de Intuit.
- `main` de Intuit continua `8178f348600f231e567589e45174f4fc507f6986`, merge da #27; não há PR própria aberta.
- Revalidados runs de `main`: Pages `33417946679` = success e Visual capture `33417946698` = success no mesmo SHA; o card já registra o Test verde correspondente.
- Revalidada `workspace#27`: continua aberta e possui escopo/critério corretos para reconciliar a capacidade de captura visual no ProjectProfile; nenhuma issue duplicada foi criada.
- ConsumerCard de Intuit atualizado para `last_reviewed: 2026-09-01`, `gap_score: 0`, com `last_verified` das capacidades atualizado para hoje.
- Nenhuma PR substantiva aberta e nenhum pattern promovido.

## Fatos observados

- O último commit de Intuit continua sendo `8178f348600f231e567589e45174f4fc507f6986` de 2026-08-31; não houve mudança de source após #27.
- Não há PR aberta no repositório Intuit.
- O último deploy de Pages observado no `main` concluiu `success` no mesmo SHA.
- A captura visual pós-merge observada concluiu `success` no mesmo SHA.
- O README atual continua documentando `?data=`, `?b64=`, Gist, editor, sandbox e execução estática, sem anunciar uma capacidade incorporada posterior à #27.
- O ProjectProfile continua desatualizado apenas quanto à existência da captura visual versionada; `workspace#27` já contém a evidência e o critério para corrigir esse fato.

## Inferências

- A rodada não encontrou regressão de superfície nem capacidade incorporada que esteja invisível ao leitor; abrir uma PR seria fabricar atividade.
- A divergência no ProjectProfile não deve inflar `gap_score`: ela é dívida de memória operacional no repo dono e já está devidamente rastreada.
- A captura visual existente continua suficiente como evidência do último estado materialmente alterado; uma nova captura idêntica sem mudança não acrescentaria causalidade.
- Intuit segue sendo pressure test local para editor → preview, mas uma solução em um único consumer não satisfaz o gate de promoção compartilhada.

## Resultado do gap

**inspection-only.** O critério foi satisfeito por não intervenção: D1, D2 e D3 permanecem coerentes com o estado incorporado do repo, `gap_score: 0` continua correto e nenhuma PR visual foi criada.

## Gates

- Nenhuma PR criada ou mergeada nesta rodada.
- `franklinbaldo/intuit`: `main` `8178f348600f231e567589e45174f4fc507f6986`; Pages `33417946679` = success; Visual capture `33417946698` = success; nenhuma PR aberta.
- `franklinbaldo/cobogo#407`: head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; aberta, `mergeable: false`; sem merge.

## Devolução ao Cobogó

- `knowledge/consumers/intuit.md` atualizado para `last_reviewed: 2026-09-01`, preservando `gap_score: 0` e revalidando capacidades pelo mesmo `main`.
- Nenhum conceito novo: não houve solução nova de superfície nem before/after novo.
- Nenhuma contagem incrementada e nenhum pattern promovido.
- `workspace#27` permanece o destino correto do único fato desatualizado fora desta autoridade; não foi duplicada.

## Bloqueios

- `franklinbaldo/cobogo#407` — head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; estado: aberta/não mergeável; condição de retomada: required checks verdes no head corrente após correção causal da suíte.
- `franklinbaldo/the-lab#128` — `main` conhecido `468b14f1e1c627eafbc4fc91513a28e6e2521aa3`; causa: Deploy Site desabilitado manualmente; condição de retomada: workflow ativo + deploy verde + recaptura pública da provenance.
- `franklinbaldo/workspace#27` — ProjectProfile de Intuit ainda não registra a captura visual versionada; condição de retomada: workspace reconciliar o inventário factual conforme a evidência já anexada. Não bloqueia a superfície nem o `gap_score` do consumer.

## Pressão para a próxima rotação

**nenhuma.** Aplicar novamente urgência → follow-up causal → maior gap/antiguidade entre os ConsumerCards ativos. The Lab só deve voltar ao foco com mudança causal no estado do deploy; `workspace#27` pertence ao repo dono e não deve prender a rotação de superfície.
