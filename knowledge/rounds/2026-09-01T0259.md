---
type: RoundReport
routine: cobogo
date: 2026-09-01T02:59:02-04:00
focus: franklinbaldo/leizilla
candidates: [franklinbaldo/leizilla, franklinbaldo/the-lab, franklinbaldo/ficha]
outcome: inspection-only
prs: []
merged: []
captures: []
blocked: [franklinbaldo/cobogo#407, franklinbaldo/the-lab#128]
---

## Foco e alternativas

As skills `brazilian-web-design`, `cobogo-design-review` e `cobogo-consumer-synergy` e os três RoundReports mais recentes foram relidos antes da seleção. O corpus corrente continua com The Lab como único `ConsumerCard` de `gap_score: 1`, mas `the-lab#128` permanece exatamente no mesmo bloqueio causal: Deploy Site desabilitado manualmente, sem deploy/recaptura nova. Repetir The Lab sem mudança seria fabricar atividade. `cobogo#407` continua própria, aberta e `mergeable: false` no head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`, portanto não decide por follow-up causal.

Entre os cards ativos de `gap_score: 0` ainda não revisados em 2026-09-01, Leizilla foi escolhido como próximo foco de antiguidade: seu card foi consolidado cedo em 2026-08-31 e não havia pressão superior. Ficha ficou como alternativa equivalente; CausaGanha e Intuit foram os dois focos imediatamente anteriores e não foram repetidos.

## Leitor de fora — antes

**D1 — o que a pessoa pode fazer diretamente no site.** O leitor pode pesquisar legislação, abrir uma norma, manter o contexto da busca, navegar dispositivos correspondentes, consultar versões/evidências/cobertura e chegar aos dados estruturados. A superfície incorporada já usa `Data do ato`, evitando transformar a data representativa da URN em prova de publicação no DOE. **Gap:** nenhum material observado. A #156 permanece draft e não é capacidade publicada.

**D2 — o que o projeto faz por trás e por que existe.** README, ConsumerCard e ProjectProfile descrevem a cadeia verificável `fonte oficial -> descoberta/coleta -> preservação/evidência -> parse + ETL -> dataset IA -> Parquet/meta -> portal DuckDB-WASM`, distinguindo dataset publicado, cobertura, pipeline e freshness. **Gap pré-registrado:** nenhum material de superfície, mas havia um sinal operacional a falsificar: o último `Parse + ETL + Release Dataset` observado estava globalmente vermelho. O teste desta rodada foi verificar se isso tornava falsa alguma afirmação pública de saúde/freshness.

**D3 — o que a pessoa pode fazer sozinha com os artefatos publicados.** `versoes.parquet` e `dataset_meta.json` seguem publicados e o README mantém uma consulta DuckDB reproduzível sobre o mesmo Parquet usado pelo portal. **Gap:** nenhum material observado; o produto não inventa API nem exige o frontend para reutilização.

## Gap escolhido e critério

**Nenhum gap de superfície escolhido para intervenção.** Critério verificável congelado: se a falha operacional observada não invalidar claims públicos, se o dataset/release continuar materializado de forma verificável e se `main` não contiver mudança de superfície posterior ao card, a rodada termina `inspection-only`, sem PR visual. Se a falha provar que a superfície afirma saúde/freshness atual sem suporte, ela passa a urgência e exige correção de superfície; se for bug operacional sem claim falso, deve permanecer no issue do repo dono.

## Capturas

- Before novo: nenhum. Não houve mudança visual proposta.
- Evidência visual vigente: nenhuma capacidade canônica dedicada de captura foi localizada no card; `last_verified` permanece registrado como ausência dessa capacidade.
- After: não aplicável; nenhuma byte da superfície de Leizilla foi alterada.
- Save Page Now: nenhum novo checkpoint executado; preservação por Wayback/Internet Archive pertence à arquitetura de dados, não foi usada como captura desta rodada.

## Ações

- Revalidado `franklinbaldo/cobogo#407`: aberta, head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`, `mergeable: false`; nenhum bypass.
- Revalidado `franklinbaldo/the-lab#128`: continua aberto e sem mudança causal de deploy desde a última rodada.
- Lidos ConsumerCard, `workspace/knowledge/projects/leizilla.md`, README e sinais atuais de `main`/Actions de Leizilla.
- `main` continua `631f62af2ff423d0842b91ed8c8410431160b53a`, sem commit de superfície após a última revisão.
- Revalidada #156: draft, mergeável no head `c9d13d205585a70ae62488d89c8e00b4feeff78b`; `Deploy Leizilla Web` e Check Pipeline Credentials verdes, `Lint & Test` vermelho. Não foi promovida nem tratada como capacidade incorporada.
- Inspecionado o run agendado `33393407511`: overall `failure` causado por `parse-casacivil-lei`; `parse-assembleia` e `parse-casacivil-lc` = success; job `etl` = success, incluindo `Consolidate XMLs -> Parquet` e `Release dataset -> IA`.
- Revalidada `leizilla#114`: issue operacional existente para exatamente `parse-release / casacivil-lei`, atualizada automaticamente no timestamp do run observado. Nenhuma issue duplicada foi criada.
- ConsumerCard de Leizilla atualizado em `2255efc2adf2f704cf42abc439fcf938055ac900` para registrar o estado parcial corretamente e `last_reviewed: 2026-09-01`, mantendo `gap_score: 0`.
- Nenhuma PR substantiva e nenhum pattern promovido.

## Fatos observados

- Leizilla `main` não mudou desde `631f62af2ff423d0842b91ed8c8410431160b53a` (2026-08-30).
- O README afirma dataset RO v0 publicado, cobertura incremental e existência do pipeline; não afirma que todo run atual está verde nem usa o workflow como prova de cobertura completa.
- O run `33393407511` terminou globalmente vermelho, mas a falha ficou restrita ao job `parse-casacivil-lei`; o job ETL/release concluiu com sucesso no mesmo run.
- `leizilla#114` está aberta desde 2026-07-15, tem atualizações recorrentes e foi atualizada em 2026-08-31T12:47:06Z, coincidindo com a falha atual de `casacivil-lei`.
- #156 continua draft e com pytest vermelho; sua migração `data_publicacao -> data_ato` não faz parte de `main` nem da superfície publicada.
- O ProjectProfile já separa #156 da dívida semântica de proveniência #157 e proíbe nova publicação de dataset enquanto essas semânticas puderem ser confundidas.

## Inferências

- `overall failure` do workflow não equivale a ausência de dataset/release: nesta execução, a etapa de release foi materialmente bem-sucedida apesar da falha parcial de parse.
- A frase do README de que o pipeline continua ampliando/conferindo fontes não foi falsificada por esse run: duas frentes de parse e o ETL/release executaram com sucesso. A superfície também não representa o workflow como saudável/current em tempo real.
- A falha de `casacivil-lei` é dívida operacional já pertencente a #114. Abrir uma issue nova ou editar a UI para refletir um único job de CI seria duplicação e confundiria operação com o contrato público do dataset.
- O card anterior era otimista ao dizer genericamente que workflows posteriores “seguem verdes”; a correção apropriada foi tornar explícito o estado parcial, sem inflar `gap_score`.

## Resultado do gap

**inspection-only.** O critério congelado foi satisfeito por não intervenção: o sinal vermelho não invalidou D1/D2/D3, o dataset/release continuou verificavelmente materializado no próprio run, e nenhuma capacidade incorporada nova ficou invisível ao leitor. `gap_score: 0` permanece correto.

## Gates

- Nenhuma PR criada ou mergeada nesta rodada.
- `franklinbaldo/leizilla#156` — head `c9d13d205585a70ae62488d89c8e00b4feeff78b`; draft; mergeable = true; Deploy Leizilla Web/credentials verdes; Lint & Test vermelho. Estado final: sem merge.
- `franklinbaldo/cobogo#407` — head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; aberta e `mergeable: false`; sem merge.

## Devolução ao Cobogó

- `knowledge/consumers/leizilla.md` atualizado para `last_reviewed: 2026-09-01`, mantendo `gap_score: 0` e distinguindo a falha parcial de parse do sucesso do ETL/release.
- Nenhum conceito novo: não houve solução nova de superfície nem evidência before/after.
- Nenhuma contagem incrementada e nenhum pattern promovido.
- Nenhuma issue nova no workspace: o ProjectProfile já contém o estado de #156/#157 e não surgiu nova capacidade/sinergia a reconciliar.

## Bloqueios

- `franklinbaldo/cobogo#407` — SHA `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; estado: aberta/não mergeável; condição de retomada: required checks verdes no head corrente após correção causal da suíte.
- `franklinbaldo/the-lab#128` — `main` conhecido `468b14f1e1c627eafbc4fc91513a28e6e2521aa3`; causa: Deploy Site desabilitado manualmente; condição de retomada: workflow ativo + deploy verde + recaptura pública da provenance.
- `franklinbaldo/leizilla#114` não bloqueia esta rodada de superfície: registra a falha parcial recorrente de `casacivil-lei`; condição operacional de conclusão pertence ao repo dono e não deve ser simulada por UI.

## Pressão para a próxima rotação

**franklinbaldo/ficha** — entre os ConsumerCards ativos de `gap_score: 0` ainda não revisados em 2026-09-01, é candidato natural de antiguidade. The Lab só retorna se #128/deploy mudar causalmente; #407 só decide por follow-up quando houver head verde/mergeável.
