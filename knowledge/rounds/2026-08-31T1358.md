---
type: RoundReport
routine: cobogo
date: 2026-08-31T13:58:00-04:00
focus: franklinbaldo/quem-sao-eles
candidates: [franklinbaldo/quem-sao-eles, franklinbaldo/tambaqui, franklinbaldo/cobogo]
outcome: inspection-only
prs: []
merged: []
captures: []
blocked: [franklinbaldo/cobogo#407]
---

## Foco e alternativas

Não há urgência nova comprovada. `franklinbaldo/cobogo#407` continua própria e aberta, mas não está verde/mergeável no head atual e portanto não decide por follow-up causal. `franklinbaldo/intuit`, foco anterior, caiu para `gap_score: 0` após o fechamento de foco/reduced-motion e não deve monopolizar a rotação.

Entre cards ainda não reconciliados ao schema D1/D2/D3, `franklinbaldo/quem-sao-eles` permanece `active` no `ProjectProfile`, tem superfície pública comprovada e última revisão canônica de projeto em 2026-08-17. `franklinbaldo/tambaqui` também é ativo como superfície, mas seu único unmet need registrado é uma decisão operacional sobre refresh, fora da autoridade desta rotina. `franklinbaldo/undogmatic` foi descartado porque o último commit observado é de 2025-11-11, ultrapassando 60 dias sem sinal de retomada. Foco: Quem São Eles.

## Leitor de fora — antes

### D1 — o que a pessoa pode fazer diretamente

A pessoa encontra a busca PEP, identifica a fonte Portal da Transparência/CGU e a competência do snapshot, executa consultas no navegador e recebe estados distintos de carregamento, erro, resultado e vazio. A home mantém essa busca separada do catálogo editorial.

Gap D1: nenhum gap material comprovado. O repo ainda não contém perfis/cronologia editoriais populados suficientes para uma experiência real dessa parte do produto; isso é ausência de matéria publicada, não uma capacidade escondida pela superfície.

### D2 — o que o projeto faz por trás e por que existe

README e `ProjectProfile` deixam recuperável a tese: catálogo jornalístico de memória política baseado em fontes secundárias arquivadas, mais uma superfície separada de consulta PEP. O runtime PEP usa Svelte + DuckDB-WASM sobre Parquet versionado.

Gap D2: nenhum material comprovado.

### D3 — o que a pessoa pode fazer sozinha com os artefatos publicados

Na superfície PEP, a pessoa pode consultar o snapshot no próprio site e também acessar o Parquet exato para reutilização independente. A competência e a autoridade ficam ligadas ao mesmo artefato.

Gap D3: nenhum material comprovado; o contrato `usar aqui ↔ levar o artefato` já possui prova Chromium/runtime registrada.

## Gap escolhido e critério

Nenhum gap fechável foi escolhido. Critério verificável para esta rodada sem intervenção: não criar conteúdo editorial fictício nem refatoração visual quando o repo não prova uma capacidade escondida; migrar o card ao schema atual, recalcular `gap_score` e preservar como pressão futura apenas a chegada de conteúdo editorial real ou uma nova classe de evidência/falha no PEP.

## Capturas

Nenhuma nova captura: não houve mudança visualmente material.

Evidência existente revalidada documentalmente: `PEP visual capture` run `32075299012`, head `1fb2c8eb55e577de07e274386b06ceec7a2bb50f`, verde; a prova registrada preserva screenshot full-page, DOM, estado machine-readable e consulta contra o snapshot publicado.

Save Page Now: nenhum.

## Ações

- Skills `brazilian-web-design`, `cobogo-design-review` e `cobogo-consumer-synergy` relidas.
- RoundReport `2026-08-31T1301.md` e estado recente da rotação relidos.
- `ProjectProfile` de Quem São Eles consultado somente para fatos, sem escrita no workspace.
- README e estado atual de `main` do consumer confrontados com o card.
- Último commit observado do consumer: `1fb2c8eb55e577de07e274386b06ceec7a2bb50f`, 2026-08-17.
- Último `PEP visual capture` observado: run `32075299012`, sucesso no mesmo SHA; o conjunto de runs também registra Pages bem-sucedido para esse estado.
- Consumer card migrado de `type: consumer` para `ConsumerCard` no commit Cobogó `1914ffc1bd1a91c2bf507252c66d4b80f35b5ec9`, com `last_reviewed: 2026-08-31` e `gap_score: 0`.
- Nenhum PR visual aberto, porque não há gap leitor-de-fora fechável sem fabricar matéria editorial.

## Fatos observados

- `ProjectProfile` classifica `franklinbaldo/quem-sao-eles` como ativo.
- O catálogo editorial continua sem conteúdo populado suficiente para testar perfis/cronologia como produto real.
- A busca PEP já tem evidência forte de runtime: fonte, competência, snapshot, consulta client-side e artifact download/reuse.
- `main` do consumer não mudou desde `1fb2c8eb55e577de07e274386b06ceec7a2bb50f` em 2026-08-17.
- O card anterior ainda misturava unmet needs de design editorial com estado factual do produto e não tinha `gap_score`.
- `franklinbaldo/cobogo#407` permanece aberta no head `32b3897335df837e0b49035ecce77d4d59fa3738` e não mergeável.

## Inferências

- A melhor ação desta rodada é reduzir dívida de conhecimento, não produzir pixels. A inexistência de perfis reais impede distinguir um problema de composição de um simples estado vazio do corpus.
- O PEP já demonstra D1 e D3 de forma mais forte que uma captura estática: a prova executa o runtime e consulta o Parquet.
- A próxima intervenção editorial só será epistemicamente útil quando existir matéria real suficiente para que hierarquia, cronologia e proveniência possam falhar de verdade.

## Resultado do gap

`inspection-only`. O critério congelado foi satisfeito: nenhum trabalho visual foi fabricado; o card agora expressa D1/D2/D3, `gap_score: 0` e a condição objetiva para nova revisão. Nenhum gap leitor-de-fora foi fechado porque nenhum gap material foi demonstrado.

## Gates

- Nenhuma PR substantiva nesta rodada.
- `franklinbaldo/cobogo#407` — aberta; head `32b3897335df837e0b49035ecce77d4d59fa3738`; não mergeável; nenhum bypass ou merge.
- Atualização canônica do card foi commitada diretamente em `main` do Cobogó como `1914ffc1bd1a91c2bf507252c66d4b80f35b5ec9`, conforme o passo de versionamento da rotina.

## Devolução ao Cobogó

- Consumer card de Quem São Eles atualizado; `last_reviewed: 2026-08-31`; `gap_score: 0`.
- Nenhum conceito novo: `Public artifact reuse` e `Provenance and freshness` já contêm o aprendizado relevante; esta rodada não adicionou uma segunda solução visual nova.
- Nenhum pattern promovido: não houve nova evidência before/after convergente em dois consumers.
- Nenhum achado novo de capacidade ou sinergia exigiu issue no workspace.

## Bloqueios

- `franklinbaldo/cobogo#407` — head `32b3897335df837e0b49035ecce77d4d59fa3738`; causa: dívida ampla de self-conformance/testes legados já conhecida; estado: aberta e não mergeável; condição de retomada: distinguir contratos vigentes de expectativas fossilizadas e obter required checks verdes no head atual.

## Pressão para a próxima rotação

`franklinbaldo/tambaqui` — card ainda legado e último review de superfície em 2026-08-17; a próxima passagem deve separar o problema operacional de refresh (fora de escopo) de eventuais gaps D1/D2/D3, sem transformar freshness futura em afirmação não provada.
