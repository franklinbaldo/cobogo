---
type: RoundReport
routine: cobogo
date: 2026-08-31T16:03:00-04:00
focus: franklinbaldo/cobogo
candidates: [franklinbaldo/cobogo, franklinbaldo/ficha, franklinbaldo/o-vigia, franklinbaldo/baliza]
outcome: blocked
prs: [franklinbaldo/cobogo#407]
merged: []
captures: []
blocked: [franklinbaldo/cobogo#407]
---

# RoundReport — Cobogó — 2026-08-31 16:03 -04:00

## Foco e alternativas

Não houve urgência comprovada nem PR própria verde de consumer que obrigasse merge. Entre os cards ativos já reconciliados, os maiores `gap_score` observados são 0; `franklinbaldo/cobogo`, `franklinbaldo/ficha`, `franklinbaldo/o-vigia` e `franklinbaldo/baliza` estavam entre os mais antigos, com `last_reviewed: 2026-08-30`. O Cobogó foi o foco porque o follow-up causal obrigatório da #407 produziu evidência fresca no próprio sistema e consumiu a única tentativa de correção permitida pela rodada. Ficha, O Vigia e Baliza foram descartados para não abrir um segundo trabalho substantivo.

## Leitor de fora — antes

### D1 — o que a pessoa pode fazer diretamente no site

O site de referência publica documentação, specimens e busca sob `/cobogo/`.

**Gap:** nenhum gap D1 material comprovado no card ou nesta inspeção.

### D2 — o que o projeto faz por trás e por que existe

A superfície documental já apresenta Cobogó como gramática e sistema de conhecimento de superfície derivado de uso real, preservando identidade local; a #412 reconciliou o README com essa tese.

**Gap:** nenhum gap D2 material comprovado.

### D3 — o que a pessoa pode fazer sozinha com os artefatos publicados

O repo publica CSS e componentes instaláveis e documenta entry points de `core`, `styles` e componentes Svelte.

**Gap:** nenhum gap D3 material comprovado.

## Gap escolhido e critério

**Nenhum gap de leitor escolhido.** Não há gap D1/D2/D3 fechável comprovado e a rotina não fabrica intervenção. O trabalho desta rodada ficou restrito ao follow-up causal já aberto de self-conformance.

Critério operacional preservado para #407: o head atual só pode ser mergeado se o novo `npm test`, Astro check e build passarem no mesmo head SHA. O critério não foi reescrito após a execução.

## Capturas

Nenhuma. A correção aplicada à #407 altera somente configuração de CI/package script e expectativas de teste; não altera a superfície renderizada. Captura before/after não se aplica e não foi contada como evidência visual.

## Ações

- `franklinbaldo/cobogo#407` — branch `fix/restore-vitest-gate` reaplicada mecanicamente sobre `main` para remover o conflito sem ampliar o escopo.
- Head após a tentativa: `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`.
- Commits reaplicados: `24291ada000020592cc4bc9f24cb21fb04378f56` (`npm test`), `364c563631da9ce0d25c124abcb0de4e34200c9a` (CI), `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e` (expectativas de Alert).
- CI run `33433669418`, job `99624839994` — `npm ci` passou; `Unit tests` falhou; Astro check/build foram corretamente interrompidos.
- Consumer card `knowledge/consumers/cobogo.md` atualizado com a evidência fresca da suíte.
- Nenhuma issue de capacidade/sinergia no workspace: o achado é self-conformance do próprio Cobogó e já está materializado na #407/#404.

## Fatos observados

- A #407 era mecanicamente conflitante com o `main` corrente antes da tentativa desta rodada.
- O patch substantivo continua limitado a três arquivos: `.github/workflows/ci.yml`, `package.json` e `src/components/Alert.test.ts`.
- Após reaplicação sobre `main`, o workflow de PR executou no head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`.
- A suíte reproduziu exatamente 87/152 testes passando e 65/152 falhando, em 9/29 arquivos passando e 20/29 falhando.
- `src/components/Alert.test.ts` passou 5/5 após alinhar as duas expectativas ao `role="alert"` e `data-intent` atuais.
- As falhas restantes atravessam várias famílias: classes legadas de estado/tamanho, estrutura antiga de Badge/Notification/OptionGroup, FormField/context, Drawer, Combobox e Tooltip.
- Não houve alteração visual nem merge da #407.

## Inferências

- O conflito de branch não era a causa das 65 falhas: removê-lo reproduziu o mesmo baseline de self-conformance.
- A dívida restante não cabe numa única correção causal sem misturar contratos vigentes, detalhes fossilizados e possíveis regressões reais.
- Tornar a suíte inteira required gate agora, ignorando as falhas ou atualizando expectativas em massa sem classificação, enfraqueceria o princípio de que gates medem contratos relevantes e não detalhes incidentais.

## Resultado do gap

**Não se aplica a gap de leitor.** Nenhum gap D1/D2/D3 foi escolhido ou alterado. O follow-up operacional ficou **bloqueado** pelo gate de evidência no próprio head atual.

## Gates

- `franklinbaldo/cobogo#407`
  - head SHA: `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`
  - CI run: `33433669418`
  - `npm ci`: verde
  - `npm test`: vermelho — 65 failed / 87 passed
  - Astro check: skipped após falha de testes
  - Build: skipped após falha de testes
  - estado final: aberta, não mergeada
  - merge: proibido nesta rodada porque o required evidence gate não ficou verde no head atual

## Devolução ao Cobogó

- `knowledge/consumers/cobogo.md`: `last_reviewed` atualizado para `2026-08-31`; `gap_score` permanece 0.
- Build/CI e smoke/self-conformance passam a registrar o run `33433669418` e o head `612f7f24` como evidência atual.
- Nenhum conceito novo registrado: não houve solução local de superfície nem captura before/after.
- Nenhum pattern promovido: o gate de ≥2 consumers com evidência convergente não foi acionado.

## Bloqueios

- **#407** — SHA `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`.
  - causa: 65/152 testes vermelhos em 20 arquivos após reaplicação limpa sobre `main`.
  - estado: conflito mecânico removido, mas CI vermelho no próprio `Unit tests`.
  - condição de retomada: classificar a dívida restante por contrato vigente versus expectativa fossilizada/possível regressão e reconciliá-la em escopo próprio; só então repetir o gate completo no novo head. Não fazer merge enquanto o head atual estiver vermelho.

## Pressão para a próxima rotação

nenhuma
