---
type: RoundReport
routine: cobogo
date: 2026-09-02T08:57:20-04:00
focus: franklinbaldo/quem-sao-eles
candidates: [franklinbaldo/quem-sao-eles, franklinbaldo/leizilla, franklinbaldo/perquire]
outcome: partial
prs: []
merged: []
captures: [artifact:9821925549]
blocked: [franklinbaldo/quem-sao-eles#23]
---

## Foco e alternativas

A rodada começou pela reconciliação de follow-ups causais. `franklinbaldo/leizilla#159` continua aberto e sem mudança desde a rodada anterior, portanto não deve monopolizar a rotação. Em `franklinbaldo/quem-sao-eles`, porém, o blocker factual de publicação #17 mudou de estado: foi fechado após #19/#21 e possui deploy + captura runtime verdes no mesmo landing. Isso vence consumer novo sem baseline pela ordem de urgência/follow-up causal.

As skills `brazilian-web-design`, `cobogo-design-review` e `cobogo-consumer-synergy`, o README opinionado, RoundReports recentes, ConsumerCards e ranking foram relidos. O `ProjectProfile` de `franklinbaldo/quem-sao-eles` foi consultado no workspace apenas como fonte factual e não foi editado.

## Leitor de fora — estado atual

### D1 — clareza

A rota `/pep` identifica o trabalho público com precisão: busca em dados abertos de Pessoas Expostas Politicamente, origem Portal da Transparência/CGU, competência julho de 2026 e link direto para `202607_pep.parquet`. O formulário explicita o mínimo de caracteres e o feedback de busca sem resultado.

Gap factual D1: nenhum material demonstrado após o fechamento de #17.

### D2 — explicabilidade

README/ProjectProfile e a própria superfície mantêm a separação entre catálogo editorial e consulta PEP. A correção #19/#21 torna verificável também a causalidade operacional refresh → deploy/capture, sem transformar um push do `GITHUB_TOKEN` em suposta garantia de recursão automática.

Gap factual D2: nenhum material demonstrado.

### D3 — autonomia

O leitor pode consultar no navegador e baixar o Parquet exato da mesma competência. A captura runtime verifica que a aplicação chega a `search-ready` e executa consulta real contra o artifact publicado.

Gap factual D3: nenhum material demonstrado. O `gap_score` volta portanto a 0.

## Assessment das dimensões relevantes

Ainda não foi criado um `SurfaceQualityAssessment` completo. Há evidência forte para D1/D2/D3 e confiança de provenance/freshness, e o screenshot desktop `1280×900` mostra hierarquia e legibilidade coerentes. Porém o artifact canônico do landing contém apenas `pep-1280x900.png`, `pep-dom.html` e `capture-state.json`; não existe captura estreita equivalente. Responsividade e containment de `/pep` em ~390×844 continuam sem pixels ligados ao SHA.

Pela regra “nota sem evidência não existe”, a rodada não atribui UI/UX totais inferindo mobile do código ou do desktop. Isso não é nota 0: é evidência insuficiente para fechar o baseline.

## Problema/recomendação escolhidos e critério imutável

Problema: **a capacidade visual existente prova runtime real em desktop, mas não cobre o viewport estreito necessário para avaliar responsividade de UI/UX com causalidade.**

Recomendação aplicável: `visual-evidence-as-quality-gate`, maturidade `stable`.

Critério congelado antes de qualquer eventual mudança no consumer, registrado em `franklinbaldo/quem-sao-eles#23`: capturar `/pep` em desktop e ~390×844 pelo mesmo workflow; registrar rota, viewport e SHA real; continuar chegando a `search-ready` e executando consulta real; no estreito, navegação, provenance, input, ação primária e feedback precisam permanecer integralmente perceptíveis/acionáveis, sem clipping ou overflow oculto; PR e landing devem ser recapturáveis pelo mesmo método.

## Captura observada

- Landing: `6d702973f7c640930a2a8a25d24fdcfaaf87c80f`.
- Deploy: run `33562803474`, `success`.
- Runtime visual: run `33562806014`, `success`, artifact `9821925549` (`pep-rendered-evidence`).
- Conteúdo do artifact: `pep-1280x900.png`, `pep-dom.html`, `capture-state.json`.
- O screenshot desktop mostra cabeçalho editorial simples, bloco de provenance com origem/competência/arquivo, formulário de busca e feedback sem resultado; não há evidência equivalente de viewport estreito.

## Ações

- Reconciliado #17 como fechado com evidência causal, em vez de carregar o card stale.
- Aberta `franklinbaldo/quem-sao-eles#23` como dívida concreta de UI/UX/confiança, com evidência, posição Cobogó, critério verificável e liberdade de implementação local.
- ConsumerCard atualizado para `last_reviewed: 2026-09-02` e `gap_score: 0`, registrando deploy/capture atuais e separando corretamente a dívida visual fora de D1–D3.
- Ranking não alterado e nenhum assessment inventado.
- Nenhuma recomendação mudou de maturidade: a evidência do consumer é consistente com `visual-evidence-as-quality-gate`, mas ainda incompleta no eixo estreito.

## Fatos observados

- #17 está `closed/completed`.
- O comentário de fechamento registra Pages run `33562803474` e captura run `33562806014`, ambos no head `6d702973...`, com `/pep` servindo `202607_pep.parquet` e competência julho/2026.
- O artifact `9821925549` está preservado e ligado ao mesmo head.
- O artifact tem apenas uma imagem `1280×900`; não contém captura de ~390×844.
- O desktop observado é claro e sóbrio; o formulário e o bloco de provenance são legíveis, e o feedback de zero resultados é explícito.

## Inferências

- O gap antigo era freshness operacional e está fechado; mantê-lo no `gap_score` seria agora memória stale.
- A falta de mobile não prova que a UI mobile é ruim. Prova que a rotina ainda não pode pontuá-la com a mesma causalidade usada nos consumers já ranqueados.
- Quem São Eles possui uma prática particularmente forte de captura funcional: screenshot + DOM + estado machine-readable após consulta real. O Cobogó deve preservar esse nível de prova e apenas ampliar a matriz de viewport, não substituí-lo por screenshot superficial.

## Resultado do gap

**Parcial / bloqueado por evidência estreita.** O gap D1–D3 de publicação foi fechado e reconciliado. O baseline de seis dimensões permanece deliberadamente pendente até #23 produzir captura estreita no mesmo método.

## Devolução ao Cobogó

- ConsumerCard reconciliado com o estado factual pós-#17.
- `gap_score` corrigido de 1 para 0; a dívida UI/UX/confiança é rastreada separadamente e não é apagada por esse score.
- Nenhum `SurfaceQualityAssessment` ou ranking foi atualizado sem evidência suficiente.
- Nenhuma nova `OpinionatedRecommendation` foi necessária: a posição transversal já existe e está `stable`.

## Bloqueios

- `franklinbaldo/quem-sao-eles#23`: falta captura runtime equivalente em viewport estreito. Condição objetiva de retomada: artifact do mesmo método com desktop + ~390×844, SHA real e consulta funcional preservada.
- `franklinbaldo/leizilla#159` continua aberto sem mudança causal e permanece fora do foco desta rodada.

## Pressão para a próxima rotação

Voltar a Quem São Eles somente se #23 tiver mudança causal. Caso contrário, escolher outro consumer ativo ainda sem baseline de seis dimensões e com evidência visual suficiente — ou criar apenas a capacidade mínima de evidência, sem fabricar notas.
