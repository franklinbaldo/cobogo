---
type: RoundReport
routine: cobogo
date: 2026-09-01T18:02:49-04:00
focus: franklinbaldo/causaganha
candidates: [franklinbaldo/causaganha, franklinbaldo/cobogo, franklinbaldo/the-lab, franklinbaldo/quem-sao-eles]
outcome: blocked
prs: [franklinbaldo/causaganha#979]
merged: []
captures: []
blocked: [franklinbaldo/causaganha#979, franklinbaldo/cobogo#407]
---

# Rodada Cobogó — 2026-09-01T1802

## Foco e alternativas

A regra que decide é follow-up causal. `franklinbaldo/causaganha#979` é PR própria aberta de superfície pública: cria `/agentes`, entrada na navegação e uma explicação local/stdio do MCP já provado pelo repositório. No head `63efd1a48df924c8f7a4fd1e95d008f0941c8585`, os 10 check-runs terminaram com sucesso e a PR está `mergeable: true`, `mergeable_state: clean`.

`franklinbaldo/cobogo#407` também é follow-up próprio, mas continua bloqueado no head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`: ao restaurar Vitest como gate, o build expõe 65 testes vermelhos em 20 arquivos, além do orçamento de uma correção mecânica nesta rodada. `franklinbaldo/the-lab` mantém o bloqueio já conhecido de deploy desabilitado. `franklinbaldo/quem-sao-eles` mudou causalmente desde a rodada anterior: #17 foi encerrada após deploy e capture verdes no snapshot 202607; portanto não há urgência remanescente que supere o follow-up de CausaGanha.

## Leitor de fora — antes

### D1 — o que pode fazer diretamente no site

O leitor consegue consultar processo por CNJ, pesquisar publicações e explorar cobertura/estado das fontes pela navegação pública. A superfície já organiza o produto pelos papéis Arquivo / Estado / Teor.

**Gap D1 [fato]:** nenhum gap material novo escolhido. A PR #979 acrescenta uma entrada informativa para agentes, mas não transforma o site em host MCP nem deve sugerir que a execução do MCP acontece no navegador.

### D2 — o que o projeto faz por trás e por que existe

A home e `/sobre` já explicam que CausaGanha preserva rastros judiciais, reconcilia fontes oficiais e mantém arquivo, estado, teor, proveniência, freshness e limites de cobertura como responsabilidades distintas. O ProjectProfile confirma MCP read-only como capacidade operacional, ao lado de Internet Archive, Parquet/DuckDB e pipelines por fonte.

**Gap D2 [fato]:** nenhum gap material contabilizado: a razão de existir e a separação Arquivo / Estado / Teor já são públicas e sustentadas pelo repo.

### D3 — o que pode fazer sozinho com os artefatos publicados

A superfície e o README permitem reutilizar `catalog.sql`, Parquets e artefatos do Internet Archive; o README também prova um servidor `causaganha-mcp` read-only executável localmente por stdio e documenta a configuração genérica para hosts MCP.

**Gap D3 [fato]:** a capacidade MCP local é provada pelo repositório, mas ainda não tem uma rota pública de autoatendimento no site. Um leitor que chega apenas à superfície publicada não encontra uma página dedicada que ligue os jobs Arquivo / Estado / Teor às tools existentes, ensine o entrypoint local e deixe explícito que não há endpoint HTTP remoto estável.

## Gap escolhido e critério

Fechar o gap D3 de autoatendimento do MCP local.

**Critério verificável, pré-registrado:** depois da mudança, a navegação pública expõe `Agentes` e a rota `/agentes` publicada mostra (1) Arquivo / Estado / Teor ligados às tools MCP que o repo realmente oferece, (2) configuração local usando o entrypoint canônico `uv run causaganha-mcp`, e (3) o transporte remoto como indisponível, sem URL inventada. A evidência visual deve conter captura before/after da própria rota `/agentes` pelo mesmo workflow/método; antes deve provar a ausência da rota no base e depois deve mostrar a página renderizada.

## Capturas

- **Before/after pretendido:** rota `/agentes`, workflow `Agents Surface Visual Capture`, desktop 1280×900 e mobile 390×844, base `6f12341d9cb0f2c76a024f663934f24f5b05bf9d` vs. head da PR.
- Run inicial `33564395680`: falhou antes da captura porque o build do base exige fixtures `site-status.json` e correlatas.
- Tentativa única de correção: head `ce11e1f26e8b4f23127408ad2c8d1d43cb4707fe`, run `33564601095`. Fixtures e ambos os builds passaram; o base gerou 110 páginas sem `agentes.html`, e o head 111 páginas incluindo `agentes.html`. O passo de screenshot navegou para `/causaganha/agentes` em `python -m http.server`; esse servidor não reescreve clean URL para `agentes.html`, então before/after renderizaram a mesma resposta. Hashes idênticos: `64fd94fb…` desktop e `d89d3e8f…` mobile. O workflow falhou e não publicou artifact.
- URL pública pretendida: `https://franklinbaldo.github.io/causaganha/agentes`.
- URL arquivada: nenhuma; Save Page Now não é capacidade registrada para a superfície deste consumer.

## Ações

- `franklinbaldo/causaganha#979` — permanece aberta, foi convertida em draft por rodada interrompida e não foi mergeada.
- `a848794252eb378699a3d3edb04399f9bf859076` — adiciona capture dedicado de `/agentes`.
- `ce11e1f26e8b4f23127408ad2c8d1d43cb4707fe` — única tentativa de correção: adiciona fixtures obrigatórias antes dos builds.
- Runs `33564395680` e `33564601095` — falhas descritas acima.
- `franklinbaldo/cobogo#407` — bloqueio reconfirmado: 65 testes Vitest falhando em 20 arquivos no head atual.
- `franklinbaldo/quem-sao-eles#17` — mudança causal observada: issue encerrada após Pages e capture verdes no snapshot 202607.

## Fatos observados

- O ProjectProfile confirma MCP read-only como capacidade operacional e a tese de arquivo/estado/teor/proveniência/freshness separados.
- O README em `main` documenta `causaganha-mcp`, configuração stdio local e declara que transporte HTTP remoto não está configurado.
- As tools `processo_consultar`, `publicacoes_buscar`, `processo_estado` e `decisoes_buscar` são provadas pelo código/testes do MCP.
- #979 cria `/agentes`, navegação e sitemap sem afirmar rollout HTTP remoto.
- No run corrigido, os builds base/head passaram e comprovaram a existência de `agentes.html` somente no head.
- A falha restante é do método de serving da evidência: `python -m http.server` não reproduz o rewrite de clean URLs do Pages.
- O ConsumerCard foi recalculado de `gap_score: 0` para `gap_score: 1`, porque o D3 provado no repo continua sem superfície publicada enquanto #979 não mergeia.

## Inferências

- O conteúdo de #979 continua dentro da autoridade de superfície e não antecipa capacidade remota inexistente.
- Para retomar, o gate deve servir a saída estática com semântica de clean URL equivalente ao Pages, ou usar uma URL de captura que represente corretamente o arquivo gerado, preservando exatamente o mesmo método before/after.
- Sem merge e sem capture válido, não há proveniência suficiente para registrar novo conceito nem qualquer promoção ao Cobogó.

## Resultado do gap

**Não fechado.** A superfície publicada continua sem `/agentes`. O critério pré-registrado exige capture before/after válido e captura pós-deploy; nenhum dos dois existe, portanto não houve merge.

## Gates

- #979 head inicial `63efd1a48df924c8f7a4fd1e95d008f0941c8585`: checks existentes verdes, mas sem evidência específica da rota material.
- Head `a848794252eb378699a3d3edb04399f9bf859076`: `capture-agents-surface` vermelho porque o base não tinha fixtures obrigatórias.
- Head atual `ce11e1f26e8b4f23127408ad2c8d1d43cb4707fe`: fixtures e builds passam; `capture-agents-surface` vermelho porque as screenshots before/after ficaram idênticas por resolução incorreta da clean URL. **Sem merge.**
- `franklinbaldo/cobogo#407` head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`: `capture` e GitGuardian verdes; `build` vermelho; bloqueado.

## Devolução ao Cobogó

- ConsumerCard de CausaGanha atualizado para `last_reviewed: 2026-09-01`, `gap_score: 1`, registrando D3 enquanto #979 não chega à superfície pública.
- Conceitos: nenhum; não há merge + capture válido para proveniência.
- Pattern promovido: nenhum; gate de ≥2 consumers não satisfeito.
- Workspace: nenhuma issue nova; MCP read-only já constava no ProjectProfile e não surgiu nova capacidade/sinergia.

## Bloqueios

- `franklinbaldo/causaganha#979` — SHA `ce11e1f26e8b4f23127408ad2c8d1d43cb4707fe`; `capture-agents-surface` vermelho porque o servidor estático usado no teste não resolve `/causaganha/agentes` para `agentes.html`, tornando before/after idênticos. Estado: PR draft, não mergeada. Condição de retomada: ajustar o método de serving/capture em nova rodada, obter artifact before/after válido no novo head, então exigir todos os checks verdes antes de merge, deploy e recaptura.
- `franklinbaldo/cobogo#407` — SHA `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; 65 falhas Vitest em 20 arquivos tornam a reconciliação maior que uma correção mecânica. Retomar em rodada própria sem enfraquecer o gate.

## Pressão para a próxima rotação

`franklinbaldo/causaganha` — follow-up causal de #979: o conteúdo está pronto, mas o único bloqueio da superfície agora é tornar a captura dedicada fiel à semântica de clean URL e obter evidência válida antes de qualquer merge.
