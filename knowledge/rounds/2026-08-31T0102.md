---
type: RoundReport
routine: cobogo
date: 2026-08-31T01:02:00-04:00
focus: franklinbaldo/ficha
candidates: [franklinbaldo/ficha, franklinbaldo/cobogo, franklinbaldo/baliza]
outcome: inspection-only
prs: []
merged: []
captures: [https://github.com/franklinbaldo/ficha/actions/runs/32119960585]
blocked: [franklinbaldo/cobogo#407]
---

## Foco e alternativas

Nenhuma urgência de superfície foi encontrada no consumer foco, e não havia PR própria de superfície verde pronta para mergear. `franklinbaldo/cobogo#407` continua bloqueada pela reconciliação ampla da suíte Vitest já registrada nas rodadas anteriores; não houve nova evidência que justificasse outra tentativa nesta rodada.

A pressão da rodada anterior apontava `franklinbaldo/ficha`. O card ainda estava no schema legado, com `last_verified: 2026-08-18`, e misturava necessidades futuras de gramática analítica com gaps do leitor. Ficha foi escolhido para reclassificação factual. Cobogó e Baliza foram descartados porque acabaram de ser avaliados com `gap_score: 0`.

## Leitor de fora — antes

### D1 — o que pode fazer no site

A home apresenta uma busca direta e, conforme os artefatos disponíveis no snapshot, modos de empresa, pessoa/sócio, endereço e CNAE. Também separa explicitamente o caminho **Consultar aqui**.

Gap D1: nenhum material. As ações reais estão legíveis e condicionadas ao que o snapshot sustenta.

### D2 — o que o projeto faz por trás e por que existe

A superfície e seu estado carregado apresentam competência, origem Receita Federal, preservação/verificação e o caráter de arquivo datado. O `ProjectProfile` confirma a tese: fichário público, histórico e reutilizável, com snapshots verificáveis, Internet Archive, manifest multi-competência e DuckDB-WASM no navegador.

Gap D2: nenhum material. A antiga necessidade de “multi-snapshot temporal comparison grammar” é uma direção condicionada a histórico real suficiente, não uma ausência que torne a tese atual incompreensível.

### D3 — o que pode fazer sozinho com os artefatos

A home oferece **Levar a base** e liga diretamente ao `manifest.json`; o README documenta os artefatos públicos e o uso externo em Parquet/protobuf/ZIP sem depender da interface.

Gap D3: nenhum material.

## Gap escolhido e critério

Nenhum gap fechável foi escolhido. Critério da rodada sem intervenção: se D1, D2 e D3 já estiverem sustentados pela superfície e pelos artefatos atuais, não criar mudança visual apenas para materializar necessidades futuras do design system. Resultado esperado: card reconciliado ao schema atual com `gap_score: 0`, sem PR de superfície.

## Capturas

Nenhuma captura nova foi necessária porque não houve mudança visualmente material.

Evidência visual vigente: workflow `Visual capture` da #222, run `32119960585`, artifact `9318141960`, rota `/ficha/`, método Chromium sobre build com base real `/ficha/`. O artifact permanece disponível até 2026-09-17. A composição de `web/src/pages/index.astro` não mudou desde o merge #222; a publicação de snapshot posterior alterou dados/manifest, não a composição da página.

## Ações

- Releitura das skills `brazilian-web-design`, `cobogo-design-review` e `cobogo-consumer-synergy`.
- Follow-up de PRs abertas: `franklinbaldo/cobogo#407` permanece bloqueada; as PRs abertas observadas em Ficha são operacionais/RFC e não são PRs próprias de superfície prontas para merge desta rotina.
- `franklinbaldo/ficha` `main`: `65bcd9a052459984d6500dcc1380635b0fe19dfa`; CI push run `33298511254` concluído com sucesso.
- Consumer card `knowledge/consumers/ficha.md` migrado para `ConsumerCard`, `last_reviewed: 2026-08-31`, `gap_score: 0`.
- Nenhuma issue de capacidade/sinergia aberta: nenhum achado novo desse tipo surgiu.
- Nenhum pattern promovido ao Cobogó.

## Fatos observados

- O `ProjectProfile` do Ficha foi revisado em 2026-08-30 e registra status ativo, snapshot verificado `2026-07`, manifest multi-competência, preservação no Internet Archive, checksums públicos e DuckDB-WASM.
- A home tem dois caminhos explícitos: **Consultar aqui** e **Levar a base**.
- A busca implementa modos `empresa`, `pessoa`, `endereco` e `cnae`, degradando datasets complementares de forma isolada.
- O README documenta o site público, `manifest.json`, Parquet, protobuf/ZIP, snapshots e uso externo.
- `.github/workflows/visual-capture.yml` continua disponível com `workflow_dispatch` e captura Chromium do Project Pages real.
- `web/src/pages/index.astro` teve sua última alteração em #222; não houve mudança de composição pública após essa adoção.

## Inferências

- Os itens legados “data-reading patterns”, “analytical filters and comparison grammar” e “multi-snapshot temporal comparison grammar” não devem ser convertidos automaticamente em `gap_score`: são necessidades de design/roadmap, algumas condicionadas a evidência e história de dados ainda em evolução.
- O Ficha já funciona para o leitor externo como arquivo datado com dois níveis de uso: consulta pontual e reutilização da base. Forçar mais uma camada explicativa agora tenderia a duplicar informação já presente.

## Resultado do gap

`inspection-only`. O critério da rodada foi satisfeito: nenhuma intervenção visual foi fabricada. O card atual agora representa D1/D2/D3 e registra `gap_score: 0`.

## Gates

- Nenhuma PR substantiva criada nesta rodada.
- `franklinbaldo/cobogo#407`: bloqueada no head já registrado anteriormente pela suíte Vitest ampla; sem merge e sem nova tentativa nesta rodada.
- Ficha `main` `65bcd9a052459984d6500dcc1380635b0fe19dfa`: CI push `33298511254` verde.

## Devolução ao Cobogó

- `knowledge/consumers/ficha.md` atualizado para o schema `ConsumerCard` e `gap_score: 0`.
- Nenhum conceito novo: a separação interface/artefato e a autoridade `core compartilhado → mapping local → CSS local` já estavam documentadas; não há segunda observação nova que altere gate de promoção.
- Nenhum pattern promovido.

## Bloqueios

- `franklinbaldo/cobogo#407` — head conhecido da rodada anterior; causa: 65/152 testes da suíte completa ainda exigem reconciliação entre contratos vigentes e asserts fossilizados. Estado: aberta/bloqueada. Condição de retomada: classificar e corrigir a suíte sem enfraquecer o gate.

## Pressão para a próxima rotação

`franklinbaldo/leizilla` — o card permanece legado no corpus e deve ser reclassificado em D1/D2/D3 antes de transformar suas necessidades extensas em trabalho de superfície.
