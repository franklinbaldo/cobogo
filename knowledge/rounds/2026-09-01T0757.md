---
type: RoundReport
routine: cobogo
date: 2026-09-01T07:57:47-04:00
focus: franklinbaldo/astronauta
candidates: [franklinbaldo/astronauta, franklinbaldo/cobogo, franklinbaldo/the-lab]
outcome: closed
prs: [franklinbaldo/astronauta#40]
merged: [franklinbaldo/astronauta#40]
captures: []
blocked: [franklinbaldo/cobogo#407, franklinbaldo/the-lab#128]
---

## Foco e alternativas

As três skills operacionais e os três RoundReports mais recentes foram relidos antes da seleção. Astronauta vence pela pressão explícita da rodada anterior e por ser o único card corrente com `gap_score: 1`: a issue #39 registra uma afirmação pública de distribuição que o próprio repo/CI já deixou para trás. Cobogó #407 e The Lab #128 permanecem candidatos de follow-up, mas só podem prevalecer se seus gates causais tiverem mudado; na última evidência ambos estavam bloqueados.

## Leitor de fora — antes

**D1 — o que a pessoa pode fazer diretamente no site/superfície pública.** O README apresenta Astronauta como admin/editor local para bundles OKF, com dashboard, tipos, conceitos, relações, diagnósticos, grafo e três classes atuais de mutação preview-first. **Gap:** nenhum material em D1.

**D2 — o que o projeto faz por trás e por que existe.** A cadeia Browser → Astro → capability client → gateway → `okf-parser` está explícita, assim como parser authority, review-binding e `--write` process-owned. **Gap:** nenhum material em D2.

**D3 — o que a pessoa pode fazer sozinha com os artefatos publicados.** O repo público fornece source checkout e o CI prova uma wheel com runtime Astro standalone instalada em ambiente consumidor fresco sem Bun/source checkout. O README, porém, ainda diz que empacotar esse runtime é a “next distribution slice”. **Gap [fato]:** a documentação pública faz o artefato instalado parecer futuro embora o estado já esteja incorporado e testado; não há GitHub Release/PyPI verificada que possa ser inventada como canal de distribuição.

## Gap escolhido e critério

Gap escolhido: **D3 — reconciliar a documentação pública de packaging com o runtime instalado já provado pelo CI, sem ampliar claims de release.**

Critério verificável e imutável: **depois da mudança, `Development and packaging boundary` deve afirmar que a wheel instalada já carrega o runtime Astro standalone em `_web/server/entry.mjs`, que um consumidor fresco não precisa Bun nem build frontend prévio para executar o artefato instalado, preservar `astronauta PATH [--write]` como contrato e não afirmar GitHub Release/PyPI não verificada.**

## Capturas

- Before visual: não aplicável; a mudança foi estritamente editorial no README, sem alteração de UI executável.
- Before textual: `README.md` em `main` `81a978200aa08d91dce8a06f0050eda25741009e`, seção `Development and packaging boundary` ainda usando “next distribution slice”.
- After visual: não aplicável pelo mesmo motivo.
- After textual: `README.md` em `main` após merge `664536ae012879ba901b451eab3ee362b9c488ce`; a seção agora afirma que a wheel instalada carrega `_web/server/entry.mjs`, que `installed-artifact` prova ambiente consumidor fresco sem Bun/source checkout/build frontend prévio e que GitHub Release/PyPI precisam de verificação independente antes de serem anunciadas.
- Save Page Now: não aplicável à superfície local/README.

## Ações

- Pré-registro criado antes de qualquer alteração no consumer.
- ConsumerCard, ProjectProfile, README e issue #39 relidos; estado incorporado confirmou o gap D3.
- Criada branch `docs/runtime-packaging-current` a partir de `81a978200aa08d91dce8a06f0050eda25741009e`.
- Aberta `franklinbaldo/astronauta#40`, head exato `8a7ce581d56f5b77356fa69f368420ed6c6a5f58`, alterando somente README (+3/-1).
- No head exato, os quatro workflows publicados concluíram `success`: CI run `33505337537`, Live editor `33505337593`, Live apply `33505337590` e Live import `33505337553`; os oito check-runs do head terminaram sem failure, incluindo `build`, `live-ssr` e `installed-artifact`.
- #40 ficou `mergeable: true` no mesmo head e foi squash-mergeada com `expected_head_sha`; merge `664536ae012879ba901b451eab3ee362b9c488ce`.
- README pós-merge foi reaberto diretamente em `main` e confirmou o critério pré-registrado.
- #39 fechou automaticamente como `completed` pelo merge.
- ConsumerCard atualizado para `gap_score: 0`, preservando `last_reviewed: 2026-09-01`.
- Revalidado `cobogo#407`: continua aberto, `mergeable: false`, head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; nenhum merge.
- Revalidado `the-lab#128`: continua aberto com a mesma causa documentada, `Deploy Site` desabilitado manualmente; nenhuma mudança causal.

## Fatos observados

- `astronauta/main` estava em `81a978200aa08d91dce8a06f0050eda25741009e` no início da rodada.
- O ProjectProfile lista `wheel com runtime Astro standalone` entre capacidades operacionais.
- O ConsumerCard registrava `gap_score: 1` somente por esse drift D3.
- A issue #39 estava aberta e não havia PR aberta em Astronauta no início da rodada.
- O head `8a7ce581d56f5b77356fa69f368420ed6c6a5f58` passou pelos gates publicados antes do merge; em especial, `installed-artifact` concluiu `success` no mesmo head que alterou a documentação.
- O merge da #40 é `664536ae012879ba901b451eab3ee362b9c488ce` e o README em `main` contém a nova descrição de packaging.
- A issue #39 está fechada como `completed`.
- `cobogo#407` permanece não mergeável no mesmo head histórico; The Lab #128 permanece aberto sem mudança causal no deploy.

## Inferências

- O gap era de explicação progressiva/distribuição, não de runtime; alterar código de packaging teria ampliado o escopo sem necessidade.
- O README pode descrever o contrato instalado provado por CI sem converter isso em claim de release pública; a nova redação mantém explicitamente essa fronteira.
- Como a mudança não alterou pixels, gerar screenshots artificiais não acrescentaria evidência causal; o before/after textual no mesmo README é a evidência adequada.
- A solução é local e factual, sem relação visual reproduzida em segundo consumer; não há base para conceito/pattern novo.

## Resultado do gap

**fechado.** O critério pré-registrado foi satisfeito sem alteração: `Development and packaging boundary` agora registra `_web/server/entry.mjs` como parte da wheel instalada, associa a prova ao gate `installed-artifact`, preserva `astronauta PATH [--write]` e recusa inferir GitHub Release/PyPI da simples existência do artefato no repo/CI.

## Gates

- `franklinbaldo/astronauta#40` — head `8a7ce581d56f5b77356fa69f368420ed6c6a5f58`; CI `33505337537` = success; Live editor `33505337593` = success; Live apply `33505337590` = success; Live import `33505337553` = success; `mergeable: true`; squash merge protegido por SHA = `664536ae012879ba901b451eab3ee362b9c488ce`.
- Não há Pages/deploy separado para Astronauta; a superfície pública modificada é o README do próprio repositório, verificado diretamente em `main` após o merge.
- `franklinbaldo/cobogo#407` — head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; estado final desta rodada: aberta e não mergeável; sem bypass.

## Devolução ao Cobogó

- `knowledge/consumers/astronauta.md` atualizado para `last_reviewed: 2026-09-01`, `gap_score: 0`.
- D3 agora registra factual e explicitamente a wheel/runtime instalado e a ausência de claim de canal público não verificado.
- Nenhum conceito novo, nenhuma contagem incrementada e nenhum pattern promovido: não houve nova solução visual before/after nem segundo consumer convergente.
- Nenhuma escrita foi feita no ProjectProfile; ele serviu apenas como autoridade factual.

## Bloqueios

- `franklinbaldo/cobogo#407` — head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; estado: aberta e não mergeável; condição de retomada: head corrente mergeável com todos os required/published checks verdes.
- `franklinbaldo/the-lab#128` — causa inalterada: `Deploy Site` desabilitado manualmente; condição de retomada: workflow ativo + deploy verde do `main` + recaptura pública comprovando `Source snapshot`.

## Pressão para a próxima rotação

**nenhuma.** Astronauta volta a `gap_score: 0`; aplicar novamente urgência → follow-up causal verde → maior gap/antiguidade. The Lab só retorna com mudança causal no deploy e #407 só vence quando o head atual estiver mergeável e verde.
