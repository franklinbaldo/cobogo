---
type: RoundReport
routine: cobogo
date: 2026-09-01T00:57:54-04:00
focus: franklinbaldo/causaganha
candidates: [franklinbaldo/causaganha, franklinbaldo/intuit, franklinbaldo/the-lab]
outcome: inspection-only
prs: []
merged: []
captures: []
blocked: [franklinbaldo/cobogo#407, franklinbaldo/the-lab#128]
---

## Foco e alternativas

As três skills operacionais e os três RoundReports mais recentes foram relidos antes da seleção. `franklinbaldo/cobogo#407` continua aberto no head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e` e não é mergeável, portanto não decide por follow-up causal. `franklinbaldo/the-lab#128` continua aberto; `main` de The Lab permanece exatamente em `468b14f1e1c627eafbc4fc91513a28e6e2521aa3`, o mesmo merge de 2026-08-14 que já contém `Source snapshot`. Como não apareceu mudança causal no bloqueio de deploy, a pressão da rodada anterior foi explicitamente descartada para não fabricar um terceiro relatório redundante sobre o mesmo estado.

Entre os ConsumerCards ativos restantes, `franklinbaldo/causaganha` tem `gap_score: 0` e `last_reviewed: 2026-08-30`, mais antigo que os cards já revisados em 2026-08-31/2026-09-01 observados nesta rotação. Ele decide o foco pelo desempate de antiguidade dentro do maior gap remanescente (zero). `franklinbaldo/intuit` fica como alternativa recente; The Lab fica fora desta escolha somente pela regra causal acima, não por desaparecimento do seu gap.

## Leitor de fora — antes

**D1 — o que a pessoa pode fazer diretamente no site.** Pelo card e pelo estado publicado já verificado, o leitor pode consultar processo por CNJ, pesquisar publicações por texto/OAB/parte e explorar cobertura; a navegação organiza o produto por jobs reais e a home apresenta Arquivo / Estado / Teor. **Gap:** nenhum gap material atual registrado. As PRs web #911/#912/#913 estão abertas e não são tratadas como capacidade publicada.

**D2 — o que o projeto faz por trás e por que existe.** O `ProjectProfile` vigente define CausaGanha como camada cívica verificável sobre rastros judiciais públicos, separando arquivo, estado, teor, significado, proveniência, freshness e limites de cobertura. `/sobre` já foi implantada para explicar progressivamente esse modelo e as fronteiras de prova por fonte. **Gap:** nenhum gap material atual registrado. O novo commit de `main` observado nesta rodada (`f661614562709665ca4e354a273536df6762756e`) altera somente o canário/SLO de publicação→arquivo e sua documentação/testes; não muda a superfície explicativa.

**D3 — o que a pessoa pode fazer sozinha com os artefatos publicados.** O leitor pode reutilizar o arquivo via Internet Archive, `catalog.sql`, `sync-manifest.parquet`, `indice_processual.parquet` e Parquets por fonte conforme disponibilidade, com receita DuckDB documentada. **Gap:** nenhum gap material atual registrado. A existência de novos trabalhos MCP em PR não altera os artefatos publicados de D3 até merge/publicação verificável.

## Gap escolhido e critério

**Nenhum gap fechável nesta rodada.** Critério verificável da rodada sem intervenção: se a inspeção do `main`, do `ProjectProfile`, do README e dos sinais de publicação não revelar regressão factual na superfície nem capacidade já incorporada que permaneça invisível ao leitor, não abrir PR de UI nem transformar trabalho futuro em gap atual. O resultado esperado é `inspection-only`, com `gap_score: 0` preservado e o card atualizado apenas pelos fatos que realmente mudaram.

## Capturas

- Antes: nenhuma captura nova. Não há mudança visual proposta e as capacidades/capturas determinísticas da última mudança de superfície permanecem registradas no card.
- Depois: não aplicável; nenhuma byte de superfície foi alterada nesta rodada.
- Save Page Now: nenhum.

## Ações

- Revalidado `franklinbaldo/the-lab#128` e o `main` de The Lab; nenhum sinal causal novo.
- Revalidado `franklinbaldo/cobogo#407`: aberta, head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`, `mergeable: false`; nenhum bypass.
- Lido o ConsumerCard de CausaGanha, o `ProjectProfile` no workspace e o README atual.
- Revalidado `main` de CausaGanha em `f661614562709665ca4e354a273536df6762756e` (2026-09-01): commit operacional do canário/SLO, sem arquivos de superfície no diff.
- Observadas PRs abertas #911/#912/#913 (site) e #942/#943/#944/#945/#947/#948/#949 (ciclo MCP/teor); todas continuam evidência de trabalho em curso, não de capacidade incorporada na superfície atual.
- Nenhuma PR substantiva aberta; nenhuma issue de capacidade/sinergia nova, porque o ProjectProfile já registra o marco semântico atual e os bloqueios pertinentes.

## Fatos observados

- CausaGanha continua `status: active` e o card anterior registrava `gap_score: 0`.
- `main` avançou de `d8c5102e...` para `f661614562709665ca4e354a273536df6762756e` depois da última revisão do card.
- O commit novo toca `docs/SERVICE_OBJECTIVES.md`, `scripts/canary_check.py` e `tests/test_canary_check.py`; não toca `web/` nem materializa as PRs web abertas.
- O novo canário adiciona uma proxy de backlog `pending_real` para o SLO publicação→arquivo e documenta que ela não mede literalmente atraso em horas.
- O README ainda descreve a superfície incorporada, incluindo as sete tools MCP atuais; as tools/contratos adicionais em PRs abertas não devem ser antecipadas como fatos do produto.
- O `ProjectProfile` foi revisado em 2026-08-31 e já separa explicitamente o trabalho web recente do próximo marco semântico da RFC 0015.

## Inferências

- Não há justificativa para uma PR de superfície nesta rodada: a mudança nova de `main` é operacional e não criou drift visível D1/D2/D3.
- PR aberta não é capacidade publicada; atualizar o card para refletir #911/#912/#913 ou a stack MCP como presente seria marketing antecipado e violaria o boundary factual.
- A inspeção confirma que o `gap_score: 0` continua mais informativo que fabricar um gap a partir do backlog.

## Resultado do gap

**inspection-only / fechado por não intervenção.** O critério foi satisfeito: nenhum gap atual foi encontrado e nenhuma atividade visual foi fabricada. D1, D2 e D3 permanecem perceptíveis de forma compatível com o repo incorporado; as melhorias ainda em PR continuam fora do contrato atual.

## Gates

- Nenhuma PR criada ou mergeada nesta rodada.
- `franklinbaldo/cobogo#407`: head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; aberta e não mergeável. Estado final: sem merge.
- Mudanças de knowledge desta rodada não alteram componente/pattern compartilhado e não exercem o gate de promoção da seção 6.

## Devolução ao Cobogó

- `knowledge/consumers/causaganha.md` será atualizado para `last_reviewed: 2026-09-01`, preservando `gap_score: 0` e registrando o avanço operacional de `main` sem apresentá-lo como mudança visual.
- Nenhum conceito novo: não houve solução nova de superfície com proveniência before/after.
- Nenhum pattern promovido ou contagem incrementada.
- Nenhuma issue no workspace: o ProjectProfile já contém o estado/marco semântico e não surgiu capacidade ou sinergia nova fora dele.

## Bloqueios

- `franklinbaldo/the-lab#128` — `main` `468b14f1e1c627eafbc4fc91513a28e6e2521aa3`; causa e condição de retomada permanecem as registradas: reabilitar Deploy Site, obter deploy verde e recapturar a provenance publicada. Sem mudança causal nesta rodada.
- `franklinbaldo/cobogo#407` — head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; aberta e não mergeável; condição de retomada: reconciliar a suíte e obter required checks verdes no head corrente.

## Pressão para a próxima rotação

**franklinbaldo/intuit** — se continuar ativo e sem urgência/follow-up superior, usar como próximo candidato de inspeção pela rotação recente, sem reabrir gaps já fechados. The Lab só volta ao foco se #128 ou o estado de deploy mudar causalmente.
