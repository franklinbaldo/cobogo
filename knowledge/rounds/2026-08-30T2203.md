---
type: RoundReport
routine: cobogo
date: 2026-08-30T22:03:00-04:00
focus: franklinbaldo/cobogo
candidates: [franklinbaldo/cobogo, ovigialocal/ovigialocal.github.io, franklinbaldo/causaganha]
outcome: blocked
prs: [franklinbaldo/cobogo#407]
merged: []
captures: [github-actions:33349546361]
blocked: [franklinbaldo/cobogo#407]
---

# Rodada Cobogó — 2026-08-30 22:03 -04

## Foco e alternativas

A rodada começou pelo follow-up causal próprio. `franklinbaldo/cobogo#407` ainda estava aberta, vermelha e baseada em um `main` antigo; foi aplicada a única tentativa de correção admitida: a branch `fix/restore-vitest-gate` foi movida para o `main` corrente e recebeu novamente apenas as três mudanças originais (`npm test`, execução de unit tests no CI e expectativa semântica do `Alert`). O head resultante `32b3897335df837e0b49035ecce77d4d59fa3738` teve `capture` e GitGuardian verdes, mas `build` vermelho porque o novo passo `Unit tests` encontrou 65 falhas em 152 testes. Como uma segunda rodada de remediação excederia o orçamento causal, Cobogó torna-se o foco bloqueado desta rodada.

O Vigia continua com `gap_score: 0`; sua nova RFC #11 é trabalho de arquitetura/publicação e a própria descrição exige revisão conjunta com a Redação, portanto não é merge automático desta rotina. CausaGanha continua com `gap_score: 0` depois do deploy de `/sobre` fechado na rodada anterior. Nenhum segundo PR substantivo foi iniciado.

## Leitor de fora — antes

### D1 — o que a pessoa pode fazer diretamente no site

A configuração Astro prova a referência pública em `https://franklinbaldo.github.io/cobogo/`, com base `/cobogo`, documentação/specimens e busca. A intervenção causal #407 não altera HTML/CSS de produto.

**Gap D1:** nenhum gap material novo comprovado nesta rodada.

### D2 — o que o projeto faz por trás e por que existe

O ProjectProfile define Cobogó como sistema de design e conhecimento de superfície cuja gramática cresce por evidência real de consumers e preserva parentesco sem uniformidade. Em contraste, o README ainda encerra o objetivo com `Unificar a interface dos projetos`.

**Gap D2 [fato]:** a formulação pública do README reduz a tese atual a uniformização de interfaces, embora o corpus canônico prove justamente a fronteira oposta. O aviso de migração da API é honesto e deve ser preservado; o problema é o objetivo editorial obsoleto.

### D3 — o que a pessoa pode fazer sozinha com os artefatos publicados

O repositório documenta instalação Git, entry points CSS e componentes Svelte. O README diferencia explicitamente a API embarcada hoje da API-alvo da migração.

**Gap D3:** nenhum gap material novo comprovado nesta rodada.

## Gap escolhido e critério

**Gap escolhido:** nenhum gap visual foi executado nesta rodada. O orçamento substantivo foi consumido pelo follow-up causal #407 antes da seleção de uma nova intervenção de superfície.

**Critério verificável do bloqueio:** #407 só pode ser mergeada quando o head corrente executar `npm test` no CI e o job `build` ficar verde sem remover, pular ou enfraquecer o gate. A captura visual verde não substitui esse critério.

## Capturas

### Antes

- nenhuma captura nova necessária: #407 é mudança de gate não visual.
- referência de superfície: `https://franklinbaldo.github.io/cobogo/`, configuração `site/base` confirmada no `main`.

### Depois

- workflow `Visual preview`, run `33349546361`, check `capture`: **success** no head `32b3897335df837e0b49035ecce77d4d59fa3738`.
- a captura serve como evidência de não-regressão visual do follow-up, não como fechamento do gate Vitest.

## Ações

- skills `brazilian-web-design`, `cobogo-design-review` e `cobogo-consumer-synergy` relidas.
- RoundReports `2026-08-30T2102`, `T1957` e `T1902` relidos.
- #407 rebaseada mecanicamente no `main` corrente e reaberta com head `32b3897335df837e0b49035ecce77d4d59fa3738`.
- run CI `33349546330`: `Unit tests` falhou; `Astro check` e `Build` foram corretamente pulados após a falha.
- run visual `33349546361`: `capture` verde.
- consumer card `knowledge/consumers/cobogo.md` migrado ao schema `ConsumerCard` e reconciliado com ProjectProfile/README/evidência do CI.
- nenhuma mudança feita no ProjectProfile do workspace.

## Fatos observados

- `Alert.test.ts`, razão pontual da versão anterior da #407, agora passa: 5/5.
- a suíte inteira, quando finalmente executada no CI, tem **29 arquivos de teste: 20 falham, 9 passam; 152 testes: 65 falham, 87 passam**.
- as falhas atravessam `Drawer`, `Combobox`, `Notification`, `Tooltip`, `Checkbox`, `FormField`, `Radio`, `Select`, `Switch`, `FileInput`, `TextInput`, pickers, `Badge` e validation-state suites.
- uma família grande espera classes legadas (`invalid`, `valid`, classes de tamanho/posição) que a migração semântica atual já não emite; outras falhas envolvem seletores/estrutura DOM antigos e interações do Tooltip.
- o README declara explicitamente que a migração está em andamento e que snippets descrevem a API-alvo; ao final, porém, ainda afirma `Unificar a interface dos projetos`.

## Inferências

- #407 revelou uma dívida maior do que seu patch original: o problema já não é “ligar Vitest no CI”, mas reconciliar a suíte legada com o contrato semântico atual sem converter testes em mera aprovação automática.
- corrigir 65 asserts dentro desta rodada exigiria decidir, componente por componente, se o teste ou o runtime é a fonte de verdade. Isso excede uma correção causal mecânica e não deve ser feito às cegas.
- a formulação `Unificar a interface dos projetos` é drift editorial factual frente ao ProjectProfile, mas sua correção fica para uma rodada de superfície própria; não foi empilhada sobre o bloqueio de CI.

## Resultado do gap

**não fechado** — nenhum gap de leitor foi atacado; o follow-up causal consumiu a rodada e permaneceu bloqueado pelo gate que ele próprio restaurou.

## Gates

- `franklinbaldo/cobogo#407`
  - head: `32b3897335df837e0b49035ecce77d4d59fa3738`
  - GitGuardian: success
  - capture: success
  - build: **failure** em `Unit tests`
  - merge: **não realizado**
  - regra preservada: nenhum verde de SHA anterior foi reaproveitado.

## Devolução ao Cobogó

- `knowledge/consumers/cobogo.md` migrado para `ConsumerCard`, `last_reviewed: 2026-08-30`, `gap_score: 1`.
- gap D2 registrado para a frase obsoleta do README.
- nenhuma promoção de pattern: não houve segunda evidência convergente nova.
- nenhum achado de capacidade/sinergia exigiu edição do workspace.

## Bloqueios

- `franklinbaldo/cobogo#407` — head `32b3897335df837e0b49035ecce77d4d59fa3738`; `npm test` falha em 65/152 testes. Estado: aberto/vermelho. Condição de retomada: reconciliar explicitamente a suíte legada com o contrato atual, distinguindo expectativa fossilizada de regressão real, até o mesmo gate passar sem ser enfraquecido.

## Pressão para a próxima rotação

`franklinbaldo/cobogo` — corrigir o drift editorial factual do README (`Unificar a interface dos projetos`) é um gap D2 pequeno e fechável independentemente da dívida ampla de testes; #407 permanece trilha causal separada até haver uma estratégia explícita para os 65 testes.
