---
type: RoundReport
routine: cobogo
date: 2026-09-02T07:01:19-04:00
focus: franklinbaldo/causaganha
candidates: [franklinbaldo/causaganha, franklinbaldo/leizilla, franklinbaldo/ficha]
outcome: closed
prs: [franklinbaldo/causaganha#999]
merged: [franklinbaldo/causaganha#999]
captures: [artifact:9824255700, artifact:9843589937, artifact:9843687466]
blocked: []
---

## Foco e alternativas

O follow-up causal `franklinbaldo/cobogo#421` foi reconciliado primeiro. Era uma PR de ledger de um arquivo, baseada em `49ff3230...`, que ficou para trás depois de #422/#423; carregar o branch obsoleto exigiria resolver conflito histórico sem produzir conhecimento novo. A PR foi fechada explicitamente como superseded, sem usar verde antigo como autorização.

Com isso resolvido, `franklinbaldo/causaganha` venceu como consumer ativo sem `SurfaceQualityAssessment`, com `gap_score: 0` mas evidência brownfield desktop/mobile suficiente para julgamento opinionado. `franklinbaldo/leizilla` permanece candidato sem baseline. `franklinbaldo/ficha` tem baseline e dívidas já possuídas, portanto não vence um consumer ainda não ranqueado sem mudança causal superior.

As skills `brazilian-web-design`, `cobogo-design-review` e `cobogo-consumer-synergy`, a governança opinionada vigente, os RoundReports recentes, ConsumerCards e ranking foram relidos antes da intervenção.

## Leitor de fora — antes

### D1 — clareza

CausaGanha deixa claros os jobs públicos: consultar processo, pesquisar publicações, explorar cobertura, entender projeto/dados e usar o MCP local. A home organiza múltiplas fontes sob Arquivo / Estado / Teor.

Gap factual D1: nenhum material demonstrado.

### D2 — explicabilidade

`/sobre` explica tese, fluxo fonte → artefato → índice → superfície e fronteiras do que cada fonte sustenta. Ausência, falha e limite não são transformados em afirmações mais fortes.

Gap factual D2: nenhum material demonstrado.

### D3 — autonomia

A superfície e o repo expõem artefatos no Internet Archive, manifests/Parquets, `catalog.sql`/DuckDB, índice processual e MCP read-only local. `/agentes` fornece receita stdio e declara a ausência de endpoint HTTP público.

Gap factual D3: nenhum material demonstrado.

### Assessment pré-intervenção

A evidência sustentava notas máximas em D1/D2/D3 e confiança, mas a captura pós-merge da #979 em `390×844` revelou dívida de UX: a navegação superior mostrava apenas os primeiros destinos e cortava os demais além da borda. O source confirmou `overflow-x: auto` + `flex-wrap: nowrap` + links `white-space: nowrap`, sem affordance lateral explícita.

Hipótese de baseline após correção: D1 4, D2 4, D3 4, UX 3, UI 4, Confiança 4 = 23/24 (`reference`), desde que o mesmo harness mostrasse os destinos perceptíveis no mobile e o deploy/recaptura do landing passassem.

## Gap escolhido e critério

Gap escolhido: **discoverability da navegação primária em viewport estreito**.

O critério verificável foi congelado em `franklinbaldo/causaganha#998` antes da edição de código: em `390×844`, todos os destinos de primeiro nível devem ficar perceptíveis sem depender de scroll horizontal oculto; desktop e identidade visual devem permanecer preservados; o workflow visual existente deve validar a mudança.

Nota de procedimento: o arquivo deste RoundReport foi persistido após a execução da mudança. Para não fingir pré-registro retroativo, a evidência temporal do critério imutável é a issue #998, criada antes do commit da #999. Rodadas futuras devem persistir também o skeleton do RoundReport antes da edição, como manda a rotina.

## Capturas

- **Before:** artifact `9824255700`, landing anterior da superfície `/agentes`, viewport `390×844`; navegação mostra os primeiros destinos e conteúdo lateral restante não é perceptível.
- **Head #999:** `Product Surface Visual Capture` run `33622720495`, artifact `9843589937`, head `15f58991e406df38073a2f045108cfafff016a4a`. Mobile mostra seis destinos distribuídos em três linhas; desktop preservado.
- **After pós-merge:** `Product Surface Visual Capture` run `33622993648`, artifact `9843687466`, landing `e82f5c00e8093dce1e580d33f4819bf89ac4e9ff`, `success`.
- Método: builds adjacentes com fixtures idênticas e Playwright/Chromium em desktop/mobile pelo workflow canônico do consumer.

## Ações

- Fechada `franklinbaldo/cobogo#421` como superseded, sem merge de ledger stale.
- Aberta `franklinbaldo/causaganha#998` com evidência, posição Cobogó, escape de implementação e critério verificável.
- Implementada `franklinbaldo/causaganha#999`: somente `SiteNav.astro`, trocando dependência de overflow horizontal invisível por wrap local no breakpoint estreito.
- #999 mergeada por squash após gates verdes no head exato.
- Criado `SurfaceQualityAssessment` 23/24 para CausaGanha.
- Criada recomendação `primary-navigation-perceptible-on-narrow-viewports` já em `validated`, pois a aplicação real foi fechada com recaptura do landing.
- ConsumerCard e ranking atualizados.

## Fatos observados

- Antes, o mobile de `/agentes` não tornava todos os seis destinos primários perceptíveis sem gesto lateral.
- O source reproduzia exatamente o mecanismo: `overflow-x: auto`, `flex-wrap: nowrap`, `white-space: nowrap`.
- No head `15f58991...`, CI run `33622720447` e visual run `33622720495` concluíram `success`; CodeQL e GitGuardian também passaram.
- O artifact `9843589937` mostra todos os destinos no mobile após o wrap e preserva desktop.
- #999 estava mergeável no mesmo head e foi mergeada como `e82f5c00...`.
- `Deploy Web` run `33622993677` concluiu build + deploy `success` no landing.
- A recaptura de `main` run `33622993648` concluiu `success`, artifact `9843687466`, ligado ao landing.
- Em páginas funcionais como `/processo`, a navegação global convive com chrome interno próprio; isso não impede a tarefa, mas adiciona uma camada de orientação em comparação com as páginas editoriais/explicativas.

## Inferências

- O gap de navegação não era acessibilidade DOM pura: era discoverability. “Pode rolar horizontalmente” não equivale a “o leitor percebe os destinos”.
- Wrap é uma solução local adequada aqui, não um componente universal. Outros consumers podem usar menu/disclosure explícito e ainda validar a mesma opinião.
- CausaGanha merece faixa `reference` porque clareza, explicabilidade, autonomia, UI e confiança são fortes e auditáveis; UX fica 3/4 para representar a complexidade residual de chrome em algumas superfícies sem inventar uma nova dívida material.
- A nova recomendação sobe diretamente a `validated` com este único consumer; um segundo é requisito apenas para `stable`.

## Resultado do gap

**Fechado.** O critério da #998 sobreviveu sem reescrita: no viewport estreito todos os destinos primários ficaram perceptíveis, o desktop permaneceu coerente, os gates do head passaram, o landing foi implantado e o mesmo workflow recapturou `main` com sucesso.

## Gates

- `franklinbaldo/causaganha#999` — head `15f58991e406df38073a2f045108cfafff016a4a`; CI run `33622720447`: `success`; Product Surface Visual Capture run `33622720495`: `success`; CodeQL: `success`; GitGuardian: `success`; mergeável no mesmo head.
- Merge squash: landing `e82f5c00e8093dce1e580d33f4819bf89ac4e9ff`.
- Landing: `Deploy Web` run `33622993677`: build `success`, deploy `success`; Product Surface Visual Capture run `33622993648`: `success`, artifact `9843687466`.

## Devolução ao Cobogó

- ConsumerCard de CausaGanha atualizado para `last_reviewed: 2026-09-02`, mantendo `gap_score: 0` e registrando as capacidades verificadas no landing.
- `SurfaceQualityAssessment`: D1 4, D2 4, D3 4, UX 3, UI 4, Confiança 4, total 23/24, `reference`.
- Ranking passa a conter O Vigia 23, CausaGanha 23, Baliza 21, Intuit 20, Ficha 18.
- Nova `OpinionatedRecommendation`: `primary-navigation-perceptible-on-narrow-viewports`, maturidade `validated`, `validated_in: [franklinbaldo/causaganha]`.
- Nenhuma aparência do CausaGanha foi promovida a padrão compartilhado; a regra compartilhada é de perceptibilidade/orientação.

## Bloqueios

Nenhum blocker do foco. `franklinbaldo/cobogo#407` continua dívida independente sem mudança causal nesta rodada. `franklinbaldo/cobogo#421` foi encerrada como superseded, não permanece blocker.

## Pressão para a próxima rotação

`franklinbaldo/leizilla` — consumer ativo já observado em rodadas anteriores, ainda fora do ranking de seis dimensões. A próxima rodada deve preferi-lo se continuar ativo e houver evidência visual suficiente; se não houver, deve primeiro criar a capacidade mínima de captura em vez de fabricar nota.
