---
type: RoundReport
routine: cobogo
date: 2026-09-01T03:57:12-04:00
focus: franklinbaldo/ficha
candidates: [franklinbaldo/ficha, franklinbaldo/the-lab, franklinbaldo/leizilla]
outcome: inspection-only
prs: []
merged: []
captures: []
blocked: [franklinbaldo/cobogo#407, franklinbaldo/the-lab#128]
---

## Foco e alternativas

As três skills e os três RoundReports mais recentes foram relidos. `cobogo#407` continua aberta e não mergeável no mesmo head, portanto não vence por follow-up causal. `the-lab#128` continua aberto sem mudança causal: Deploy Site permanece documentado como desabilitado manualmente, sem evidência nova de deploy/recaptura. A pressão explícita da rodada anterior apontava Ficha; seu card ativo estava em `last_reviewed: 2026-08-31`, `gap_score: 0`.

## Leitor de fora — antes

**D1 — no site.** O leitor pode consultar a competência publicada no navegador por empresa, pessoa/sócio, endereço e CNAE quando os artefatos correspondentes existem. **Gap:** nenhum material observado; a home já oferece `Consultar aqui` sem prometer modalidades ausentes.

**D2 — por trás.** A superfície e o README explicam snapshots mensais verificáveis da Receita Federal, preservação no Internet Archive, manifest multi-competência, Parquets/protobuf e consulta client-side com DuckDB-WASM. **Gap:** nenhum material observado. O `main` atual continua o merge #225, cujo diff é hardening de checkpoint ETL e testes, não mudança de superfície.

**D3 — por conta própria.** O leitor pode abrir `manifest.json` e reutilizar os artefatos públicos fora do frontend; o README documenta o mesmo contrato. **Gap:** nenhum material observado.

## Gap escolhido e critério

**Nenhum gap fechável pré-registrado.** Critério verificável: se `main`, README, ProjectProfile, deploy/smoke e PRs abertas não mostrarem uma capacidade já incorporada que a superfície omite nem claim publicado que perdeu suporte, terminar `inspection-only`, não abrir PR de UI e preservar `gap_score: 0`.

## Capturas

- Before novo: nenhum; não houve mudança visual proposta.
- Evidência visual vigente: `.github/workflows/visual-capture.yml` permanece a capacidade canônica registrada para a última mudança material de composição.
- After: não aplicável; nenhuma byte da superfície do consumer foi alterada.
- Save Page Now: nenhum.

## Ações

- Pré-registro criado em `knowledge/rounds/2026-09-01T0357.md` antes de alterar o ConsumerCard.
- Revalidados ConsumerCard, ProjectProfile, README, `main`, runs recentes e PRs abertas de Ficha.
- `ficha/main` confirmado em `65bcd9a052459984d6500dcc1380635b0fe19dfa`.
- Revalidado run `33396841333` (`ETL — upstream + mirror smoke check`) = `success` e CI de push `33298511254` = `success` no mesmo SHA.
- PRs abertas #220, #184, #109 e #108 inspecionadas como trabalho antigo operacional/investigativo; nenhuma representa nova capacidade de superfície incorporada. #220 é draft temporária; #108 declara explicitamente que não deve ser mergeada.
- ConsumerCard atualizado em `knowledge/consumers/ficha.md` para `last_reviewed: 2026-09-01`, mantendo `gap_score: 0` e atualizando `last_verified` das capacidades.
- Nenhuma PR substantiva, nenhum conceito novo e nenhum pattern promovido.

## Fatos observados

- `ficha/main` = `65bcd9a052459984d6500dcc1380635b0fe19dfa`, merge #225 de 2026-08-30.
- O diff de #225 toca hardening ETL/testes e não `web/`.
- Run agendado `33396841333` concluiu `success` em 2026-08-31 no mesmo `main`.
- CI de push `33298511254` concluiu `success` no mesmo SHA.
- O README continua descrevendo consulta browser, manifest público, Parquets/protobuf e reuso independente de forma compatível com o card.
- O ProjectProfile continua alinhado ao mesmo `main` e descreve #225 como estado atual.
- `the-lab#128` permanece aberto com a mesma causa documentada; `cobogo#407` permanece aberta e `mergeable: false` no head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`.

## Inferências

- Não há evidência atual de regressão pública ou de capacidade incorporada invisível ao leitor.
- PR aberta/investigação não é capacidade publicada e não deve aumentar `gap_score`.
- Uma nova captura sem mudança visual não acrescentaria evidência causal; preservar a captura determinística já registrada é mais correto do que fabricar before/after idêntico.

## Resultado do gap

**inspection-only.** O critério pré-registrado foi satisfeito: nenhum claim público perdeu suporte, nenhuma capacidade já incorporada ficou invisível e nenhuma PR de UI foi fabricada. D1, D2 e D3 permanecem coerentes; `gap_score: 0` foi preservado.

## Gates

- Nenhuma PR criada ou mergeada nesta rodada.
- `cobogo#407`: head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`, aberta, `mergeable: false`; sem merge.
- Ficha: `main` `65bcd9a052459984d6500dcc1380635b0fe19dfa`; CI `33298511254` = success; smoke `33396841333` = success.

## Devolução ao Cobogó

- `knowledge/consumers/ficha.md` atualizado para `last_reviewed: 2026-09-01`, `gap_score: 0`, com verificações recentes de CI/smoke e sem inventar mudança visual.
- Nenhum conceito novo: não houve solução nova de superfície nem evidence pair before/after.
- Nenhuma contagem incrementada e nenhum pattern promovido.
- Nenhuma issue no workspace: o ProjectProfile não apresentou capacidade/sinergia nova desatualizada nesta passagem.

## Bloqueios

- `the-lab#128`: causa inalterada; retomar apenas com workflow ativo + deploy verde + recaptura da provenance.
- `cobogo#407`: head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; retomar quando o head corrente tiver required checks verdes e estado mergeável.

## Pressão para a próxima rotação

**franklinbaldo/baliza** — card ativo ainda em `last_reviewed: 2026-08-31`, `gap_score: 0`; usar como candidato natural de antiguidade salvo urgência/follow-up causal superior. The Lab só deve retornar com mudança causal real no deploy.
