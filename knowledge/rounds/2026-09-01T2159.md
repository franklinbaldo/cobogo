---
type: RoundReport
routine: cobogo
date: 2026-09-01T21:59:53-04:00
focus: franklinbaldo/the-lab
candidates: [franklinbaldo/the-lab, franklinbaldo/cobogo]
outcome: blocked
prs: []
merged: []
captures: []
blocked: [franklinbaldo/the-lab#128, franklinbaldo/cobogo#407, franklinbaldo/workspace#30]
---

# Rodada Cobogó — 2026-09-01T2159

## Foco e alternativas

A regra que decide é **Urgência**: `franklinbaldo/the-lab` é consumer ativo e a superfície publicada continua divergindo do `main` porque o deploy está bloqueado. A pressão da rodada anterior apontava exatamente para revalidar esse estado antes de qualquer intervenção substantiva.

`franklinbaldo/cobogo#407` foi revalidada como aberta e não mergeável no head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; portanto não satisfaz o gate de follow-up verde e não supera a urgência do consumer ativo.

## Leitor de fora — antes

- **D1 — o que pode fazer diretamente no site:** a home pública permite navegar por Articles, Lab, Team e About, ler o latest dispatch e entrar no arquivo. **Gap D1:** nenhum gap material novo demonstrado.
- **D2 — o que o projeto faz por trás e por que existe:** o repo prova um Astro público do Rosencrantz Substrate Invariance Lab e `main` contém provenance de source snapshot (`franklinbaldo/rosencrantz-coin` + SHA + data), mas a superfície publicada ainda não mostra `Source snapshot`. **Gap D2:** material; a publicação não deixa o leitor perceber qual snapshot upstream alimenta o conteúdo, apesar de a solução já existir em `main`.
- **D3 — o que pode fazer sozinho com os artefatos publicados:** o leitor consegue abrir GitHub, Papers e Lab State e inspecionar o source público. **Gap D3:** nenhum dataset/API/release autônomo adicional foi provado e ocultado nesta rodada.

## Gap escolhido e critério

Gap escolhido: **D2 — provenance do source snapshot não materializada na superfície publicada**.

**Critério verificável:** o gap só pode ser fechado quando `Deploy Site` estiver ativo, um run do `main` atual concluir build + deploy com sucesso, a superfície pública mostrar `Source snapshot` com repository@short-sha e data conforme `content/provenance.json`, e houver captura visual after reproduzível da rota publicada. Não reescrever este critério para contornar o deploy ou a falta de captura.

## Capturas

- Antes visual canônico: **nenhum novo**. O ConsumerCard já registra que não foi localizada capacidade canônica/reproduzível de screenshot para comparação before/after atual; inspeção HTML não é promovida a captura visual.
- Verificação pública textual: `https://franklinbaldo.github.io/the-lab/`, reaberta nesta rodada; a home responde e não expõe `Source snapshot` no conteúdo observável.
- URL arquivada: nenhuma; Save Page Now não está verificado como capacidade desta superfície.
- Depois: nenhum, pois não houve mudança nem deploy; o loop não pode ser classificado como fechado.

## Ações

- Revalidado `franklinbaldo/the-lab#128`: aberta; a causa registrada permanece `Deploy Site` em `disabled_manually`, com critério de conclusão já cobrindo reativação, run verde, publicação e provenance verificável.
- Revalidado `main` de `franklinbaldo/the-lab`: head `468b14f1e1c627eafbc4fc91513a28e6e2521aa3`, merge da #127; nenhum commit posterior em `main` foi observado.
- Revalidado `.github/workflows/deploy.yml`: continua declarando `workflow_dispatch`, push em `main`, build (`npm ci` + `npm run build`) e `actions/deploy-pages@v4`; o bloqueio não pede alteração de UI.
- Revalidado `content/provenance.json`: source `franklinbaldo/rosencrantz-coin`, SHA `2a054aec8030ae6a7b21f277a2ba6600403835cc`, data `2026-03-22T15:38:44Z`.
- Revalidada a home pública: navegação e conteúdo respondem, mas `Source snapshot` não aparece.
- Revalidado `franklinbaldo/workspace#30`: permanece aberta; o `ProjectProfile` de The Lab continua fora da autoridade canônica do workspace.
- Revalidado `franklinbaldo/cobogo#407`: aberta, `mergeable: false`, head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; nenhum merge.
- Nenhum PR substantivo aberto: o achado material é um bloqueio de pipeline já corretamente roteado ao repo dono; pelos invariantes da rotina, não implementar aqui.

## Fatos observados

- O README atual descreve The Lab como blog público do Rosencrantz Substrate Invariance research lab, Astro estático publicado em GitHub Pages e conteúdo sincronizado de `rosencrantz-coin`.
- `main` continua no landing da #127 (`468b14f1...`) e contém `content/provenance.json` com source repo/SHA/data.
- O workflow de deploy no source possui triggers e jobs adequados ao deploy; a issue #128 já registra como causa confirmada o estado `disabled_manually` no GitHub.
- A home pública atual responde, mas não expõe `Source snapshot` no HTML observável.
- `workspace#30` permanece aberta, logo esta rotina não amplia claims de tese/capacidades/sinergias além do que repo e superfície provam diretamente.
- `cobogo#407` permanece não mergeável no mesmo head já bloqueado.

## Inferências

- Não há nova solução de superfície a desenhar: a solução local de provenance já está implementada em `main`; o único caminho causal para fechar D2 é materializar o deploy e recapturar.
- Alterar HTML/CSS para “compensar” um workflow desabilitado produziria atividade falsa e violaria a fronteira da rotina.
- A ausência de ProjectProfile reduz a autoridade para ampliar narrativa D2, mas não impede reconhecer o bloqueio de publicação já provado diretamente pelo repo e pela issue do dono.
- A provenance progressiva continua evidência de um único consumer e permanece abaixo do gate de promoção ao Cobogó compartilhado.

## Resultado do gap

**Não fechado / bloqueado.** O critério pré-registrado não pode ser satisfeito nesta rodada: não houve deploy do `main` atual nem captura after reproduzível, e a superfície pública segue sem `Source snapshot`.

## Gates

- `franklinbaldo/the-lab#128`: issue aberta; causa registrada `Deploy Site=disabled_manually`; condição de retomada: workflow ativo + run válido do `main` + build/deploy verdes + superfície publicada com provenance + captura after.
- `franklinbaldo/cobogo#407`: head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`, aberta e `mergeable: false`; nenhum merge.
- PR substantivo desta rodada: nenhum.

## Devolução ao Cobogó

- Consumer card: fatos continuam coerentes com `last_reviewed: 2026-09-01` e `gap_score: 1`; nenhuma reescrita necessária porque nenhum fato material mudou desde a última verificação do mesmo dia.
- Conceitos: nenhum novo; provenance progressiva continua observada em apenas um consumer e sem captura after atual.
- Pattern promovido: nenhum; gate de ≥2 consumers não satisfeito.
- Workspace: `franklinbaldo/workspace#30` já possui o achado e critério de conclusão; nenhuma issue duplicada aberta.

## Bloqueios

- `franklinbaldo/the-lab#128` — causa: `Deploy Site` registrado como `disabled_manually`; estado: aberto; condição de retomada: reativar o workflow no repo dono, executar deploy verde do `main` e recapturar a superfície publicada.
- `franklinbaldo/workspace#30` — causa: ausência de `ProjectProfile` canônico; estado: aberto; condição de retomada: criar/reconciliar `knowledge/projects/the-lab.md` no workspace.
- `franklinbaldo/cobogo#407` — head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; estado: aberta e não mergeável; condição de retomada: resolver merge/conflito e obter todos os required checks verdes no head atual.

## Pressão para a próxima rotação

`franklinbaldo/the-lab` somente se #128 mudar de estado ou surgir um deploy novo; caso contrário, aplicar a rotação normal por maior gap/`last_reviewed` e não repetir inspeção sem novo sinal.
