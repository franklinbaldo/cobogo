---
type: RoundReport
routine: cobogo
date: 2026-08-31T20:58:13-04:00
focus: franklinbaldo/the-lab
candidates: [franklinbaldo/the-lab, franklinbaldo/sisprev, franklinbaldo/causaganha]
outcome: blocked
prs: []
merged: []
captures: []
blocked: [franklinbaldo/the-lab#128, franklinbaldo/cobogo#407]
---

## Foco e alternativas

O gate de membership foi aplicado antes da seleção: somente cards atuais do corpus entraram na rotação; cards removidos/históricos não participaram de urgência, gap, antiguidade ou pressão. A busca pelo schema atual encontrou 12 cards `status: active`, e somente `the-lab.md` registra `gap_score: 1`.

`franklinbaldo/the-lab` continua ganhando pela primeira regra de desempate, urgência. O repositório resolve ao vivo, `main` continua no commit `468b14f1e1c627eafbc4fc91513a28e6e2521aa3` de 2026-08-14, a issue #128 continua aberta e a superfície Pages responde sem a provenance que já existe em `main`. `sisprev` e `causaganha` permanecem alternativas sem urgência superior demonstrada nesta passagem. A pressão histórica para consumers removidos foi ignorada pelo gate fail-closed.

## Leitor de fora — antes

**D1 — o que a pessoa pode fazer diretamente no site.** A home pública continua permitindo navegar por Articles, Lab, Team e About, ler o latest dispatch e entrar no arquivo de pesquisa. **Gap:** nenhum D1 material novo observado.

**D2 — o que o projeto faz por trás e por que existe.** A superfície explica o Rosencrantz Substrate Invariance Lab e oferece acesso ao arquivo; o repo prova que o site é alimentado por conteúdo do `franklinbaldo/rosencrantz-coin` e que `main` já contém uma inscrição `Source snapshot` com repo/SHA/data upstream. **Gap:** material e inalterado. A home pública observada ainda termina em `GitHub · Papers · Lab State`, sem `Source snapshot`, portanto o leitor não consegue identificar na própria superfície qual snapshot upstream materializou aquele estado.

**D3 — o que a pessoa pode fazer sozinha com os artefatos publicados.** O leitor pode seguir GitHub, Papers e Lab State e ler o arquivo público. **Gap:** nenhum dataset/API/release adicional foi provado pelo repo e ocultado pela superfície nesta passagem.

O `ProjectProfile` continua ausente do workspace e já é rastreado por `franklinbaldo/workspace#30`; esta rotina não amplia claims institucionais enquanto essa autoridade não for restaurada.

## Gap escolhido e critério

Gap escolhido: **D2 — materializar na superfície publicada a provenance de snapshot upstream que já existe em `main`.**

Critério verificável e imutável: **a home pública deve renderizar no footer `Source snapshot: franklinbaldo/rosencrantz-coin@<short-sha>` acompanhado da data do snapshot; o deploy deve corresponder ao `main` que contém essa implementação e uma captura da home deve mostrar a inscrição.**

## Capturas

- **Before textual público revalidado** — `https://franklinbaldo.github.io/the-lab/`, observado em 2026-08-31T20:58:13-04:00; footer ainda termina em GitHub · Papers · Lab State, sem `Source snapshot`.
- **Before visual novo** — nenhum. Não foi localizada capacidade canônica/reproduzível de screenshot exercitável para o estado atual.
- **After** — nenhum. Não houve deploy novo nem mudança de superfície nesta rodada.
- **Save Page Now** — nenhum mecanismo disponível/verificado.

## Ações

- Revalidado `franklinbaldo/the-lab#128`: segue aberta; nenhum comentário anterior existia.
- Revalidado `main` de The Lab: sem commits posteriores ao merge `468b14f1e1c627eafbc4fc91513a28e6e2521aa3` da #127.
- Revalidada a home pública: continua sem `Source snapshot`.
- Adicionado comentário de evidência em `franklinbaldo/the-lab#128` (`comment_id: 5486943580`) com o estado atual e o mesmo critério de conclusão, sem implementar o bug operacional fora da autoridade desta rotina.
- Revalidado `franklinbaldo/cobogo#407`: head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; CI `33433669418` continua `failure`, Greenfield visual preview `33433669444` continua `success`, Auto-merge Jules `33433669641` continua `skipped`.
- Nenhuma PR substantiva foi aberta: a solução de superfície já está em `main`; o blocker real continua sendo o deploy.

## Fatos observados

- `the-lab#128` permanece aberta e documenta `Deploy Site` como `disabled_manually`.
- O último commit de `main` continua `468b14f1e1c627eafbc4fc91513a28e6e2521aa3`, de 2026-08-14.
- A home Pages responde, mas ainda não mostra a inscrição `Source snapshot` presente no código de `main`.
- A listagem recente de Actions não apresenta uma execução nova que materialize o merge da #127 na superfície pública.
- O card atual de The Lab continua `status: active`, `last_reviewed: 2026-08-31`, `gap_score: 1`; nenhum fato do card ficou obsoleto nesta passagem.
- `workspace#30` continua aberta e o ProjectProfile de The Lab continua fora da autoridade desta rotina.
- `cobogo#407` continua no mesmo head e com CI vermelho.

## Inferências

- Não existe uma segunda correção de UI a fazer: o código necessário para o critério já existe. Nova PR de superfície seria duplicação sem causalidade.
- A única ação útil dentro do orçamento desta rodada foi fortalecer o issue dono do blocker com evidência fresca; reabilitar workflow é dívida operacional do repo e permanece fora do escopo de design desta rotina.
- Pages disponível continua não sendo prova de que Pages representa o `main` atual.
- Como não houve mudança visual, fabricar captura before/after não acrescentaria evidência causal e não foi usado para simular fechamento.

## Resultado do gap

**não fechado / blocked.** O critério permanece intacto e não foi satisfeito: a provenance ainda não aparece na home pública, não existe deploy verde do `main` que contém a implementação e não há captura after.

## Gates

- `franklinbaldo/the-lab`: nenhuma PR criada nesta rodada. O blocker operacional continua #128; não houve merge/deploy/captura a promover.
- `franklinbaldo/cobogo#407`: head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; CI run `33433669418` = `failure`; Greenfield visual preview `33433669444` = `success`; Auto-merge Jules `33433669641` = `skipped`. Estado final: aberta/bloqueada, sem merge.

## Devolução ao Cobogó

- `knowledge/consumers/the-lab.md` já estava no schema atual, revisado hoje e factual; não foi reescrito sem mudança de fato.
- Nenhum conceito novo foi criado e nenhuma contagem foi incrementada: continua faltando fechamento before/after atual e segundo consumer convergente para promoção.
- `workspace#30` continua como destino correto do achado de autoridade factual; nenhuma escrita cruzou para ProjectProfile.

## Bloqueios

- `franklinbaldo/the-lab#128` — SHA observado `468b14f1e1c627eafbc4fc91513a28e6e2521aa3`; causa: `Deploy Site` desabilitado manualmente; estado: provenance implementada em `main`, não materializada na home pública; condição de retomada: workflow ativo + execução verde do `main` atual + captura pública do footer com `Source snapshot`.
- Captura atual da home — causa: nenhuma capacidade canônica/reproduzível localizada para screenshot atual; condição de retomada: capacidade de captura identificada por SHA/viewport/rota, sem reutilizar screenshot histórico como atual.
- `franklinbaldo/cobogo#407` — head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; causa: suíte Vitest ainda falha; condição de retomada: reconciliar contrato atual vs expectativas fossilizadas vs regressões reais e obter todos os required checks verdes no head corrente.

## Pressão para a próxima rotação

**nenhuma.** Enquanto #128 continuar comprovadamente aberto e a superfície pública continuar divergindo de `main`, The Lab volta a ganhar por urgência. Uma rodada futura deve revalidar isso e, se não houver mudança, evitar nova intervenção substantiva ou relatório redundante além de evidência realmente nova.