---
type: RoundReport
routine: cobogo
date: 2026-08-31T13:01:00-04:00
focus: franklinbaldo/intuit
candidates: [franklinbaldo/intuit, franklinbaldo/cobogo]
outcome: closed
prs: [franklinbaldo/intuit#27]
merged: [franklinbaldo/intuit#27]
captures: [intuit-home-2bbf3376e2b7f461ddd7e90e1a1a8cb791ac0d06, intuit-home-8178f348600f231e567589e45174f4fc507f6986]
blocked: [franklinbaldo/cobogo#407]
---

## Foco e alternativas

Não há urgência nova comprovada. `franklinbaldo/cobogo#407` continua própria e aberta, porém não verde/mergeável, portanto não decide por follow-up causal. `franklinbaldo/intuit` permanece com `gap_score: 1` e pressão explícita da rodada anterior; a #26 já removeu o bloqueio de captura que impedia fechar o residual visual de #14. Foco: Intuit.

## Leitor de fora — antes

### D1 — o que a pessoa pode fazer diretamente

A pessoa pode editar/colar HTML, renderizar no preview, copiar link, atualizar a URL, limpar o editor, alternar scripts no sandbox e alternar tema. Preview, erros e grupos operacionais já têm semântica acessível.

Gap D1: os controles principais ainda não têm foco visual explícito equivalente ao hover para navegação por teclado, e as transições/escala atuais não respeitam explicitamente `prefers-reduced-motion`. O repo prova o residual em `intuit#14`; o baseline visual reproduzível de `main` existe desde #26.

### D2 — o que o projeto faz por trás e por que existe

Intuit é um renderer HTML/visual-testing client-side cuja relação principal é fonte/edit → preview renderizado. O default permanece `sandbox=""`, scripts são opt-in, o runtime usa `srcdoc` e o estado pode ser transportado por `?data=`, `?b64=` e Gist.

Gap D2: nenhum material estabelecido.

### D3 — o que a pessoa pode fazer sozinha com os artefatos publicados

A pessoa pode transportar/reproduzir estado por URL/Base64/Gist e executar o source checkout como aplicação estática. Não há release/dataset/API autônoma adicional escondida pela superfície.

Gap D3: nenhum material estabelecido.

## Gap escolhido e critério

Gap escolhido: D1 — foco visual explícito para teclado + tratamento de `prefers-reduced-motion` nas transições/escala existentes.

Critério verificável congelado: depois da mudança, os controles principais devem exibir um indicador de foco perceptível ao receber foco por teclado; em contexto `reducedMotion: reduce`, botões e labels/toggles não devem manter scaling/transições animadas relevantes. Assertions Playwright direcionadas devem provar ambos sem alterar `sandbox=""`, `srcdoc`, Base64, tema ou fluxo editor→preview. A mesma captura `1440x900` deve produzir o `after`; Test + Visual capture precisam estar verdes no head atual antes do merge; depois do merge, Test + Pages + Visual capture precisam ficar verdes no merge SHA.

## Capturas

Before — URL `https://franklinbaldo.github.io/intuit/`; rota `/intuit/`; commit/deploy `2bbf3376e2b7f461ddd7e90e1a1a8cb791ac0d06`; Visual capture run `33413210167`; artifact `intuit-home-2bbf3376e2b7f461ddd7e90e1a1a8cb791ac0d06` id `9765969339`; viewport `1440x900`; método Playwright versionado + artifact de GitHub Actions.

After PR — mesma URL/rota/viewport/método; head `27674167e4210949508098213da65a57e554c62b`; Visual capture run `33417741529`; artifact `intuit-home-27674167e4210949508098213da65a57e554c62b` id `9767680953`.

After pós-merge — mesma URL/rota/viewport/método; merge/deploy `8178f348600f231e567589e45174f4fc507f6986`; Visual capture run `33417946698`; artifact `intuit-home-8178f348600f231e567589e45174f4fc507f6986` id `9767763311`.

Save Page Now: nenhum.

## Ações

- Skills `brazilian-web-design`, `cobogo-design-review` e `cobogo-consumer-synergy` relidas.
- RoundReport anterior e card Intuit relidos; `ProjectProfile` consultado somente para fatos.
- `intuit#14` confirmada aberta e limitada ao residual foco/reduced-motion antes da intervenção; fechada automaticamente por #27 após merge.
- Pré-registro congelado antes de alteração de código no consumer.
- `franklinbaldo/intuit#27` criada sobre `main`, com CSS local para `:focus-visible`/`prefers-reduced-motion` e assertions Playwright direcionadas.
- Primeira execução de Test no head `83322935184f746f5d26348196adf430f6d9711a` falhou apenas porque a assertion exigia `outline-offset >= 3px`; 53/54 Playwright passaram e o browser computou outline de 3px com offset positivo de 1px.
- Única tentativa de correção usada para ajustar a assertion ao contrato perceptível (`outline >= 3px` + separação positiva), sem enfraquecer a UI nem alterar o critério pré-registrado.
- #27 squash-mergeada somente após Test + Visual capture verdes no head atual.
- Pages, Test e Visual capture verificados verdes no merge SHA `8178f348600f231e567589e45174f4fc507f6986`.

## Fatos observados

- `main` Intuit no início: `2bbf3376e2b7f461ddd7e90e1a1a8cb791ac0d06`.
- Os quatro botões usavam `transition-all duration-200 transform hover:scale-105`, sem classe de `focus-visible` explícita.
- Os labels dos toggles usavam `transition-colors`; inputs já tinham `focus:ring-2`.
- #27 adicionou outline explícito para controles focados por teclado e tratamento de reduced-motion que remove o hover scaling e reduz transições/animações relevantes.
- Playwright passou no head final `27674167e4210949508098213da65a57e554c62b`, incluindo assertions de foco, reduced-motion e preservação de `sandbox=""` + `srcdoc`.
- Merge SHA: `8178f348600f231e567589e45174f4fc507f6986`.
- Pós-merge: Pages run `33417946679`, Test run `33417946657` e Visual capture run `33417946698`, todos verdes no mesmo SHA.
- `intuit#14` está fechada como concluída.

## Inferências

- A menor intervenção coerente foi CSS local + assertions Playwright; não houve razão para alterar arquitetura, identidade visual ou promover pattern compartilhado.
- `prefers-reduced-motion` retirou movimento sem retirar affordance visual estática.
- A falha inicial do teste foi evidência útil: o contrato relevante é foco perceptível e separado do componente, não um valor arbitrário de offset.

## Resultado do gap

Fechado. O critério congelado foi satisfeito: teclado recebe indicador de foco explícito e perceptível; reduced-motion remove scaling e reduz transições relevantes; `sandbox=""`, `srcdoc`, Base64, tema e fluxo editor→preview permaneceram protegidos pela suíte. A mesma captura versionada produziu before e after, e a captura pós-merge existe no merge SHA.

## Gates

- `franklinbaldo/intuit#27` — head final `27674167e4210949508098213da65a57e554c62b`; Test run `33417741517` verde; Visual capture run `33417741529` verde; mergeável antes do merge; squash merge `8178f348600f231e567589e45174f4fc507f6986`.
- Pós-merge Intuit — Pages `33417946679` verde; Test `33417946657` verde; Visual capture `33417946698` verde no SHA `8178f348600f231e567589e45174f4fc507f6986`.
- `franklinbaldo/cobogo#407` — permanece aberta, head `32b3897335df837e0b49035ecce77d4d59fa3738`, não mergeável; sem merge/bypass.

## Devolução ao Cobogó

- Consumer card de Intuit atualizado em 2026-08-31; `gap_score` caiu de 1 para 0.
- Nenhum pattern promovido: há uma solução local comprovada para foco + reduced-motion, mas não existe evidência before/after convergente em dois consumers para o gate da seção 6.
- Nenhum novo achado de capacidade/sinergia: a capacidade de captura já havia sido devolvida ao workspace em `workspace#27` na rodada anterior.

## Bloqueios

- `franklinbaldo/cobogo#407` — head `32b3897335df837e0b49035ecce77d4d59fa3738`; dívida ampla de self-conformance conhecida; estado: aberta e não mergeável; condição de retomada: distinguir contratos vigentes de expectativas fossilizadas e obter required checks verdes no head atual.

## Pressão para a próxima rotação

nenhuma
