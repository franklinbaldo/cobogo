---
type: RoundReport
routine: cobogo
date: 2026-09-02T01:02:00-04:00
focus: franklinbaldo/intuit
candidates: [franklinbaldo/intuit, franklinbaldo/ficha, franklinbaldo/leizilla]
outcome: closed
prs: [franklinbaldo/intuit#28]
merged: [franklinbaldo/intuit#28]
captures: [artifact:9767763311/home-1440x900.png, artifact:9832628580]
blocked: [franklinbaldo/cobogo#407]
---

# Rodada Cobogó — 2026-09-02T0102

## Foco e alternativas

Não surgiu urgência nova. `franklinbaldo/cobogo#407` permanece no mesmo estado não mergeável, sem mudança causal, e `franklinbaldo/aleph-the-game#6` continua sem evidência nova; portanto nenhum deles deve monopolizar a rotação. O corpus de `SurfaceQualityAssessment` ainda está vazio. Entre consumers ativos empatados com `last_reviewed: 2026-09-01`, Intuit foi escolhido porque já possui captura browser causal por SHA e gate Playwright, o que torna possível produzir o primeiro baseline completo sem inventar nota. Ficha e Leizilla permanecem candidatos válidos para as rodadas seguintes.

## Leitor de fora — antes

**D1 — Clareza.** A home mostra imediatamente um editor HTML, ação `Render` e região `Preview`; a captura desktop liga visualmente entrada, ação e saída. Gap factual preliminar: nenhum material na entrada natural.

**D2 — Explicabilidade.** A inscrição `A minimalist HTML renderer & visual testing tool` explica a finalidade imediata; README/ProjectProfile provam que o valor é fonte/edit → preview renderizado, com sandbox estrito por default. Gap factual preliminar: a home pública explica pouco da fronteira sandbox/scripts, mas a criticidade só será decidida após inspeção completa do fluxo.

**D3 — Autonomia.** O produto expõe `Copy Link`/`Update URL`, e o README documenta estado transportável via URL/Base64/Gist. Gap factual preliminar: nenhum material estabelecido.

**UX/UI/Confiança.** A captura desktop pós-merge #27 mostra hierarquia legível e relação editor → ações/settings → preview. O repo prova foco visível, reduced-motion e semântica por Playwright. Porém a capacidade de captura registra apenas `1440x900`; não há evidência narrow causal no artifact atual. Sem viewport estreito, não é legítimo fechar UI/UX responsiva nem o baseline de seis dimensões.

## Gap escolhido e critério

Gap escolhido: **Confiança/UI — a capacidade canônica de evidência visual do Intuit não cobre viewport estreito, embora responsividade faça parte do baseline opinionado.**

Critério congelado: o workflow `Visual capture` deve produzir, no mesmo SHA e artifact, a home em `1440x900` e em viewport estreito representativo, registrar ambas as dimensões na provenance e continuar verde no head da PR. Depois do merge, a captura pós-merge deve permitir atribuir as seis notas do `SurfaceQualityAssessment` com evidência, sem tratar ausência de observação como nota zero.

Recomendação Cobogó aplicável: `visual-evidence-as-quality-gate`, maturidade `opinionated` até que a aplicação downstream seja provada nesta rodada.

## Capturas

Antes — artifact `9767763311`, `home-1440x900.png`, rota `/intuit/`, merge `8178f348600f231e567589e45174f4fc507f6986`, método Playwright/Chromium via workflow `Visual capture`. A captura mostra editor, ações, settings e preview em desktop. Não existe arquivo narrow no artifact.

Depois — run `33593643021`, artifact `9832628580`, merge `34a3102a61d3676e34597d20606ffe3a53bdfe3d`, rota `/intuit/`, método Playwright/Chromium. O metadata registra `viewports=1440x900,390x844`; o artifact contém `home-1440x900.png` e `home-390x844.png`. A captura narrow preserva título/tagline, editor, grid 2×2 das ações, settings, preview e footer sem overflow aparente ou rótulo truncado.

## Ações

- Pré-registro gravado no Cobogó antes de qualquer alteração no consumer.
- Aberta e mergeada `franklinbaldo/intuit#28`, limitada ao spec/workflow de captura.
- No head `046f7330a417da385520f360c9d2d045a6ab2947`, `test`, `capture` e `GitGuardian Security Checks` concluíram `success`; só então ocorreu squash merge.
- Landing em `main`: `34a3102a61d3676e34597d20606ffe3a53bdfe3d`.
- Pages pós-merge: run `33593643062`, `success`.
- Visual capture pós-merge: run `33593643021`, `success`, artifact `9832628580`.
- Criado o primeiro `SurfaceQualityAssessment` do corpus: Intuit 20/24, faixa boa.
- Ranking inicializado com o único baseline existente, sem atribuir notas aos demais consumers.
- `visual-evidence-as-quality-gate` promovido de `validated` a `stable`: Quem São Eles + Intuit convergem no mesmo contrato com implementações locais diferentes.

## Fatos observados

- Antes da #28, a capacidade canônica do Intuit capturava somente 1440×900.
- O artifact da PR #28 e o artifact pós-merge contêm desktop e 390×844 ligados ao respectivo SHA.
- Em 390 px, a tarefa editor → ações/settings → preview permanece legível e sem overflow aparente.
- Pages publicou o landing da #28 com sucesso.
- O corpus já tinha `visual-evidence-as-quality-gate` validado em `franklinbaldo/quem-sao-eles`; Intuit fornece a segunda aplicação convergente.

## Inferências

- A qualidade do Intuit é **boa (20/24)**: D1 e confiança atingem 4; D2, D3, UX e UI ficam em 3 por serem fortes mas não referência transversal demonstrada.
- A captura dual-viewport no mesmo artifact é uma implementação barata e suficiente do contrato de evidência causal para uma superfície pequena; Cobogó deve preferir proporcionalidade a uma plataforma pesada.
- O ranking com um único baseline é apenas início de corpus, não afirma superioridade sobre consumers ainda não avaliados.

## Resultado do gap

**fechado.** O critério congelado foi satisfeito: o mesmo artifact do SHA pós-merge contém desktop+narrow, provenance registra as duas dimensões, o gate de captura ficou verde e a evidência permitiu produzir assessment completo sem nota inventada.

## Gates

- `franklinbaldo/intuit#28` — head `046f7330a417da385520f360c9d2d045a6ab2947`; `test`, `capture`, GitGuardian = `success`; mergeável; squash merge → `34a3102a61d3676e34597d20606ffe3a53bdfe3d`.
- Pós-merge Intuit — Pages `33593643062`: `success`; Visual capture `33593643021`: `success`.
- `franklinbaldo/cobogo#407` — sem mudança causal, permanece blocker independente e não consumiu tentativa.
- PR de memória do Cobogó — pendente de checks do head atual; só será mergeada com required checks verdes.

## Devolução ao Cobogó

- ConsumerCard atualizado para `last_reviewed: 2026-09-02`, `gap_score: 0`, sem confundir isso com nota global.
- `SurfaceQualityAssessment` criado com 20/24 (`good`).
- Ranking passa a conter apenas Intuit até surgirem outros baselines reais.
- `visual-evidence-as-quality-gate` agora `stable`, com provenance de Quem São Eles e Intuit.
- A linguagem conservadora residual do card foi removida: múltiplos consumers são gate de `stable`, não de formulação/validação inicial.

## Bloqueios

- `franklinbaldo/cobogo#407` — blocker pré-existente, sem mudança causal nesta rodada; condição de retomada permanece a registrada nas rodadas anteriores.

## Pressão para a próxima rotação

`franklinbaldo/ficha` — consumer ativo ainda sem `SurfaceQualityAssessment`, com contrato público forte e capacidade de captura já registrada; produzir o próximo baseline permite começar comparação real sem pré-ranking.
