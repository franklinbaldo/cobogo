---
type: RoundReport
routine: cobogo
date: 2026-08-31T02:06:00-04:00
focus: franklinbaldo/leizilla
candidates: [franklinbaldo/leizilla, franklinbaldo/astronauta, franklinbaldo/cobogo]
outcome: inspection-only
prs: []
merged: []
captures: []
blocked: [franklinbaldo/cobogo#407]
---

## Foco e alternativas

As três skills operacionais foram relidas no início da rodada. Os três RoundReports mais recentes mantinham a sequência Cobogó -> Baliza -> Ficha, todos já reclassificados no schema atual e com `gap_score: 0` no último estado observado.

O follow-up causal `franklinbaldo/cobogo#407` permanece aberto no head `32b3897335df837e0b49035ecce77d4d59fa3738`, agora não mergeável contra o `main` atual. A tentativa substantiva anterior já demonstrou 65 falhas em 152 testes; reconciliar a suíte ampla excede o orçamento desta rodada e não há base para enfraquecer o gate. Portanto ficou registrado como bloqueio, sem merge.

Não foi encontrada urgência de superfície no Leizilla. A pressão da rodada anterior apontava `franklinbaldo/leizilla`, cujo card ainda estava no schema legado (`last_verified: 2026-08-17`) e misturava necessidades compartilháveis futuras com gaps concretos do leitor. `franklinbaldo/astronauta` foi mantido como alternativa porque também permanece no schema legado e tem `last_verified: 2026-08-14`; Cobogó foi descartado por revisão recente e `gap_score: 0`.

## Leitor de fora — antes

### D1 — o que pode fazer no site

A superfície atual permite pesquisar legislação, abrir uma norma, preservar o contexto da busca, navegar pelos dispositivos correspondentes, ler texto estruturado, versões, evidências e cobertura, e chegar aos dados da norma.

Gap D1: nenhum material. O card antigo tratava padrões de busca/leitura como `unmet_need`, mas o journey já implementa essas tarefas. A dívida `data_publicacao -> data_ato` está no domínio/pipeline; a superfície já usa **Data do ato** e não afirma publicação DOE sem prova.

### D2 — o que o projeto faz por trás e por que existe

O README e o `ProjectProfile` explicam uma cadeia pública coerente: fontes oficiais -> descoberta/coleta -> preservação/evidência -> parse + ETL -> dataset versionado no Internet Archive -> Parquet/metadados -> portal estático com DuckDB-WASM. Também separam explicitamente dataset publicado de cobertura completa.

Gap D2: nenhum material. O projeto deixa perceptível tanto a finalidade de preservação/auditabilidade quanto o mecanismo que produz a superfície.

### D3 — o que pode fazer sozinho com os artefatos

O README liga o dataset RO v0, `versoes.parquet` e `dataset_meta.json`, e fornece uma consulta DuckDB mínima sobre exatamente o Parquet usado pelo portal. A página de dados também oferece o handoff para reutilização fora da interface.

Gap D3: nenhum material.

## Gap escolhido e critério

Nenhum gap fechável foi escolhido. Critério da rodada sem intervenção: se D1, D2 e D3 já estiverem sustentados pela superfície e pelos artefatos atuais, não transformar `unmet_needs` legados em trabalho visual artificial. Resultado esperado: migrar o card para `ConsumerCard`, recalcular `gap_score: 0` e não abrir PR de superfície.

## Capturas

Nenhuma captura nova foi produzida porque não houve mudança visualmente material. A revisão não localizou uma capacidade canônica dedicada de captura visual comparável no repositório do Leizilla; isso foi registrado no card sem tratar a ausência como gap do leitor.

URL pública observada/documentada: `https://franklinbaldo.github.io/leizilla/`.

## Ações

- Skills `brazilian-web-design`, `cobogo-design-review` e `cobogo-consumer-synergy` relidas.
- `ProjectProfile` do Leizilla lido no workspace, revisado em 2026-08-30.
- README atual do Leizilla confrontado com o card legado.
- Discovery leve: `main` observado em `631f62af2ff423d0842b91ed8c8410431160b53a`; `Parse + ETL + Release Dataset` run `33308584550` terminou verde no mesmo SHA; PRs abertas são Dependabot/OPF, sem PR própria de superfície pronta para merge desta rotina.
- `knowledge/consumers/leizilla.md` migrado para `ConsumerCard`, `last_reviewed: 2026-08-31`, `gap_score: 0`.
- Nenhuma issue de capacidade/sinergia aberta: #129 já é a autoridade correta para a migração estrutural de data e não surgiu sinergia nova.
- Nenhum pattern promovido.

## Fatos observados

- O `ProjectProfile` marca o Leizilla como ativo e descreve a tese de preservar, estruturar e republicar legislação brasileira como dados públicos auditáveis.
- Rondônia v0 tem dataset público e Parquet reutilizável; o portal usa DuckDB-WASM sobre esse artefato.
- A superfície já corrigiu a apresentação para **Data do ato**, enquanto #129 permanece responsável por migrar o shape canônico do domínio.
- O README liga portal, dataset no Internet Archive, Parquet e metadata da release e inclui consulta DuckDB reproduzível.
- Dataset publicado, cobertura, pipeline e freshness são explicitamente tratados como fatos distintos.
- O último workflow de release observado nesta rodada terminou com sucesso no `main` atual.

## Inferências

- Os `unmet_needs` legados de proveniência/freshness, coverage/status e data-reading não representam três gaps atuais do leitor. Eles descrevem relações de design que a superfície já cobre em grande parte e/ou problemas de pipeline que possuem autoridade própria.
- A dívida #129 não justifica uma nova intervenção de superfície enquanto a UI continuar usando o rótulo epistemicamente correto e não inventar publicação DOE.
- A reutilização via Parquet + metadata + DuckDB é evidência forte de D3 e continua sendo uma realização local do pattern compartilhado `public-artifact-reuse`, não um motivo para padronizar a apresentação dos consumers.

## Resultado do gap

`inspection-only`. O critério foi satisfeito: nenhuma atividade visual foi fabricada. O card agora representa D1/D2/D3 no schema atual e registra `gap_score: 0`.

## Gates

- Nenhuma PR substantiva criada nesta rodada.
- `franklinbaldo/cobogo#407`: aberta, head `32b3897335df837e0b49035ecce77d4d59fa3738`, não mergeável contra o `main` atual; bloqueio conhecido de reconciliação da suíte Vitest, sem merge.
- Leizilla `main` `631f62af2ff423d0842b91ed8c8410431160b53a`: release run `33308584550` verde.

## Devolução ao Cobogó

- `knowledge/consumers/leizilla.md` atualizado para `ConsumerCard`, `last_reviewed: 2026-08-31`, `gap_score: 0`.
- Nenhum conceito novo: `public-artifact-reuse` e a separação entre proveniência, freshness, cobertura e artefato publicado já possuem autoridade existente.
- Nenhum pattern promovido; não surgiu nova evidência em segundo consumer que alterasse gate.

## Bloqueios

- `franklinbaldo/cobogo#407` — SHA `32b3897335df837e0b49035ecce77d4d59fa3738`; causa: a suíte Vitest restaurada revela ampla dívida de expectativas fossilizadas e a branch está atrás do `main` atual. Estado: aberta/não mergeável. Condição de retomada: rebase/reconciliação deliberada da suíte, mantendo os contratos vigentes e deixando o gate integral verde no novo head.

## Pressão para a próxima rotação

`franklinbaldo/astronauta` — card ainda legado, `last_verified: 2026-08-14`, com necessidades de densidade/administração que precisam ser separadas entre gaps reais D1/D2/D3 e requisitos de design-system antes de qualquer intervenção.
