---
type: RoundReport
routine: cobogo
date: 2026-09-01T20:25:24-04:00
focus: franklinbaldo/causaganha
candidates: [franklinbaldo/causaganha, franklinbaldo/cobogo, franklinbaldo/franklinbaldo.github.io]
outcome: closed
prs: [franklinbaldo/causaganha#979]
merged: [franklinbaldo/causaganha#979]
captures:
  - https://api.github.com/repos/franklinbaldo/causaganha/actions/artifacts/9824255700
blocked: [franklinbaldo/cobogo#407]
---

# Rodada Cobogó — 2026-09-01T2025

## Foco e alternativas

A rodada começa pelo follow-up causal explicitamente deixado pela anterior: `franklinbaldo/causaganha#979` já estava mergeada no landing `333308303bcabf681a6513960fa6d53299ffc65b`, mas sem a conclusão observada de deploy + recaptura pós-merge. Esse estado decide o foco antes de novo trabalho substantivo.

`franklinbaldo/cobogo#407` continua aberta no head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e` e agora está `mergeable: false`; não satisfaz o gate de follow-up verde. `franklinbaldo/franklinbaldo.github.io` tem mudança causal relevante para a próxima rotação: o deploy `33535970297`, antes pendente, concluiu `success` na tentativa 2, mas não foi aprofundado aqui para preservar o orçamento de um consumer foco.

## Leitor de fora — antes

Pré-registro causal preservado da rodada `2026-09-01T1857.md`; nenhuma linha do critério foi reescrita após observar os workflows pós-merge.

- **D1 — o que pode fazer diretamente no site:** consultar processos, pesquisar publicações e explorar cobertura/estado das fontes. **Gap:** nenhum gap D1 material novo.
- **D2 — o que o projeto faz por trás e por que existe:** a superfície já explica arquivo, estado, teor, proveniência, freshness e limites de cobertura conforme o `ProjectProfile`. **Gap:** nenhum gap D2 material novo.
- **D3 — o que pode fazer sozinho com os artefatos publicados:** o repo prova MCP read-only local por stdio, mas o fechamento ainda dependia de demonstrar que a nova rota `/agentes`, já mergeada, foi de fato implantada e recapturada após o merge. **Gap:** evidência final de publicação/recaptura ainda faltava no início deste follow-up.

## Gap escolhido e critério

Gap escolhido: D3, autoatendimento público do MCP local.

**Critério verificável, sem alteração:** depois da mudança, a navegação pública expõe `Agentes` e `/agentes` mostra (1) Arquivo / Estado / Teor ligados às tools MCP provadas pelo repo, (2) configuração local com `uv run causaganha-mcp`, e (3) transporte remoto como indisponível; a evidência before/after deve usar a própria rota `/agentes`, e a rodada só fecha após deploy verificado e recaptura publicada.

## Capturas

### Antes

- URL original alvo: `https://franklinbaldo.github.io/causaganha/agentes`.
- URL arquivada: nenhuma; Save Page Now não é capacidade registrada para esta superfície.
- artifact pós-merge: `https://api.github.com/repos/franklinbaldo/causaganha/actions/artifacts/9824255700`.
- timestamp: `2026-09-01T23:01:54Z` (artifact criado).
- rota/método: `/causaganha/agentes`; workflow `Agents Surface Visual Capture`, builds adjacentes com fixtures idênticas, serving local que reproduz a resolução da clean URL do Pages, Playwright desktop 1280×900 e mobile 390×844.
- estado before: 404 `File not found`, provando que a rota não existia no estado anterior ao landing.

### Depois

- URL original alvo: `https://franklinbaldo.github.io/causaganha/agentes`.
- artifact: o mesmo `9824255700`, produzido pelo run pós-merge `33568994835` no landing `333308303bcabf681a6513960fa6d53299ffc65b`.
- estado after: a captura mostra `Use o CausaGanha com um agente`, navegação `Usar com agente`, os jobs Arquivo / Estado / Teor, configuração stdio com `uv run causaganha-mcp` e o estado remoto `aguardando rollout validado`/sem endpoint oficial.
- método: idêntico ao before, nas duas viewports.

## Ações

- Confirmado `Deploy Web` run `33568994734`: `completed/success` no landing `333308303bcabf681a6513960fa6d53299ffc65b`.
- Confirmado `Agents Surface Visual Capture` run `33568994835`: `completed/success` no mesmo landing.
- Confirmado artifact `9824255700` (`agents-surface-before-after`), digest `sha256:d4025268642e8b6d080a8b66c997938181ec8dbfff8652ed58cdfccbdd7397f1`.
- Artifact baixado e inspecionado: before desktop é 404; after desktop contém a superfície MCP completa. O mesmo artifact inclui mobile before/after.
- Confirmado CI do landing #979, run `33568994932`, `success`.
- ConsumerCard `knowledge/consumers/causaganha.md` atualizado: `gap_score: 0` e capacidades de deploy/captura/recaptura revalidadas em 2026-09-01.
- Nenhuma mudança compartilhada no Cobogó e nenhum novo PR substantivo: esta rodada fecha evidência de uma mudança já mergeada.
- Nenhuma issue nova no workspace: o MCP read-only já constava do `ProjectProfile`; não surgiu nova capacidade ou sinergia.

## Fatos observados

- A PR #979 foi mergeada no landing `333308303bcabf681a6513960fa6d53299ffc65b` na rodada anterior, depois dos checks do head da PR ficarem verdes.
- No landing exato, `Deploy Web` `33568994734` concluiu `success` em 2026-09-01T23:02:06Z.
- No mesmo landing, `Agents Surface Visual Capture` `33568994835` concluiu `success` e gerou o artifact `9824255700` em 2026-09-01T23:01:54Z.
- O workflow dedicado executa em `push` para `main` e usa `github.event.before` como base; portanto o artifact pós-merge compara o estado imediatamente anterior do `main` com o landing da #979.
- A imagem before mostra 404; a after mostra a nova rota, sua hierarquia e a configuração local MCP.
- `franklinbaldo/cobogo#407` permanece aberta e não mergeável no head atual.
- O deploy `franklinbaldo/franklinbaldo.github.io` run `33535970297`, que estava pendente no RoundReport `2026-09-01T1257.md`, agora está `completed/success` na tentativa 2.

## Inferências

- O before/after pós-merge é causalmente adequado ao critério porque usa o mesmo harness, a mesma rota e estados adjacentes de `main`; o deploy verde no mesmo landing cobre separadamente a publicação Pages.
- A superfície não faz claim de endpoint remoto inexistente: o estado remoto permanece explicitamente indisponível, coerente com a evidência do repo.
- Não há evidência de um segundo consumer convergindo para este mesmo pattern; portanto a solução de `/agentes` não deve ser promovida ao Cobogó compartilhado.

## Resultado do gap

**Fechado.** O critério pré-registrado foi satisfeito sem alteração: #979 está em `main`; o `Deploy Web` do landing ficou verde; a recaptura pós-merge da mesma rota e método ficou verde; e a captura after mostra Arquivo / Estado / Teor, `uv run causaganha-mcp` e a indisponibilidade do remoto. O `gap_score` do consumer volta de 1 para 0.

## Gates

- PR #979: merge já realizado na rodada anterior somente após o head `b270a88ee5bda10e259832d3e7b1f18a7aa221f3` ficar sem checks pendentes/vermelhos observados; landing `333308303bcabf681a6513960fa6d53299ffc65b`.
- Landing `333308303...`: CI `33568994932=success`; Deploy Web `33568994734=success`; Agents Surface Visual Capture `33568994835=success`.
- Cobogó #407: head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`, aberta, `mergeable: false`; nenhum merge.

## Devolução ao Cobogó

- ConsumerCard de CausaGanha atualizado com `last_reviewed: 2026-09-01`, `gap_score: 0` e proveniência operacional dos runs/artifact pós-merge.
- Conceitos: nenhum novo. A solução local de entrada MCP tem evidência em apenas um consumer e permanece abaixo do gate de promoção.
- Pattern promovido: nenhum.
- Workspace: nenhuma issue nova; nenhum achado novo de capacidade/sinergia.

## Bloqueios

- `franklinbaldo/cobogo#407` — head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; estado: aberta e `mergeable: false`; condição de retomada: conflito/estado de merge resolvido mecanicamente e todos os required checks verdes no head atual.
- CausaGanha: nenhum bloqueio restante para o gap escolhido.

## Pressão para a próxima rotação

`franklinbaldo/franklinbaldo.github.io` — o deploy `33535970297` que mantinha a rodada `2026-09-01T1257.md` parcial agora concluiu `success`; a próxima rotação deve fechar causalmente esse estado, verificar a superfície/a evidência já capturada e ajustar o card/issue sem abrir novo PR substantivo se nada mais faltar.
