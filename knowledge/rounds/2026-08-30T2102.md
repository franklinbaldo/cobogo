---
type: RoundReport
routine: cobogo
date: 2026-08-30T21:02:59-04:00
focus: franklinbaldo/causaganha
candidates: [franklinbaldo/causaganha]
outcome: closed
prs: []
merged: []
captures: [github-actions:33338217168/artifact:9739713589]
blocked: [franklinbaldo/cobogo#407, ovigialocal/ovigialocal.github.io#8]
---

# Rodada Cobogó — 2026-08-30 21:02 -04

## Foco e alternativas

A regra de urgência decidiu novamente a rodada: CausaGanha começou com o `main@d8c5102e6de42f8391cdedbf3fa83306b41c8142` ainda bloqueado pela falha do `Deploy Web` registrada em `franklinbaldo/causaganha#941`. Nenhum segundo consumer foi aprofundado. A única intervenção admissível foi uma nova tentativa do mesmo workflow falho, sem alteração de código, para separar falha transitória de defeito persistente.

## Leitor de fora — antes

### D1 — o que a pessoa pode fazer diretamente no site

A home multi-fonte introduzida por #901 já estava comprovadamente publicada. O leitor pode consultar por CNJ, pesquisar publicações e explorar cobertura com navegação organizada pelos jobs reais.

**Gap D1:** nenhum gap material observado.

### D2 — o que o projeto faz por trás e por que existe

O repositório já continha a nova `/sobre` de #902 no `main`, ensinando Arquivo / Estado / Teor, limites de prova por fonte, fluxo fonte → artefato → índice → superfície e caminhos de reuso. A composição também já tinha captura pós-merge verde no mesmo SHA.

**Gap D2 [fato]:** no início da rodada o leitor público ainda não estava comprovado recebendo a nova `/sobre`, porque a primeira tentativa do `Deploy Web` run `33338217183` havia falhado em `Setup Python` e pulado o deploy.

### D3 — o que a pessoa pode fazer sozinha com os artefatos publicados

Continuavam comprovados ZIPs no Internet Archive, `sync-manifest.parquet`, `catalog.sql`, Parquets por fonte conforme disponibilidade e receita DuckDB reproduzível.

**Gap D3:** nenhum gap material observado.

## Gap escolhido e critério

**Gap escolhido: D2 — publicação efetiva da `/sobre` já implementada e capturada.**

**Critério congelado:** um SHA de `main` contendo `d8c5102e...` deve concluir `Setup Python`, geração de dados, build Astro, upload do artifact e deploy para GitHub Pages com sucesso, sem bypass ou enfraquecimento do workflow. A evidência visual deve continuar ligada ao mesmo SHA. A verificação pública pode usar a cadeia canônica do Pages — artifact contendo `sobre.html` + deployment success para o environment URL — sem fabricar commit apenas para forçar nova captura quando os bytes não mudaram.

## Capturas

### Antes

- Evidência herdada e ainda válida: `Product Surface Visual Capture` run `33338217168`, artifact `9739713589`, no merge SHA `d8c5102e...`.
- Método: Chromium com fixtures idênticas, `/sobre` desktop/mobile, além de home e `/stats` de controle.
- A captura já era pós-merge; o bloqueio era exclusivamente a implantação pública.

### Depois

- Não houve mudança de bytes de superfície nesta rodada; portanto a mesma captura do merge SHA continua sendo a evidência visual do conteúdo implantado.
- O workflow visual não oferece `workflow_dispatch`; criar commit artificial apenas para produzir screenshot cronologicamente posterior ao retry violaria a regra de não fabricar atividade.
- A cadeia de publicação nova é `Deploy Web` run `33338217183`, tentativa 2: build verde, artifact Pages id `9742155841` contendo `sobre.html`, deploy verde no mesmo `d8c5102e...`, environment URL `https://franklinbaldo.github.io/causaganha/`.
- Não houve probe HTTP independente da rota; a verificação desta rodada é pela cadeia build/artifact/deployment do GitHub Pages.

## Ações

- skills `brazilian-web-design`, `cobogo-design-review` e `cobogo-consumer-synergy` relidas;
- RoundReports recentes e consumer card do CausaGanha relidos;
- `ProjectProfile` do workspace relido sem edição;
- `main` e `causaganha#941` verificados antes da ação;
- executado `rerun failed jobs` do próprio `Deploy Web` run `33338217183`, sem alteração de código ou workflow;
- na tentativa 2, `Setup Python` passou; geração dos contratos, build Astro e upload do artifact concluíram verdes;
- job `deploy` concluiu verde e o Pages reportou sucesso para o SHA `d8c5102e...`;
- issue `franklinbaldo/causaganha#941` encerrada como `completed`;
- consumer card atualizado para `gap_score: 0`;
- nenhum PR de superfície criado ou mergeado nesta rodada.

## Fatos observados

- a primeira falha de `Setup Python` não se reproduziu na tentativa 2 do mesmo workflow e do mesmo SHA;
- o build gerou 118 rotas estáticas, incluindo `/sobre.html`;
- o artifact Pages `9742155841` foi criado a partir do mesmo SHA e inclui `sobre.html`;
- `actions/deploy-pages` criou o deployment de `d8c5102e...`, reportou sucesso e definiu `https://franklinbaldo.github.io/causaganha/` como environment URL;
- a captura pós-merge de #902 continua no mesmo SHA e não há mudança de superfície entre aquela captura e o deploy bem-sucedido;
- warnings opcionais de geração de dados ocorreram no build, mas não impediram os contratos required, o Astro build ou o deploy; nenhuma afirmação de superfície nova foi baseada neles.

## Inferências

- a causa de #941 era transitória no ambiente/ação de setup, não um defeito determinístico comprovado no workflow atual;
- não há justificativa para alterar o pipeline após uma repetição íntegra no mesmo SHA passar de ponta a ponta;
- com D1, D2 e D3 sem gap material comprovado hoje, CausaGanha não deve permanecer artificialmente preso à frente da rotação.

## Resultado do gap

**fechado.** O mesmo SHA que contém #902 passou build e deploy no GitHub Pages sem bypass. A rota `/sobre.html` está presente no artifact implantado, e a evidência visual pós-merge do mesmo SHA permanece válida. A verificação não incluiu probe HTTP independente e isso é registrado explicitamente, sem alterar o critério factual de implantação.

## Gates

- `franklinbaldo/causaganha#941` — encerrada como completed; `Deploy Web` run `33338217183`, tentativa 2, `build=success`, `deploy=success`, SHA `d8c5102e6de42f8391cdedbf3fa83306b41c8142`.
- captura do merge SHA — `33338217168/9739713589`: success e ainda aplicável porque nenhum byte de superfície mudou.
- `franklinbaldo/cobogo#407` — bloqueio previamente registrado; nenhum merge nesta rodada.
- `ovigialocal/ovigialocal.github.io#8` — bloqueio previamente registrado; nenhum merge nesta rodada.

## Devolução ao Cobogó

- consumer card do CausaGanha atualizado para `gap_score: 0` e deploy de #902 reconciliado como publicado pela cadeia canônica do Pages;
- conceito `source-proof-boundaries` permanece como conceito de um consumer; não há segunda evidência para promoção;
- nenhum novo conceito ou pattern promovido;
- nenhum achado novo de capacidade/sinergia exigiu issue no workspace.

## Bloqueios

- `franklinbaldo/cobogo#407` — condição de retomada conforme RoundReports anteriores.
- `ovigialocal/ovigialocal.github.io#8` — condição de retomada conforme RoundReports anteriores.

## Pressão para a próxima rotação

`franklinbaldo/causaganha` — a revisão visual ampla de #898 volta a ser tecnicamente elegível, mas CausaGanha agora tem `gap_score: 0`; a próxima rodada deve respeitar a regra de maior-gap entre todos os consumers ativos antes de voltar a ele.
