---
type: RoundReport
routine: cobogo
date: 2026-08-31T03:01:00-04:00
focus: franklinbaldo/astronauta
candidates: [franklinbaldo/astronauta, franklinbaldo/adibaldo.github.io, franklinbaldo/cobogo]
outcome: inspection-only
prs: []
merged: []
captures: []
blocked: [franklinbaldo/cobogo#407]
---

## Foco e alternativas

As três skills `brazilian-web-design`, `cobogo-design-review` e `cobogo-consumer-synergy` foram relidas. A pressão da rodada anterior apontava `franklinbaldo/astronauta`, cujo card ainda estava no schema legado e tinha `last_verified: 2026-08-14`.

O follow-up causal `franklinbaldo/cobogo#407` continua aberto no head `32b3897335df837e0b49035ecce77d4d59fa3738`, não mergeável contra `main`; a rodada anterior já provou dívida ampla da suíte Vitest, portanto não houve base para merge ou para enfraquecer o gate. Não há PR própria aberta no Astronauta.

Não foi observada urgência de Pages/deploy no Astronauta: o produto atual é um SSR local servido em loopback, não um site Pages. O último CI de `main` observado, run `33231518179` no SHA `8b356e93a5012c0edd10edaee4c677016e77c7e9`, terminou verde. `franklinbaldo/adibaldo.github.io` foi mantido como pressão seguinte porque seu card permanece legado e registra uma superfície Pages real; Cobogó foi descartado como foco por revisão recente e `gap_score: 0`.

## Leitor de fora — antes

### D1 — o que pode fazer diretamente

O README e a superfície implementada deixam o usuário abrir um bundle OKF local, navegar dashboard/tipos/conceitos, pesquisar e paginar conceitos, ler detalhes e relações, inspecionar grafo/diagnósticos e, com `--write`, executar edição de body, Apply e import/create em fluxos preview-first.

Gap D1: nenhum material. As necessidades legadas de compact density, tabelas/filtros/forms/status e foco são requisitos de qualidade/composição, não capacidades atuais escondidas da superfície.

### D2 — o que o projeto faz por trás e por que existe

A entrada pública explica explicitamente Browser → Astro SSR/Actions → capability client → gateway → serviços de `okf-parser` → bundle e a regra de autoridade: Astronauta possui apresentação/interação; `okf-parser`, semântica e mutação. O README também explica o cache do read adapter e sua invalidação por árvore para manter liveness em bundles grandes.

Gap D2: nenhum material. A tese e a mecânica principal são perceptíveis sem exigir contexto externo.

### D3 — o que pode fazer sozinho com artefatos publicados

O repositório público traz a aplicação e receita reproduzível a partir de checkout (`uv sync --frozen`, `bun install --frozen-lockfile`, `bun run build`, `uv run astronauta /path/to/bundle`). Não há GitHub Release publicada; o README não inventa uma distribuição pronta e deixa explícita a fronteira de packaging.

Gap D3: nenhum material. Ausência de release pronta é estado de distribuição/capacidade, não diferença entre uma capacidade comprovada e uma superfície que a oculte.

## Gap escolhido e critério

Nenhum gap fechável foi escolhido. Critério da rodada sem intervenção: se D1, D2 e D3 já estiverem perceptíveis a partir do README/superfície atual, não converter requisitos legados de densidade/administração em mudança visual artificial. Resultado esperado: migrar o card para `ConsumerCard`, recalcular `gap_score: 0` e registrar fora do Cobogó qualquer drift factual do `ProjectProfile`.

## Capturas

Nenhuma captura nova. Não houve alteração visualmente material e não foi localizada capacidade canônica de screenshot no Astronauta. A superfície de produto é local/loopback; portanto não existe rota pública estável a arquivar nesta rodada.

## Ações

- Skills operacionais relidas.
- `knowledge/consumers/astronauta.md` e `workspace/knowledge/projects/astronauta.md` lidos.
- README, `pyproject.toml`, `src/astronauta/mutations.py`, workflows e páginas atuais confrontados com o card legado.
- Discovery leve confirmou ausência de PR própria aberta no Astronauta, CI verde no último `main` observado e ausência de GitHub Releases.
- `knowledge/consumers/astronauta.md` migrado para `ConsumerCard`, `last_reviewed: 2026-08-31`, `gap_score: 0`.
- Aberta `franklinbaldo/workspace#23` para corrigir o drift factual do ProjectProfile: o repo já consome `okf-parser[graphql]==0.45.1`, enquanto o profile ainda diz 0.43.0/aguardo de release; o transporte `preview_token -> expected_preview_token` em import continua realmente pendente.
- Nenhum pattern compartilhado promovido.

## Fatos observados

- Astronauta continua ativo e é um admin/editor local de bundles OKF com `okf-parser` como autoridade semântica e de filesystem.
- O `main` atual fixa `okf-parser[graphql]==0.45.1`.
- O caminho `import_write` ainda chama `import_bundle(..., write=True)` sem `expected_preview_token`.
- O README documenta as capacidades atuais, a fronteira de autoridade, o runtime `astronauta PATH [--write]`, busca/paginação e a estratégia de cache/liveness.
- O repo não possui GitHub Releases publicadas.
- O último CI de `main` observado terminou com sucesso no SHA `8b356e93a5012c0edd10edaee4c677016e77c7e9`.

## Inferências

- O card legado confundia requisitos de design administrativo com gaps do leitor. A superfície atual já torna D1/D2 perceptíveis e oferece um caminho reprodutível de D3 via source checkout.
- O ProjectProfile está parcialmente defasado: disponibilidade da release deixou de ser blocker, mas o gap de review-binding do import permanece verdadeiro. Por autoridade, a correção pertence ao workspace e foi encaminhada em issue, não editada pelo Cobogó.
- A ausência de Pages não é falha de superfície neste consumer: o produto é deliberadamente local. Transformá-lo em site público para satisfazer a rotina mudaria arquitetura/produto sem evidência.

## Resultado do gap

`inspection-only`. O critério foi satisfeito: nenhuma intervenção visual foi fabricada. O card agora representa D1/D2/D3 no schema atual com `gap_score: 0`, e o drift de capacidade foi encaminhado ao dono correto.

## Gates

- Nenhuma PR substantiva criada ou mergeada nesta rodada.
- `franklinbaldo/cobogo#407`: aberta, head `32b3897335df837e0b49035ecce77d4d59fa3738`, `mergeable: false`; bloqueio conhecido, sem merge.
- Astronauta `main` observado `8b356e93a5012c0edd10edaee4c677016e77c7e9`; CI run `33231518179` verde.

## Devolução ao Cobogó

- Card Astronauta atualizado para `ConsumerCard`, `last_reviewed: 2026-08-31`, `gap_score: 0`.
- Nenhum conceito novo registrado: o specimen `astronauta-concept-review` já cobre a evidência útil de densidade administrativa/parentesco sem uniformidade.
- Nenhum pattern promovido; não surgiu nova dupla de evidência visual before/after que satisfaça o gate.

## Bloqueios

- `franklinbaldo/cobogo#407` — SHA `32b3897335df837e0b49035ecce77d4d59fa3738`; causa: branch não mergeável e dívida ampla da suíte Vitest já evidenciada. Estado: aberta. Condição de retomada: rebase/reconciliação deliberada da suíte com todos os required checks verdes no novo head.

## Pressão para a próxima rotação

`franklinbaldo/adibaldo.github.io` — card legado (`last_verified: 2026-08-16`) em consumer ativo de GitHub Pages; reavaliar se os `unmet_needs` de foundations/editorial rhythm ainda são gaps D1/D2/D3 ou apenas requisitos compartilháveis já atendidos pela superfície simples.
