---
type: RoundReport
routine: cobogo
date: 2026-08-30T19:57:22-04:00
focus: franklinbaldo/causaganha
candidates: [franklinbaldo/causaganha]
outcome: inspection-only
prs: []
merged: []
captures: []
blocked: [franklinbaldo/causaganha#941, franklinbaldo/cobogo#407, ovigialocal/ovigialocal.github.io#8]
---

# Rodada Cobogó — 2026-08-30 19:57 -04

## Foco e alternativas

A regra de urgência continua decidindo a rodada antes de follow-up causal, maior gap ou pressão: CausaGanha é consumer ativo e o deploy público do SHA atual de `main` permanece quebrado. `main` continua em `d8c5102e6de42f8391cdedbf3fa83306b41c8142`, contendo #902, e `franklinbaldo/causaganha#941` permanece aberta. Não há commit descendente nem novo `Deploy Web` verde que satisfaça o critério de retomada.

Há PRs próprias de produto abertas (#911, #912, #913), mas nenhuma pode ultrapassar a urgência de deploy. Abrir ou mergear nova superfície enquanto o SHA atual não é publicável aumentaria a fila causal sem benefício para o leitor externo. Nenhum segundo consumer foi aprofundado.

## Leitor de fora — antes

### D1 — o que a pessoa pode fazer diretamente no site

A última superfície publicamente comprovada continua sendo a versão implantada após #901: consulta por CNJ, busca DJEN, exploração de cobertura e navegação pelos jobs reais.

**Gap D1:** nenhum gap material novo observado nesta rodada.

### D2 — o que o projeto faz por trás e por que existe

O repositório continua provando no `main` atual a nova `/sobre` de #902: Arquivo / Estado / Teor, fronteiras explícitas de prova por fonte, fluxo fonte → artefato → índice → superfície e acesso reproduzível.

**Gap D2 [fato]:** o leitor público ainda não está comprovado recebendo essa explicação. O último `Deploy Web` do SHA atual continua falho; a captura local/Actions do próprio merge SHA não substitui publicação canônica.

### D3 — o que a pessoa pode fazer sozinha com os artefatos publicados

Continuam comprovados ZIPs no Internet Archive, `sync-manifest.parquet`, `catalog.sql`, Parquets por fonte conforme disponibilidade e receita DuckDB reproduzível.

**Gap D3:** nenhum gap material novo observado nesta rodada.

## Gap escolhido e critério

**Gap escolhido: D2 — publicação efetiva da `/sobre` já implementada e capturada.**

**Critério verificável:** um SHA de `main` contendo `d8c5102e...` deve concluir `Deploy Web` com build e deploy verdes; a rota pública `/sobre` deve então ser verificável no Pages. Não vale substituir esse critério pela captura de Actions já existente nem alterar a superfície para contornar o pipeline.

## Capturas

### Antes

Nenhuma captura nova. Evidência herdada e ainda válida: `Product Surface Visual Capture` run `33338217168`, artifact `9739713589`, no merge SHA `d8c5102e...`, com `/sobre` desktop/mobile capturada com sucesso.

### Depois

Nenhuma. O deploy público permanece bloqueado, portanto não há nova superfície publicada a recapturar.

## Ações

- skills `brazilian-web-design`, `cobogo-design-review` e `cobogo-consumer-synergy` relidas;
- RoundReports `2026-08-30T1902`, `2026-08-30T1759` e `2026-08-30T1558` relidos;
- lista canônica de consumer cards relida; `knowledge/consumers/causaganha.md` permanece `active`, `last_reviewed: 2026-08-30`, `gap_score: 1`;
- `ProjectProfile` do workspace relido sem edição; continua factual e ativo;
- `main` de CausaGanha verificado: continua em `d8c5102e6de42f8391cdedbf3fa83306b41c8142`;
- `causaganha#941` verificada aberta, sem comentários ou mudança de estado;
- histórico de Actions do SHA atual verificado: `Render Manifest Parquet` run `33341692346` concluiu com sucesso em 2026-08-30T23:28:02Z, enquanto o `Deploy Web` mais recente continua sendo `33338217183=failure`;
- PRs próprias abertas foram inspecionadas como sinal (#911/#912/#913 entre as mais recentes), mas nenhuma foi tocada porque a urgência de deploy tem precedência;
- nenhuma mudança de superfície, pattern ou pipeline foi implementada.

## Fatos observados

- `main` continua `d8c5102e...`;
- `causaganha#941` continua aberta;
- não existe novo `Deploy Web` verde para o SHA atual;
- um workflow agendado posterior, `Render Manifest Parquet` run `33341692346`, concluiu com sucesso no mesmo SHA;
- isso mostra que o repositório/Actions continua executando trabalho no SHA atual, mas não prova nem substitui o deploy do frontend;
- o consumer card continua correto e não mudou factual ou semanticamente;
- o `ProjectProfile` continua ativo e registra outras frentes operacionais, mas nenhuma autoriza contornar o bloqueio de superfície.

## Inferências

- o sucesso posterior de `Render Manifest Parquet` estreita a hipótese operacional: o bloqueio permanece específico ao caminho de `Deploy Web`/setup Python, não a uma incapacidade geral de executar GitHub Actions no SHA;
- #911/#912/#913 podem ser bons trabalhos de produto, mas não devem avançar por esta rotina enquanto a urgência de publicação permanece;
- não há base para alterar `gap_score`, promover pattern ou abrir nova issue no workspace;
- a ação correta continua sendo não fabricar atividade visual.

## Resultado do gap

**não fechado.** Nenhum fato novo satisfaz o critério de deploy público. A implementação e a evidência visual de #902 continuam válidas, mas o leitor externo ainda não está comprovado recebendo a nova `/sobre`.

## Gates

- `franklinbaldo/causaganha#941` — aberta; `main@d8c5102e...`; `Deploy Web` `33338217183=failure`; condição de retomada: build + deploy verdes em SHA contendo #902, seguido de verificação pública de `/sobre`.
- `Render Manifest Parquet` `33341692346=success` — sinal de saúde de workflow no mesmo SHA, mas não é gate substituto de deploy.
- `franklinbaldo/cobogo#407` — bloqueio previamente registrado; nenhum merge nesta rodada.
- `ovigialocal/ovigialocal.github.io#8` — bloqueio previamente registrado; nenhum merge nesta rodada.

## Devolução ao Cobogó

- consumer card do CausaGanha mantido sem edição porque `last_reviewed`, `gap_score: 1` e os fatos de superfície continuam corretos;
- conceito `source-proof-boundaries` permanece abaixo do gate de promoção: um consumer observado e implantação pública ainda bloqueada;
- nenhum novo conceito, pattern ou issue de capacidade/sinergia foi criado.

## Bloqueios

- `franklinbaldo/causaganha#941` — causa: `Deploy Web` falha no `Setup Python` antes do build; estado: aberto, sem retry/commit descendente de deploy bem-sucedido; condição de retomada: deploy verde e verificação de `/sobre` pública.
- `franklinbaldo/cobogo#407` — condição de retomada conforme RoundReports anteriores.
- `ovigialocal/ovigialocal.github.io#8` — condição de retomada conforme RoundReports anteriores.

## Pressão para a próxima rotação

`franklinbaldo/causaganha#941` — verificar primeiro se surgiu commit/retry com `Deploy Web` verde; somente depois retomar #911/#912/#913, #898 ou outro gap de superfície.
