---
type: RoundReport
routine: cobogo
date: 2026-09-01T23:11:00-04:00
focus: franklinbaldo/cobogo
candidates: [franklinbaldo/cobogo, franklinbaldo/the-lab, franklinbaldo/aleph-the-game]
outcome: closed
prs: [franklinbaldo/cobogo#415]
merged: [franklinbaldo/cobogo#415]
captures: []
blocked: [franklinbaldo/cobogo#407]
---

# Rodada Cobogó — 2026-09-01T2311

## Foco e alternativas

A rodada foi decidida por **follow-up causal próprio**: `franklinbaldo/cobogo#415` codificava a mudança de governança para um Cobogó opinionado e precisava ser reconciliada, validada e mergeada antes de qualquer novo baseline de consumer. `franklinbaldo/the-lab` não recebeu nova inspeção substantiva porque a rodada anterior determinou que só voltasse ao foco após mudança causal em #128 ou novo deploy. `franklinbaldo/aleph-the-game` permanece candidato natural para a próxima fase de baseline porque aparece como consumer ativo com `last_reviewed` anterior à maioria dos cards atuais, mas não foi aprofundado nesta rodada.

`franklinbaldo/cobogo#407` foi mantida como blocker independente: head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`, aberta e não mergeável na última revalidação. Não houve bypass nem segundo PR substantivo.

## Leitor de fora — antes

**D1 — o que pode fazer diretamente.** O README de `main` explicava instalação, API alvo e componentes, mas não oferecia ao leitor/contribuidor uma entrada para entender como o Cobogó julga ou melhora surfaces dos consumers. **Gap D1:** a capacidade de governança existia na branch, mas não era descobrível na entrada canônica do projeto.

**D2 — o que o projeto faz por trás e por que existe.** O texto de `main` ainda dizia que o Cobogó compartilhava conhecimento “aprendido em uso real” e subia relações reutilizáveis, linguagem compatível com o modelo conservador anterior. A branch #415 já propunha `opinionated → validated → stable`, mas essa decisão ainda não estava assentada em `main`. **Gap D2:** intenção nova e documentação canônica contavam histórias diferentes, permitindo que rodadas futuras retornassem ao gate antigo de dois consumers.

**D3 — o que pode fazer sozinho com os artefatos publicados.** Antes do merge não havia em `main` contrato canônico para produzir `SurfaceQualityAssessment`, ranking e `OpinionatedRecommendation` com maturidade verificável. **Gap D3:** o método de avaliação e o corpus normativo não estavam disponíveis como artefatos versionados da branch principal.

## Gap escolhido e critério

Gap escolhido: **D2 — eliminar a contradição entre a governança opinionada decidida e a documentação/corpus canônicos de `main`.**

**Critério verificável congelado:** o README de `main` deve declarar que evidência determina maturidade, não permissão para recomendar; dois consumers devem ser gate de `stable`, não de existência; a governança deve definir `SurfaceQualityAssessment` em seis dimensões e `OpinionatedRecommendation`; todo o novo knowledge deve passar `okf-parser` no head corrente; CI, Knowledge e Greenfield visual preview devem estar verdes no mesmo head; somente então a #415 pode ser mergeada, e o landing deve ser verificável em `main`.

## Capturas

Nenhuma captura before/after foi produzida porque a intervenção é de governança, README e corpus OKF, não uma mudança visual material em rota de consumer. `Greenfield visual preview` foi tratado apenas como gate de não-regressão, não como substituto de captura causal.

## Ações

- Relidas as skills `brazilian-web-design`, `cobogo-design-review` e `cobogo-consumer-synergy`; esta última confirma que um consumer pode justificar capacidade experimental e que evidência múltipla fortalece estabilização, sem exigir repetição prévia para experimentação.
- Revalidada #415 e confirmado que seus arquivos pertenciam ao pacote de governança, não a mudanças aleatórias acumuladas.
- Identificado que a #415 não alterava o README; acrescentada seção canônica `Como o Cobogó governa superfícies`, explicitando `opinionated`, `validated`, `stable`, `retired`, seis dimensões de avaliação e que `gap_score: 0` não significa “superfície resolvida”.
- No head `201d812a97c8d2523546a80e850b4f5a8b0c7266`, CI e preview passaram, mas Knowledge falhou. Foram adicionadas specs normativas para `OpinionatedRecommendation` e `SurfaceQualityAssessment`.
- A segunda falha de Knowledge foi diagnosticada pelos logs do job: oito `OKF001` porque arquivos auxiliares dentro de `knowledge/` não começavam com frontmatter YAML (`assessments/README.md`, `baseline-plan.md`, `issue-template-surface-quality.md`, `ranking.md`, `recommendations/README.md`, os dois docs em `schemas/` e `surface-quality-model.md`).
- Os oito arquivos foram integrados ao corpus como `type: knowledge-policy`, preservando o conteúdo e sem excluir ou enfraquecer o gate.
- Head final da #415: `7bbc8cf9e5cf6cd7d2c6d2eace1d623cf1a07d7f`.
- Gates nesse head: CI `33585789293` = success; Knowledge `33585789300` = success; Greenfield visual preview `33585789332` = success.
- #415 revalidada como mergeável no mesmo head e mergeada por squash com `expected_head_sha` fixado. Landing: `21b4c1b36f6d1a261e1942134f9aee53f3011b5c`.
- README em `main` reaberto após o merge e confirmado com a nova governança.
- Pós-merge em `main`: CI `33585913804` = success; Knowledge `33585913801` = success. A mudança não exige recaptura de consumer porque não altera superfície visual material.

## Fatos observados

- Antes da intervenção, o README de `main` não explicava o modelo opinionado nem a maturidade das recomendações.
- A skill de consumer synergy aceita um consumer como evidência suficiente para capacidade experimental e reserva evidência múltipla como argumento mais forte para core estável.
- O novo corpus possui `OpinionatedRecommendation`, `SurfaceQualityAssessment`, ranking, baseline, rotina operacional e recomendações normativas.
- O `okf-parser` rejeitou explicitamente oito documentos auxiliares sem frontmatter; após integrá-los como `knowledge-policy`, Knowledge passou no head final.
- CI, Knowledge e Greenfield ficaram verdes no mesmo head `7bbc8cf...` antes do merge.
- O README atual de `main` declara literalmente que dois consumers são gate para `stable`, não para pensar, documentar ou implementar uma opinião.
- O merge landing é `21b4c1b36f6d1a261e1942134f9aee53f3011b5c`, e os checks pós-merge observados de CI e Knowledge concluíram com sucesso.
- #407 permanece aberta e separada desta governança.

## Inferências

- A contradição entre prompt novo e documentação antiga era um mecanismo concreto para regressão operacional: uma sessão que reconstruísse contexto apenas do repo poderia reencontrar a regra conservadora e aplicá-la novamente.
- Colocar a decisão simultaneamente no README, RFC, rotina, schemas e corpus de recomendações reduz essa ambiguidade sem transformar chat em fonte de verdade.
- Tornar os documentos auxiliares knowledge OKF válido é mais coerente do que excluí-los do parser: a governança passa a ser validada pelo mesmo sistema que governa o restante do corpus.
- A próxima etapa não é produzir mais governança abstrata; é gerar baselines reais e deixar consumer evidence validar, ajustar ou retirar as recomendações opinionadas.

## Resultado do gap

**fechado.** O critério congelado foi satisfeito: README e corpus canônicos em `main` contam a mesma história opinionada; os tipos e contratos estão versionados; CI, Knowledge e Greenfield passaram no head exato antes do merge; #415 foi mergeada com proteção pelo SHA; README em `main` foi confirmado e os checks pós-merge observados de CI e Knowledge passaram.

## Gates

- `franklinbaldo/cobogo#415` — head `7bbc8cf9e5cf6cd7d2c6d2eace1d623cf1a07d7f`; CI `33585789293` = success; Knowledge `33585789300` = success; Greenfield visual preview `33585789332` = success; mergeable = true; squash merge `21b4c1b36f6d1a261e1942134f9aee53f3011b5c`. Estado final: **merged**.
- Pós-merge `21b4c1b...`: CI `33585913804` = success; Knowledge `33585913801` = success.
- `franklinbaldo/cobogo#407` — head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; segue aberta e não mergeável; nenhum bypass.

## Devolução ao Cobogó

- README agora expõe a governança opinionada como entrada canônica.
- RFC 0002, rotina operacional, Surface Quality Model, baseline, ranking e contrato de issue estão em `main`.
- `OpinionatedRecommendation` e `SurfaceQualityAssessment` possuem specs normativas OKF.
- O corpus inicial de recomendações está disponível com maturidade explícita; dois consumers são necessários para `stable`, não para existência.
- Nenhum assessment de consumer foi inventado nesta rodada; baseline continua pendente e deve partir de inspeção/captura real.
- Nenhum ConsumerCard externo foi alterado porque a rodada foi de formulação/follow-up no próprio Cobogó.

## Bloqueios

- `franklinbaldo/cobogo#407` — SHA `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; estado observado: aberta e não mergeável; condição de retomada: correção causal da suíte/gate, head mergeável e todos os required checks verdes no SHA corrente, sem enfraquecer Vitest.

## Pressão para a próxima rotação

`franklinbaldo/aleph-the-game` — consumer ativo com `last_reviewed: 2026-08-31`, enquanto a maioria dos cards observados já está em 2026-09-01; produzir o primeiro `SurfaceQualityAssessment` real pela nova régua, salvo surgimento de urgência ou follow-up causal mais prioritário.
