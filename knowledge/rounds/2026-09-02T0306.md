---
type: RoundReport
routine: cobogo
date: 2026-09-02T03:06:00-04:00
focus: franklinbaldo/ficha
candidates: [franklinbaldo/ficha, franklinbaldo/intuit, franklinbaldo/aleph-the-game]
outcome: closed
prs: [franklinbaldo/ficha#226]
merged: [franklinbaldo/ficha#226]
captures: [artifact:9833746642]
blocked: []
---

## Foco e alternativas

Follow-up causal decidiu o foco: `franklinbaldo/ficha#226` estava mergeável e os checks do head atual estavam verdes. Intuit já possuía baseline; Aleph permanecia bloqueado por sua capacidade de captura. Após o merge, a própria evidência estreita do Ficha revelou a maior dívida de qualidade da rodada.

## Leitor de fora — antes

D1 — a home comunica com clareza que o leitor pode entender uma empresa e suas conexões públicas por busca de CNPJ/Razão Social. Gap factual: nenhum.

D2 — origem Receita Federal, competência, preservação, verificação e estado tornam o mecanismo/proveniência perceptíveis. Gap factual: nenhum.

D3 — `Levar a base` e `manifest.json` tornam o dataset reutilizável fora da interface. Gap factual: nenhum.

Assessment relevante antes da ação corretiva: clareza 4, explicabilidade 4, autonomia 4; UX/UI ainda não tinham baseline estreito confiável.

## Gap escolhido e critério

Gap de evidência: a captura canônica precisava tornar viewport estreito observável. Critério pré-existente da #226: o run do PR deve produzir desktop `1280×900` e narrow `390×844`, com estado legível por máquina.

## Capturas

- BEFORE/HEAD — artifact `9833746642`, run `33596961590`, rota `/ficha/`, desktop `1280×900` e narrow `390×844`, método Chromium do workflow canônico. O estado capturado foi `remote-data-unavailable`, ortogonal ao layout.
- Não houve mudança visual de produto na #226; a PR alterou apenas `web/scripts/capture-home.mjs`. O deploy pós-merge do mesmo conteúdo de superfície concluiu `success` no run `33601434385`.
- Achado causal adicional: em evento `pull_request`, `GITHUB_SHA` registrou merge-ref sintético `f8efac2...`, não o head `306e1d4...`; issue `franklinbaldo/ficha#228` aberta para corrigir identidade causal da captura.

## Ações

- #226 mergeada por squash; landing `ca67d1c15fd60e02e574d6a429a5686b0bded7d4`.
- CI pós-merge `33601434433`: success.
- Deploy site `33601434385`: success.
- Artifact `9833746642` inspecionado em desktop e narrow.
- Issue `franklinbaldo/ficha#227`: overflow do botão `Buscar` em 390×844.
- Issue `franklinbaldo/ficha#228`: registrar head SHA real em captura de PR.
- Recomendação Cobogó `primary-action-viewport-containment` formulada como `opinionated`.

## Fatos observados

- Em desktop, a composição da home é compacta e a busca cabe integralmente.
- Em 390×844, o botão `Buscar` ultrapassa a borda direita e seu rótulo fica parcialmente cortado.
- A home expõe origem, competência, preservação, verificação e caminho para `manifest.json`.
- O artifact do PR usa merge-ref SHA no nome/JSON, não o head SHA da branch.

## Inferências

- `gap_score: 0` em D1–D3 não significa superfície resolvida: Ficha estreia com `UI: 1/4` apesar de D1/D2/D3 máximos.
- Responsividade do CTA primário merece uma recomendação opinionada própria; não é necessário esperar segundo consumer para formulá-la.

## Resultado do gap

Fechado para o objetivo da #226: viewport estreito passou a existir como evidência canônica e revelou uma dívida antes invisível. A dívida visual em si não foi corrigida nesta rodada porque o orçamento de um PR substantivo foi consumido pelo follow-up causal; #227 possui critério verificável para a próxima aplicação.

## Gates

- `franklinbaldo/ficha#226` — head `306e1d4d7d8c32ea9660f74eddb7f73d091eff8c`; CI, Visual capture e GitGuardian concluíram verdes; mergeável antes do squash.
- landing `ca67d1c15fd60e02e574d6a429a5686b0bded7d4`; CI `33601434433` e Deploy site `33601434385` concluíram `success`.

## Devolução ao Cobogó

- ConsumerCard do Ficha atualizado para `last_reviewed: 2026-09-02`, mantendo `gap_score: 0` e registrando dívida UI separadamente.
- `SurfaceQualityAssessment`: 18/24, faixa boa; D1 4, D2 4, D3 4, UX 2, UI 1, confiança 3.
- ranking ampliado para Intuit + Ficha.
- `primary-action-viewport-containment`: nova recomendação `opinionated`, ainda sem aplicação bem-sucedida.

## Bloqueios

nenhum para encerrar a rodada. #228 é dívida de infraestrutura de evidência com critério próprio, não impede reconhecer o artifact observado nem o deploy do landing.

## Pressão para a próxima rotação

`franklinbaldo/ficha` somente se #227 ou #228 tiver mudança causal; caso contrário, escolher outro consumer ativo sem baseline. A dívida prioritária conhecida é `UI: 1/4` no Ficha, mas repetição sem mudança causal deve ser evitada.
