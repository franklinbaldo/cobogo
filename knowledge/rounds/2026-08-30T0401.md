---
type: RoundReport
routine: cobogo
date: 2026-08-30T04:01:00-04:00
focus: franklinbaldo/ovigia-redacao
candidates: [franklinbaldo/ovigia-redacao, franklinbaldo/causaganha]
outcome: blocked
prs: []
merged: []
captures: []
blocked:
  - franklinbaldo/ovigia-redacao#29
  - franklinbaldo/cobogo#407
---

# Rodada Cobogó — 2026-08-30 04:01 -04

## Foco e alternativas

O Vigia vence pelo primeiro critério, **urgência**: a superfície pública continua em `ovigialocal/ovigialocal.github.io@67eb821dcc1ae0c81913b87d14cfe91259ae5d74`, ainda afirmando como factual/verificada uma matéria legada com fixture/proveniência sintética. A redação mudou causalmente desde a rodada anterior — `ovigia-redacao#42` agora registra Multivaccination `draft-v12` em `pass → factual-integrity` — mas nenhuma candidata corrente é `article-published`. CausaGanha permanece alternativa/pressão: o `Public Catalog Smoke` mais recente encontrado na janela de discovery é `run #17`, concluído como `skipped`, e `causaganha#872` continua aberto.

O follow-up causal próprio `franklinbaldo/cobogo#407` foi checado antes da escolha: permanece aberto, no head `0d08a0531ac5da43c53a0ff121997cad5ff0b8ee`, e não mergeável; `Greenfield visual preview` passa e `CI` falha no gate Vitest, portanto não existe verde no head atual que autorize merge.

## Leitor de fora — antes

### D1 — o que a pessoa pode fazer diretamente no site

O leitor pode abrir a matéria estática, ler título, corpo, bloco de proveniência e voltar à home. A rota `/porto-velho/2026/07/novo-registro-alimentos-nova-porto-velho/` continua servida pelo mesmo commit público legado e, conforme a evidência versionada revalidada nesta rodada, apresenta `Matéria Factual Verificada`, CNPJ `11.222.333/0001-81`, SHA-256 do conteúdo vazio e `Evaluation Passed (0 alucinações)` como se fossem prova editorial.

**Gap D1 [fato]:** a superfície pública continua epistemicamente mais forte que o estado que o repo consegue provar. O publisher atual já não gera esses elementos, mas nenhum `article-published` governado substituiu os bytes legados.

### D2 — o que o projeto faz por trás e por que existe

O repo prova uma redação hiperlocal static-first, verifiable-source-first, com producer self-review, avaliações independentes ligadas ao digest, freshness, `article-ready`, publication-editor e `article-published + evidência pública` como estados distintos. O estado corrente de `#42` é mais recente que o ProjectProfile do workspace: Multivaccination `draft-v12` voltou à fila de factual-integrity após nova apuração encontrar fonte municipal explícita para Porto Velho Shopping na campanha atual; Sine `draft-v8` continua substantivamente aprovado e bloqueado no binding governado do `source_digest` canônico.

**Gap D2 [fato]:** a matéria pública antiga não deixa perceber essa fronteira fail-closed nem a distinção entre preparação, aprovação, freshness e publicação; em vez disso, exibe estado interno como prova para o leitor.

### D3 — o que a pessoa pode fazer sozinha com os artefatos publicados

Para esta matéria, a evidência pública demonstrada continua sendo o HTML estático sob CC BY 4.0. O repo não prova, para esses bytes, dataset, API, bundle público reconstruível ou outro produto independente publicado que a superfície esteja escondendo.

**Gap D3:** nenhum gap material contabilizado nesta dimensão; não inferir reutilização a partir do corpus privado, drafts ou `article-ready` histórico.

## Gap escolhido e critério

**Gap escolhido: D1 — remover a falsa equivalência pública entre fixture/estado interno e matéria factual governada.**

**Critério pré-registrado, congelado antes de qualquer ação de implementação:** a mesma rota pública deixa de mostrar `Matéria Factual Verificada`, o CNPJ de fixture e `Evaluation Passed (0 alucinações)` como prova; os bytes publicados atravessam o boundary canônico e correspondem a evidência `article-published`; a captura posterior da mesma rota mostra a nova apresentação. Patch manual do repositório Pages não satisfaz o critério.

## Capturas

### Antes

- URL original: `https://ovigialocal.github.io/porto-velho/2026/07/novo-registro-alimentos-nova-porto-velho/`
- URL arquivada: nenhuma verificada nesta rodada.
- timestamp: 2026-08-30T04:01:00-04:00.
- rota: `/porto-velho/2026/07/novo-registro-alimentos-nova-porto-velho/`.
- commit/deploy observado: `ovigialocal/ovigialocal.github.io@67eb821dcc1ae0c81913b87d14cfe91259ae5d74`.
- método: busca por `screenshot`, `Playwright` e `capture` no consumer não encontrou capacidade registrada. O HTML versionado foi revalidado como evidência factual de conteúdo; não foi tratado como pixel proof.

### Depois

Nenhuma. Não houve superfície governada elegível para deploy e a capacidade visual continua indisponível; o loop não pode ser declarado fechado.

## Ações

- As três skills canônicas em `franklinbaldo/skills` foram relidas no início da rodada.
- Os três `RoundReport` mais recentes foram relidos antes do follow-up/discovery.
- `franklinbaldo/cobogo#407` revalidada no head `0d08a0531ac5da43c53a0ff121997cad5ff0b8ee`: `Greenfield visual preview` success; `CI` failure; aberta e não mergeável.
- `ovigialocal/ovigialocal.github.io` revalidado em `67eb821dcc1ae0c81913b87d14cfe91259ae5d74`; o HTML da rota ainda contém os sinais problemáticos.
- `franklinbaldo/ovigia-redacao#42` revalidada: Multivaccination `draft-v12` está `pass → factual-integrity`; Sine `draft-v8` permanece substantivamente aprovado e bloqueado no `source_digest` canônico; nenhuma candidata é `article-published`.
- `franklinbaldo/ovigia-redacao#29` recebeu comentário com o estado causal atualizado e o critério de conclusão preservado.
- `franklinbaldo/workspace#20` recebeu a atualização factual do `ProjectProfile` stale. Uma issue #21 foi aberta por engano para o mesmo drift e imediatamente fechada como `duplicate`, sem criar autoridade paralela.
- CausaGanha foi mantido em discovery leve: `causaganha#872` continua aberto; na janela recente, `Public Catalog Smoke` run #17 está `skipped`, não é evidência verde pós-correção.
- Consumer card de O Vigia atualizado no Cobogó com o estado `draft-v12` e capacidades revalidadas.

## Fatos observados

- A rota pública ainda contém `Matéria Factual Verificada`, CNPJ `11.222.333/0001-81`, SHA-256 `e3b0c442...b855` e `Evaluation Passed (0 alucinações)`; `datePublished` continua em formato humano no JSON-LD.
- O repo público permanece em `67eb821dcc1ae0c81913b87d14cfe91259ae5d74`.
- `ovigia-redacao#42` agora registra Multivaccination `draft-v12` em fila de factual-integrity; nova apuração corrigiu a conclusão anterior e encontrou suporte municipal explícito para Porto Velho Shopping na campanha corrente até 01/09, sem inventar horário para domingo 30/08.
- Sine `draft-v8` mantém pass substantivo de factual-integrity e continua bloqueada no binding governado do `source_digest` canônico.
- Nenhuma candidata corrente é `article-published`.
- O `ProjectProfile` do workspace permanece no snapshot de 24/08 com Dia D v9 / Sine v7 como readies correntes; `workspace#20` continua sendo a autoridade para corrigir o drift.
- README/CLAUDE/CURRENT_PLAN confirmam static-first, fonte verificável, `okf-parser` como autoridade estrutural e publicação separada de `article-ready`; `CURRENT_PLAN.md` também carrega estado operacional antigo frente à #42 viva.
- Nenhuma capacidade de captura visual foi encontrada no consumer.

## Inferências

- A urgência continua real, mas não existe nesta rodada uma mudança de superfície admissível dentro da autoridade desta rotina: remediar manualmente o Pages produziria aparência de correção sem provar `article-published`.
- O avanço para `draft-v12` é evidência de que a maquinaria fail-closed continua corrigindo a própria apuração; isso reforça, e não reduz, a necessidade de a superfície pública parar de apresentar um gate interno antigo como certeza factual.
- A ausência de captura é um bloqueio independente: mesmo uma publicação governada futura não fecha o loop visual sem recaptura da rota.
- CausaGanha ainda não apresenta nova evidência verde no smoke público que justificasse deslocar a urgência ou fechar #872.

## Resultado do gap

**Não fechado / bloqueado.** O critério pré-registrado permaneceu intacto: a rota continua com os elementos problemáticos, nenhum `article-published` substituiu os bytes e não há captura posterior.

## Gates

- `franklinbaldo/cobogo#407` — head `0d08a0531ac5da43c53a0ff121997cad5ff0b8ee`; `Greenfield visual preview`: success; `CI`: failure; estado final: aberta, não mergeada.
- O Vigia — nenhuma PR de superfície criada. Não existe `article-published` elegível para remediação governada e a captura obrigatória permanece indisponível.
- Cobogó knowledge — pré-registro e fechamento versionados diretamente em `main`; consumer card atualizado na mesma rodada.

## Devolução ao Cobogó

- Consumer card de O Vigia atualizado com `last_reviewed: 2026-08-30`, `gap_score: 2`, Multivaccination `draft-v12` e capacidades de superfície revalidadas.
- Nenhum conceito novo: ainda não existe solução de superfície fechada com captura antes/depois.
- Nenhum pattern promovido: o gate de ≥2 consumers com solução convergente e capturas antes/depois não foi satisfeito.
- Drift do `ProjectProfile` devolvido ao workspace via #20; #21 foi fechada como duplicata imediatamente.

## Bloqueios

- **O Vigia / D1** — commit público `67eb821dcc1ae0c81913b87d14cfe91259ae5d74`; causa: artefato legado permanece público e nenhuma candidata corrente chegou a `article-published`. Condição de retomada: remediação/publicação governada correspondente a #29.
- **Captura O Vigia** — nenhuma capacidade de screenshot/capture registrada. Condição de retomada: capacidade de captura disponível para a mesma rota; sem isso não declarar fechamento visual.
- **Cobogó #407** — head `0d08a0531ac5da43c53a0ff121997cad5ff0b8ee`; causa: CI vermelho no gate Vitest. Condição de retomada: reconciliar a suíte sem enfraquecer o gate.

## Pressão para a próxima rotação

`franklinbaldo/causaganha` — verificar especificamente o próximo `Public Catalog Smoke` executado (não `skipped`) para `catalog.sql público → DuckDB local → comunicacoes`. Um run verde pós-correção mudaria D3 e pode fechar `causaganha#872`. O Vigia volta a ser exercitável assim que existir remediação/publicação governada ou capacidade de captura que permita fechar o loop.
