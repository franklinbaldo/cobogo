---
type: RoundReport
routine: cobogo
date: 2026-09-01T06:58:35-04:00
focus: franklinbaldo/astronauta
candidates: [franklinbaldo/astronauta, franklinbaldo/the-lab, franklinbaldo/cobogo]
outcome: partial
prs: [franklinbaldo/astronauta#38]
merged: [franklinbaldo/astronauta#38]
captures: []
blocked: [franklinbaldo/astronauta#39, franklinbaldo/cobogo#407, franklinbaldo/the-lab#128]
---

## Foco e alternativas

As três skills operacionais e os três RoundReports mais recentes foram relidos antes da seleção. `franklinbaldo/cobogo#407` permanece aberta no head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; o workflow CI desse mesmo SHA continua `failure`, portanto não vence por follow-up causal e não pode ser mergeada. `franklinbaldo/the-lab#128` permanece com o mesmo bloqueio causal já registrado, sem evidência nova que justifique repetir uma rodada bloqueada. Astronauta foi escolhido pela pressão explícita da rodada anterior e por ainda estar em `last_reviewed: 2026-08-31`.

## Leitor de fora — antes

**D1 — o que a pessoa pode fazer diretamente no site/superfície pública.** O README público apresenta Astronauta como admin/editor local vivo para bundles OKF e enumera dashboard, tipos, conceitos, relações, diagnósticos, grafo, body edit preview/commit e Apply preview/commit. O `main` atual, porém, já implementa também import/create preview-first e review-bound pela #37. **Gap:** a entrada pública do projeto não deixa o leitor perceber que import/create é uma terceira classe atual de mutação revisável, embora o repo e o ProjectProfile provem que ela está incorporada.

**D2 — o que o projeto faz por trás e por que existe.** A arquitetura Browser → Astro → capability client → gateway → `okf-parser` está explícita, assim como a fronteira de autoridade e `--write`. A #37 passou a transportar o `preview_token` opaco de import do preview até `expected_preview_token`, falhando fechado contra preview stale. **Gap:** o README explica review-binding para Apply, mas omite o mesmo contrato já incorporado para import/create, deixando a fronteira de segurança atual incompleta para um leitor de fora.

**D3 — o que a pessoa pode fazer sozinha com os artefatos publicados.** O README oferece receita reproduzível a partir do checkout e o runtime local; não há release GitHub publicada prometida como disponível. **Gap:** nenhum material independente além do mesmo gap editorial de documentação das capacidades de import/create já incorporadas.

## Gap escolhido e critério

Gap escolhido: **D2 — tornar explícito no README o contrato atual de import/create preview-first e review-bound sem ampliar autoridade do Astronauta.**

Critério verificável e imutável: **depois da mudança, a seção `Current admin/editor` do README deve mencionar import/create via serviço canônico do `okf-parser`, preview obrigatório e commit ligado ao `preview_token` opaco revisado; o texto deve preservar que somente o processo iniciado com `--write` pode conceder escrita e não deve inventar release/distribuição não provada.**

## Capturas

- Before visual: não aplicável nesta mudança estritamente editorial de README; nenhuma geometria, CSS, componente ou estado visual do admin foi alterado.
- After visual: não aplicável pelo mesmo motivo.
- Before textual: `README.md` em `main` no commit `aaa7c0ed1db711685a6aee31f078d18a6510c08b`, sem import/create na lista `Current admin/editor`.
- After textual: `README.md` em `main` no merge `81a978200aa08d91dce8a06f0050eda25741009e`, com import/create preview-first, `preview_token` opaco e autoridade process-owned de `--write` explícitos.
- Save Page Now: não aplicável à superfície local/README nesta rodada.

## Ações

- Pré-registro criado em `knowledge/rounds/2026-09-01T0658.md` antes de qualquer alteração no consumer.
- Lidos ConsumerCard, ProjectProfile, README e `main` de Astronauta; #37 confirmado como estado incorporado em `aaa7c0ed1db711685a6aee31f078d18a6510c08b`.
- Aberta `franklinbaldo/astronauta#38`, head `9f8aab4c2afa186334b78747a4387c160f3e5321`, modificando somente README (+5/-1).
- No head exato da #38: CI run `33500360936` = success; Live editor `33500361012` = success; Live apply `33500360964` = success; Live import `33500361081` = success.
- #38 ficou `mergeable: true` no mesmo head e foi squash-mergeada com `expected_head_sha`; merge `81a978200aa08d91dce8a06f0050eda25741009e`.
- README pós-merge foi reaberto em `main` e confirmou o critério do gap escolhido.
- Durante a verificação pós-merge foi identificado um segundo drift factual já presente antes desta rodada: `Development and packaging boundary` ainda chama o runtime empacotado de “next distribution slice”, embora ProjectProfile e job `installed-artifact` provem wheel com runtime standalone em ambiente consumidor fresco. Como o orçamento de uma PR substantiva já estava consumido, foi aberta `franklinbaldo/astronauta#39` com evidência e critério de conclusão, sem segunda PR.
- ConsumerCard atualizado para `last_reviewed: 2026-09-01`; D2 fechado e `gap_score` recalculado para 1 por causa do gap D3 de packaging público rastreado em #39.

## Fatos observados

- `astronauta/main` antes da intervenção estava em `aaa7c0ed1db711685a6aee31f078d18a6510c08b`, merge da #37.
- O ProjectProfile registra #37 como conclusão do import review-binding e registra `wheel com runtime Astro standalone` entre as capacidades operacionais.
- O README anterior documentava Apply review-bound, mas não import/create na lista `Current admin/editor`.
- Todos os quatro workflows publicados para a #38 ficaram verdes no head exato `9f8aab4c2afa186334b78747a4387c160f3e5321`.
- O merge da #38 é `81a978200aa08d91dce8a06f0050eda25741009e`; o README em `main` contém a correção.
- O job `installed-artifact` do CI da #38 construiu wheel com runtime web staged e avançou pelo contrato de instalação em ambiente consumidor fresco; o workflow CI concluiu `success`.
- A seção de packaging do README ainda fala desse estado como futuro; `astronauta#39` é a dívida corrente.
- `cobogo#407` segue no mesmo head com CI vermelho; The Lab #128 não apresentou mudança causal nesta passagem.

## Inferências

- O gap escolhido era de explicação progressiva da capacidade já incorporada, não de implementação funcional; a correção mínima de README foi suficiente.
- A omissão de import/create não justificava redesign do admin nem promoção de pattern Cobogó.
- O drift de packaging encontrado depois é material para D3 porque descreve como um terceiro consegue consumir o artefato; não deve ser escondido só porque o gap D2 foi fechado.
- Abrir uma segunda PR no mesmo consumer nesta rodada violaria o orçamento de uma PR substantiva; preservar #39 como pressão explícita é preferível a ampliar silenciosamente o escopo.

## Resultado do gap

**fechado para o gap pré-registrado.** O README pós-merge satisfaz exatamente o critério: import/create aparece como serviço canônico do `okf-parser`, exige preview, o commit carrega o `preview_token` opaco revisado e a autoridade continua pertencendo ao processo iniciado com `--write`.

**Resultado da rodada: partial.** Um novo gap D3 factual foi identificado na mesma superfície (`#39`) e permanece aberto por limite de orçamento; por isso o ConsumerCard termina em `gap_score: 1`.

## Gates

- `franklinbaldo/astronauta#38` — head `9f8aab4c2afa186334b78747a4387c160f3e5321`; CI `33500360936` = success; Live editor `33500361012` = success; Live apply `33500360964` = success; Live import `33500361081` = success; `mergeable: true`; merge squash protegido por SHA = `81a978200aa08d91dce8a06f0050eda25741009e`.
- Não há Pages/deploy separado para Astronauta; a superfície pública modificada é o README do próprio repositório, verificado diretamente em `main` após o merge.
- `franklinbaldo/cobogo#407` — head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; CI continua failure; nenhum merge.

## Devolução ao Cobogó

- `knowledge/consumers/astronauta.md` atualizado para `last_reviewed: 2026-09-01`, `gap_score: 1`.
- D2 registra a correção de #38; D3 registra explicitamente o drift de packaging e a issue #39.
- Nenhum conceito novo e nenhuma contagem de promotion gate foram criados: não houve solução visual before/after nem segundo consumer convergente.
- Nenhuma escrita foi feita no ProjectProfile; ele serviu apenas como autoridade factual.

## Bloqueios

- `franklinbaldo/astronauta#39` — causa: README ainda apresenta como futura uma capacidade de runtime instalado que repo/CI já provam; estado: issue aberta, sem segunda PR por orçamento; condição de retomada: corrigir a documentação pública via PR, obter checks verdes no head e confirmar README em `main`.
- `franklinbaldo/cobogo#407` — head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; CI vermelho; retomar somente com checks verdes no head corrente.
- `franklinbaldo/the-lab#128` — bloqueio causal inalterado; retomar somente com deploy ativo, verde e recaptura pública.

## Pressão para a próxima rotação

**franklinbaldo/astronauta#39**, por ser agora o único gap material registrado no card (`gap_score: 1`) e uma afirmação pública de distribuição que o repo já deixou para trás, salvo urgência superior ou follow-up causal verde.
