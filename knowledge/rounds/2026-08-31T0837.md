---
type: RoundReport
routine: cobogo
date: 2026-08-31T08:37:00-04:00
focus: franklinbaldo/intuit
candidates: [franklinbaldo/intuit, franklinbaldo/cobogo]
outcome: closed
prs: [franklinbaldo/intuit#15, franklinbaldo/intuit#24]
merged: [franklinbaldo/intuit#24]
captures: []
blocked: [franklinbaldo/cobogo#407]
---

## Foco e alternativas

Follow-up causal decidiu o foco: `franklinbaldo/intuit#15` era PR própria aberta e não mergeável depois da #17. O `ProjectProfile` já registrava que a branch misturava trabalho absorvido, solução obsoleta de sandbox e um residual de acessibilidade; portanto a única tentativa de correção da rodada foi reaplicar sobre o `main` atual somente o residual que não conflita com o runtime provado.

`franklinbaldo/cobogo#407` permaneceu alternativa bloqueada: head `32b3897335df837e0b49035ecce77d4d59fa3738`, não mergeável, com a dívida já medida de 65 falhas em 152 testes. Não houve bypass nem nova tentativa ampla de reconciliar a suíte.

## Leitor de fora — antes

### D1 — o que a pessoa pode fazer diretamente
A pessoa edita HTML, renderiza o resultado, compartilha/atualiza URL, limpa o editor, controla scripts e tema e inspeciona o preview. O preview já é nomeado semanticamente no `main` após #17.

Gap D1: erros dinâmicos ainda não são anunciados como alerta; ações do editor e configurações do preview são visualmente agrupadas, mas o agrupamento não é exposto semanticamente. O repo prova que essas relações existem e a superfície não as torna igualmente recuperáveis por tecnologia assistiva.

### D2 — o que o projeto faz por trás e por que existe
O projeto é um renderer HTML/visual-testing client-side cujo valor é a relação imediata fonte/edit → preview. O sandbox estrito é default e o runtime usa `srcdoc`, sem depender de acesso parent-side ao documento do iframe.

Gap D2: nenhum material. A finalidade e a fronteira de segurança estão coerentes com o `ProjectProfile` atual.

### D3 — o que a pessoa pode fazer sozinha com os artefatos publicados
O estado pode ser transportado por URL/Base64/Gist e o source checkout é reproduzível. Não há artefato autônomo adicional escondido pela superfície.

Gap D3: nenhum material estabelecido.

## Gap escolhido e critério

Gap escolhido: D1 — semântica de erro e agrupamentos operacionais.

Critério verificável congelado: depois da mudança, o erro dinâmico terá `role="alert"`/live semantics; as ações do editor e settings do preview terão grupos nomeados; testes Playwright direcionados provarão essas relações; o default continuará `sandbox=""` com render por `srcdoc`. Não alterar o critério após a implementação.

## Capturas

Nenhuma captura before/after foi exigida para a fatia escolhida porque a intervenção foi estritamente semântica: nenhum estilo, geometria, cor, motion ou pixel do estado normal foi alterado. A ausência de captura browser versionada continua registrada separadamente como dívida de evidência e impede usar esta rodada como before/after para promoção visual.

Rota afetada: `https://franklinbaldo.github.io/intuit/`.

## Ações

- Skills `brazilian-web-design`, `cobogo-design-review` e `cobogo-consumer-synergy` relidas; três RoundReports anteriores relidos.
- `intuit#15` revalidada: head `8c3753868de13cf2cb595ef6f5d795883fae2a85`, não mergeável.
- `ProjectProfile`, card, README, `index.html`, testes Playwright, runs de Test/Pages e releases confrontados; não há GitHub Release publicada.
- Criada branch `fix/14-residual-accessibility` diretamente do `main` `0c51520be81abc7086c6ddacef4335ada54284dc`.
- Aberta `intuit#24`, head `3789c9c5e7d97f2239ec3f12448eca63e7fb9630`, com somente alert semantics, grupos nomeados e assertions de proteção do strict sandbox/`srcdoc`.
- Workflow Test da PR #24 concluiu verde no head atual antes do merge.
- #24 foi squash-mergeada como `65f0decbb225d78a1277265023634133abf98322`.
- Pages run `33393191726` concluiu verde no merge SHA; Test de `main` run `33393191740` concluiu verde, incluindo unit tests + Playwright.
- #15 recebeu comentário de supersessão e foi fechada sem merge.
- `intuit#14` foi estreitada para o residual real: foco visível + `prefers-reduced-motion`, sem reabrir trabalho semântico/gate já fechado.
- `knowledge/consumers/intuit.md` migrado para `ConsumerCard`, `last_reviewed: 2026-08-31`, `gap_score: 1`.

## Fatos observados

- Antes da rodada, #15 estava não mergeável e continha uma solução antiga que permitiria `allow-same-origin` no default, contrariando a decisão posterior provada por #17.
- O `main` já continha preview nomeado, `sandbox=""`, render por `srcdoc`, Base64 corrigido, tema visualmente observável, gate Test e Pages verdes desde #17.
- O README atual documenta editor, parâmetros `?data=`/`?b64=`/Gist, sandbox, tema e execução local; não há release publicada.
- #24 alterou apenas atributos semânticos e testes: `role="alert"`, `aria-live="assertive"`, `Editor actions`, `Preview settings` e assertion de `sandbox=""` + `srcdoc`.
- O head `3789c9c5e7d97f2239ec3f12448eca63e7fb9630` ficou mergeável e passou o workflow Test completo.
- O merge `65f0decbb225d78a1277265023634133abf98322` recebeu Pages verde e Test verde em `main`.
- #14 permanece aberta apenas para foco visível e reduced-motion.

## Inferências

- A correção segura de uma PR conflitante não era rebasing cego: a branch antiga misturava mudanças absorvidas e uma decisão de sandbox invalidada por evidência posterior.
- Separar a fatia semântica da fatia visual/motion permitiu fechar um gap real sem exigir captura artificial nem reabrir riscos de runtime.
- A relação editor → preview continua útil como pressure-test de Cobogó, mas esta rodada não produz evidência visual before/after e portanto não aumenta o gate de promoção de pattern.

## Resultado do gap

**Fechado.** O critério pré-registrado foi satisfeito sem ser reescrito: `#errorMessage` agora tem alert/live semantics; ações e settings são grupos nomeados; Playwright testa essas relações; o default continua `sandbox=""` e o render continua por `srcdoc`. O residual de foco/reduced-motion é um gap D1 separado e permanece explicitamente em #14.

## Gates

- `intuit#24` — head `3789c9c5e7d97f2239ec3f12448eca63e7fb9630`; mergeable; workflow Test verde no head atual; squash-merge somente depois do verde.
- merge #24 — `65f0decbb225d78a1277265023634133abf98322`; Pages run `33393191726`: success; Test run `33393191740`: success.
- `intuit#15` — fechada sem merge após ser substituída por #24.
- `cobogo#407` — head `32b3897335df837e0b49035ecce77d4d59fa3738`, não mergeável; sem merge.

## Devolução ao Cobogó

- Card Intuit migrado para `ConsumerCard`, `last_reviewed: 2026-08-31`, `gap_score: 1` porque D1 ainda contém o residual material de foco/reduced-motion.
- Capacidades de Pages, Test/Playwright e estado transportável foram revalidadas com data e SHA; ausência de screenshot continua explícita.
- Nenhum conceito novo criado: editor → preview e parentesco sem uniformidade já estavam registrados; esta rodada apenas adiciona evidência semântica local, sem captura visual de promoção.
- Nenhum pattern promovido e nenhuma contagem de candidate foi incrementada sem before/after em dois consumers.
- Nenhum achado novo de capacidade/sinergia exigiu issue no workspace; o `ProjectProfile` já representava corretamente o residual #14/#15.

## Bloqueios

- `franklinbaldo/cobogo#407` — SHA `32b3897335df837e0b49035ecce77d4d59fa3738`; causa: branch não mergeável + 65/152 falhas Vitest já classificadas como dívida ampla. Condição de retomada: reconciliar deliberadamente a suíte em novo head e obter todos os required checks verdes.

## Pressão para a próxima rotação

Nenhuma. A próxima rotação deve reaplicar urgência/follow-up/maior `gap_score`; Intuit permanece com `gap_score: 1` pelo residual foco/reduced-motion, mas sem pressão artificial para repetir o mesmo consumer.
