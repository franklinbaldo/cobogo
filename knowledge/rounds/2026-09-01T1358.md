---
type: RoundReport
routine: cobogo
date: 2026-09-01T13:58:22-04:00
focus: franklinbaldo/franklinbaldo.github.io
candidates: [franklinbaldo/franklinbaldo.github.io]
outcome: closed
prs: [franklinbaldo/franklinbaldo.github.io#1640]
merged: [franklinbaldo/franklinbaldo.github.io#1640]
captures:
  - https://api.github.com/repos/franklinbaldo/franklinbaldo.github.io/actions/artifacts/9806697863
  - https://api.github.com/repos/franklinbaldo/franklinbaldo.github.io/actions/artifacts/9811726761
blocked: []
---

# Rodada — Franklin blog

## Foco e alternativas

Foco mantido em `franklinbaldo/franklinbaldo.github.io` por urgência/follow-up causal: a rodada anterior deixou #1640 mergeada, evidência visual after pronta e o deploy `33535970297` pendente. Nesta rodada a tentativa 1 do deploy terminou `failure`, a falha foi registrada em #1645 e um único rerun do job `deploy` foi executado no mesmo SHA. A tentativa 2 concluiu `success`, permitindo fechar o loop sem nova mudança de código.

Alternativas: nenhum segundo consumer aprofundado. Depois do fechamento causal, a rotação volta ao desempate normal na próxima rodada.

## Leitor de fora — antes

Pré-registro herdado e preservado das rodadas anteriores; não foi reescrito após a intervenção.

- **D1 — no site:** `/audiobooks/` existe e funciona diretamente, mas no início desta rodada a home pública ainda não mostrava `Audiobooks` no grupo `More` nem no Footer. **Gap:** o source e a captura pós-merge provavam a nova navegação, mas o Pages publicado ainda servia a versão anterior.
- **D2 — por trás:** o repo prova Audiobook Factory, catálogo derivado dos manifests e feed condicionado ao estado de cada obra. **Gap:** nenhum gap D2 separado; não anunciar podcast/feed desabilitado.
- **D3 — por conta própria:** source, RSS, arquivo/busca e estrutura reproduzível da fábrica permanecem disponíveis. **Gap:** nenhum gap D3 material independente do D1.

## Gap escolhido e critério

Gap escolhido: D1, descoberta global de `/audiobooks/`.

Critério verificável preservado: Header e Footer expõem `Audiobooks` / `Audiolivros` em hierarquia secundária; checks do head exato verdes; merge no mesmo head; Pages verde no merge SHA; captura before/after da home pelo mesmo método mostra a nova entrada.

## Capturas

### Antes

- URL original: `https://franklinbaldo.github.io/`
- URL arquivada: nenhum Save Page Now verificado.
- artifact: `https://api.github.com/repos/franklinbaldo/franklinbaldo.github.io/actions/artifacts/9806697863`
- rota: `/`
- commit/head: `6503538c187bd6cb860252dd1865ef7bdb2199f5`
- método: workflow `Visual evidence` / `scripts/screenshots.mjs`, desktop 1440x900.
- observação: sem Audiobooks na navegação global.

### Depois

- URL original: `https://franklinbaldo.github.io/`
- artifact: `https://api.github.com/repos/franklinbaldo/franklinbaldo.github.io/actions/artifacts/9811726761`
- rota: `/`
- commit/merge: `eebbe97af4e3aaf71b8a45b5752056dcf158122d`
- método: mesmo workflow/harness do before.
- observação: artifact pós-merge mostra `Audiobooks` no Footer; após o rerun verde do Pages, a home pública também foi verificada com `Audiobooks` no grupo secundário `More` e no Footer.

## Ações

- PR já mergeada: `franklinbaldo/franklinbaldo.github.io#1640`.
- merge SHA: `eebbe97af4e3aaf71b8a45b5752056dcf158122d`.
- deploy `33535970297`: tentativa 1 `failure`; build `success`, deploy falhou em `actions/deploy-pages`.
- issue operacional `#1645` aberta com evidência, escopo e critério de conclusão.
- rerun isolado do job `deploy` no mesmo run/SHA: tentativa 2 `success`.
- home pública reaberta após o rerun e confirmada com Audiobooks no Header/More e Footer.
- issue de superfície `#1637` fechada como concluída.
- issue operacional `#1645` fechada como concluída.
- consumer card atualizado para `gap_score: 0`.

## Fatos observados

- O build e o upload do artifact de Pages concluíram `success` no merge SHA.
- A primeira execução de `actions/deploy-pages` falhou após aproximadamente 10 minutos.
- Antes do rerun, a home pública ainda não continha Audiobooks na navegação global.
- O rerun do job de deploy reutilizou o mesmo run e merge SHA e concluiu `success` na tentativa 2.
- Depois do rerun, a home pública passou a mostrar `Audiobooks` no grupo `More` e no Footer.
- A evidência before/after continua ligada aos SHAs observados pelo mesmo harness visual.

## Inferências

- A falha inicial era operacional no passo de publicação, não uma regressão de build ou da implementação visual.
- O rerun no mesmo SHA foi suficiente para restaurar a publicação sem alterar source nem diluir o critério.
- O leitor externo agora recebe o caminho global para `/audiobooks/` que o repo já sustentava, sem transformar a fábrica em CTA principal.

## Resultado do gap

**Fechado.** O critério pré-registrado foi satisfeito integralmente: implementação e captura before/after, checks verdes no head exato, merge protegido, Pages `success` no merge SHA após rerun e verificação da home pública com a nova entrada. O critério não foi reescrito.

## Gates

- PR `#1640`: mergeada anteriormente somente após checks verdes no head exato `a03c3670ca95100ae9bc638e7633a0efa13f7088`.
- merge `eebbe97af4e3aaf71b8a45b5752056dcf158122d`:
  - Visual evidence pós-merge: `success`.
  - Deploy to GitHub Pages `33535970297`, tentativa 1: `failure`.
  - build: `success`.
  - deploy/actions-deploy-pages tentativa 1: `failure`.
  - tentativa 2 do job deploy: `success` no mesmo merge SHA.
  - superfície pública: verificada com `Audiobooks` em `More` e no Footer.

## Devolução ao Cobogó

- Consumer card `knowledge/consumers/franklin-blog.md` atualizado para `gap_score: 0` e estado factual do Pages.
- Conceito `commit-addressed-visual-evidence` permanece `concept`, observado em um consumer; nenhum gate de promoção novo foi satisfeito.
- Nenhum achado de capacidade/sinergia exigiu issue no workspace.

## Bloqueios

nenhum

## Pressão para a próxima rotação

nenhuma; voltar à ordem normal de urgência → follow-up causal → maior gap/antiguidade.
