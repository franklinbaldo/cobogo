---
type: RoundReport
routine: cobogo
date: 2026-09-01T13:58:22-04:00
focus: franklinbaldo/franklinbaldo.github.io
candidates: [franklinbaldo/franklinbaldo.github.io]
outcome: blocked
prs: [franklinbaldo/franklinbaldo.github.io#1640]
merged: [franklinbaldo/franklinbaldo.github.io#1640]
captures:
  - https://api.github.com/repos/franklinbaldo/franklinbaldo.github.io/actions/artifacts/9806697863
  - https://api.github.com/repos/franklinbaldo/franklinbaldo.github.io/actions/artifacts/9811726761
blocked: [franklinbaldo/franklinbaldo.github.io@eebbe97af4e3aaf71b8a45b5752056dcf158122d]
---

# Rodada — Franklin blog

## Foco e alternativas

Foco mantido em `franklinbaldo/franklinbaldo.github.io` por urgência/follow-up causal: a rodada anterior tinha deixado o merge #1640 concluído, evidência visual after materializada e apenas o deploy `33535970297` pendente. Nesta rodada esse deploy concluiu como `failure`, tornando a superfície publicada o bloqueio prioritário.

Alternativas: nenhum segundo consumer aprofundado. A rotina consumiu a ação mínima cabível em diagnosticar e registrar o deploy quebrado.

## Leitor de fora — antes

Pré-registro herdado e preservado das rodadas anteriores; não foi reescrito após a falha do deploy.

- **D1 — no site:** `/audiobooks/` existe e funciona diretamente, mas a home pública verificada nesta rodada ainda não mostra `Audiobooks` no grupo `More` nem no Footer. **Gap:** o source e a captura pós-merge provam a nova navegação, mas o Pages publicado ainda serve a versão anterior.
- **D2 — por trás:** o repo prova Audiobook Factory, catálogo derivado dos manifests e feed condicionado ao estado de cada obra. **Gap:** nenhum gap D2 separado; não anunciar podcast/feed desabilitado.
- **D3 — por conta própria:** source, RSS, arquivo/busca e estrutura reproduzível da fábrica permanecem disponíveis. **Gap:** nenhum gap D3 material independente do D1.

## Gap escolhido e critério

Gap escolhido: D1, descoberta global de `/audiobooks/`.

Critério verificável preservado: Header e Footer expõem `Audiobooks` / `Audiolivros` em hierarquia secundária; checks do head exato verdes; merge no mesmo head; Pages verde no merge SHA; captura before/after da home pelo mesmo método mostra a nova entrada.

## Capturas

### Antes

- URL original: `https://franklinbaldo.github.io/`
- URL arquivada: nenhum Save Page Now verificado.
- artifact: `https://api.github.com/repos/franklinbaldo/franklinbaldo.github.io/actions/artifacts/9806697863`
- rota: `/`
- commit/head: `6503538c187bd6cb860252dd1865ef7bdb2199f5`
- método: workflow `Visual evidence` / `scripts/screenshots.mjs`, desktop 1440x900.

### Depois (build/merge, ainda não publicado)

- URL original pretendida: `https://franklinbaldo.github.io/`
- artifact: `https://api.github.com/repos/franklinbaldo/franklinbaldo.github.io/actions/artifacts/9811726761`
- rota: `/`
- commit/merge: `eebbe97af4e3aaf71b8a45b5752056dcf158122d`
- método: mesmo workflow/harness do before.
- observação: o artifact mostra a mudança, mas a home pública consultada nesta rodada ainda servia a versão anterior, sem Audiobooks na navegação global.

## Ações

- PR já mergeada: `franklinbaldo/franklinbaldo.github.io#1640`.
- merge SHA: `eebbe97af4e3aaf71b8a45b5752056dcf158122d`.
- deploy `33535970297`: tentativa 1 concluiu `failure`; job `build` `success`, job `deploy` falhou em `actions/deploy-pages`.
- uma única tentativa de rerun do job `deploy` foi disparada no mesmo run/SHA; durante esta rodada ela entrou em `in_progress`.
- issue operacional aberta: `franklinbaldo/franklinbaldo.github.io#1645` com evidência, escopo e critério de conclusão.
- issue de superfície `#1637` permanece aberta.
- consumer card atualizado no Cobogó para registrar a falha factual de Pages.

## Fatos observados

- O build e o upload do artifact de Pages concluíram `success` no merge SHA.
- A primeira execução de `actions/deploy-pages` falhou após aproximadamente 10 minutos.
- A home pública consultada após a falha ainda não contém `Audiobooks` no grupo `More` nem no Footer.
- A recaptura do merge SHA já contém a nova entrada no Footer e o source do mesmo SHA contém Audiobooks no grupo secundário do Header.
- Um rerun isolado do job de deploy foi iniciado, sem alterar source ou build.

## Inferências

- O bloqueio atual não é a implementação visual nem o build: é a publicação do artifact pelo Pages.
- Reverter a #1640 ou alterar a superfície como workaround seria incorreto; o achado é operacional e foi encaminhado ao repo dono em #1645.
- O gap D1 continua material para o leitor externo porque o site publicado ainda não oferece a descoberta global que o repo já implementou.

## Resultado do gap

**Não fechado / bloqueado.** O critério visual e de merge está satisfeito no source e nos artifacts, mas o critério de Pages verde no merge SHA falhou e a superfície pública ainda serve a versão anterior. O critério não foi reescrito.

## Gates

- PR `#1640`: mergeada anteriormente somente após checks verdes no head exato `a03c3670ca95100ae9bc638e7633a0efa13f7088`.
- merge `eebbe97af4e3aaf71b8a45b5752056dcf158122d`:
  - Visual evidence pós-merge: `success`.
  - Deploy to GitHub Pages `33535970297`, tentativa 1: `failure`.
  - build: `success`.
  - deploy/actions-deploy-pages: `failure`.
  - tentativa 2 do job deploy: iniciada nesta rodada; ainda `in_progress` na última leitura.

## Devolução ao Cobogó

- Consumer card `knowledge/consumers/franklin-blog.md` atualizado; `gap_score: 1` mantido.
- Conceito `commit-addressed-visual-evidence` permanece `concept`, observado em um consumer; nenhum gate de promoção novo foi satisfeito.
- Nenhum achado de capacidade/sinergia exigiu issue no workspace.

## Bloqueios

- SHA: `eebbe97af4e3aaf71b8a45b5752056dcf158122d`.
- causa: `actions/deploy-pages` falhou apesar de build e upload do artifact verdes.
- estado: #1645 aberta; rerun único do job em andamento; #1637 permanece aberta; home pública ainda sem a nova navegação.
- condição de retomada: um deploy `success` para um SHA que preserve integralmente a #1640, seguido de verificação da home publicada; só então fechar #1637 e reduzir o card para `gap_score: 0`.

## Pressão para a próxima rotação

`franklinbaldo/franklinbaldo.github.io` enquanto o deploy continuar falhando, por urgência de superfície publicada. Se o rerun ficar verde, apenas fechar causalmente #1637/card e voltar à rotação normal.
