---
type: RoundReport
routine: cobogo
date: 2026-08-31T07:01:00-04:00
focus: franklinbaldo/letra_bonita
candidates: [franklinbaldo/letra_bonita, franklinbaldo/cobogo, franklinbaldo/intuit]
outcome: blocked
prs: []
merged: []
captures: []
blocked: [franklinbaldo/letra_bonita, franklinbaldo/cobogo#407]
---

## Foco e alternativas

As skills `brazilian-web-design`, `cobogo-design-review` e `cobogo-consumer-synergy` foram relidas, assim como os RoundReports `2026-08-31T0401`, `T0501` e `T0602`. A pressão explícita da rodada anterior apontava Letra Bonita: card ainda legado, última verificação factual em 2026-08-17 e `unmet_needs` centrados em captura/browser evidence e gate pré-merge.

O único follow-up próprio aberto continua sendo `franklinbaldo/cobogo#407`. O head permanece `32b3897335df837e0b49035ecce77d4d59fa3738`, `mergeable: false`, com a dívida já registrada de 65 falhas em 152 testes. Rebase isolado não fecharia o gate e reconciliar a suíte excede o orçamento desta rodada; não houve merge nem enfraquecimento do gate.

Letra Bonita foi escolhida pela pressão anterior e pela necessidade de reclassificar o card. Na leitura factual surgiu o mesmo boundary observado em Ecos do Pampa e Formato Legal: o `ProjectProfile` vigente declara `franklinbaldo/letra_bonita` ativo, mas `get_repo` retorna 404 e a busca de repositórios acessíveis não encontra o nome. Busca pública também não localizou repo substituto. Ao mesmo tempo, `https://franklinbaldo.github.io/letra_bonita/` continua respondendo e expondo o fluxo central. Isso prova superfície publicada, não resolve identidade/status do repo. Intuit ficou como próxima alternativa legada; Cobogó foi descartado por `gap_score: 0` e bloqueio conhecido da #407.

## Leitor de fora — antes

### D1 — o que a pessoa pode fazer diretamente

A superfície publicada permite inserir texto, usar exemplos, configurar fonte/tamanho, texto tracejado, linhas-guia, inclinação, altura-X, cor, tamanho A4/Carta e orientação; também expõe impressão da pré-visualização e abertura do SVG.

Gap D1: nenhum material estabelecido. Os antigos `unmet_needs` de captura reproduzível e gate pré-merge são capacidade de evidência/validação, não tarefa ausente para o leitor. O site público segue acessível, mas a implementação atual não pôde ser revalidada contra o repo canônico.

### D2 — o que o projeto faz por trás e por que existe

O `ProjectProfile` vigente descreve uma aplicação zero-build que transforma texto + configuração tipográfica/guias/geometria em worksheet SVG pronta para inspeção, impressão e reúso vetorial; Gemini é opcional e user-keyed no browser. A própria superfície deixa perceptível a finalidade `configurar → visualizar → imprimir/SVG` e identifica separadamente o gerador de citações com IA.

Gap D2: nenhum material estabelecido na última verificação. O repo atual não pôde ser recuperado para confirmar detalhes de implementação, dependências e fronteiras em 2026-08-31.

### D3 — o que a pessoa pode fazer sozinha com os artefatos publicados

A superfície oferece diretamente `Abrir SVG (Melhor Qualidade para Impressão)` e impressão do preview atual. Na evidência de 2026-08-17, o source checkout também era reproduzível; não havia dataset/API/release autônoma adicional.

Gap D3: nenhum material conhecido. Nenhum artefato novo foi inferido sem acesso ao repo atual.

## Gap escolhido e critério

Nenhum gap visual/editorial foi escolhido para intervenção. Os dois antigos `unmet_needs` foram reclassificados como dívida de evidência/CI.

Critério verificável congelado: a rodada só poderia fechar como `inspection-only` se o repo atual pudesse ser recuperado e confirmasse que D1/D2/D3 permanecem sem gap material. Se a superfície pública responder mas a fonte canônica não puder ser revalidada, registrar `blocked`, migrar o card sem inventar estado novo e encaminhar repo/status ao workspace.

## Capturas

Nenhuma captura antes/depois foi produzida porque não houve mudança visualmente material. A rota pública foi inspecionada em 2026-08-31 e continua respondendo em `https://franklinbaldo.github.io/letra_bonita/`, mas isso não foi promovido a pixel-proof controlado nem a evidência do estado interno de Pages/deploy.

Última evidência de captura/browser reproduzível registrada no card: ausente em 2026-08-17; esse fato permanece dívida de evidência.

## Ações

- Skills operacionais e três RoundReports mais recentes relidos.
- `franklinbaldo/cobogo#407` revalidada: aberta, head `32b3897335df837e0b49035ecce77d4d59fa3738`, não mergeável.
- Card legado `knowledge/consumers/letra-bonita.md` confrontado com `workspace/knowledge/projects/letra-bonita.md`.
- `get_repo(franklinbaldo/letra_bonita)` retornou 404; busca entre repositórios acessíveis e busca pública não localizaram repo substituto.
- A rota `https://franklinbaldo.github.io/letra_bonita/` continua respondendo e expõe controles de autoria, preview, impressão e SVG.
- Aberta `franklinbaldo/workspace#26` para revalidar repo canônico/status sem inferência do Cobogó.
- `knowledge/consumers/letra-bonita.md` migrado para `ConsumerCard`, `last_reviewed: 2026-08-31`, `gap_score: 0`, mantendo `status: active` por deferência ao `ProjectProfile` vigente.
- Nenhuma PR substantiva de superfície criada; nenhum pattern promovido e nenhuma contagem de conceito incrementada sem nova captura.

## Fatos observados

- O `ProjectProfile` vigente marca `franklinbaldo/letra_bonita` como ativo, última revisão em 2026-08-17.
- A evidência canônica de 2026-08-17 registra Pages `built`, aplicação zero-build em `index.html`, geração dinâmica de SVG e gate `.github/workflows/static-app.yml`.
- O card legado transformava falta de captura reproduzível e fortalecimento de pre-merge evidence em `unmet_needs`.
- Em 2026-08-31, o repo canônico não pôde ser recuperado pelo conector e não apareceu nas buscas disponíveis.
- A superfície pública continua acessível e exibe o fluxo central, inclusive `Imprimir Pré-visualização Atual` e `Abrir SVG (Melhor Qualidade para Impressão)`.
- O site também marca a newsletter como `Em Breve`/`Funcionalidade em desenvolvimento`, portanto não foi tratada como capacidade operacional presente.
- `franklinbaldo/cobogo#407` continua aberta e não mergeável no mesmo head conhecido.

## Inferências

- Captura reproduzível e browser smoke são capacidades de verificação; não devem inflar `gap_score` quando D1/D2/D3 do leitor já estão cobertos.
- Uma URL Pages viva não prova que o repo canônico ainda existe sob o mesmo nome nem que o pipeline registrado continua vigente.
- Manter `status: active` no card é deferência ao `ProjectProfile`, não nova confirmação do repo; o workspace deve resolver identidade/status primeiro.
- Letra Bonita continua um pressure case conceitual para editor/preview e document/print, mas sem nova captura não deve aumentar evidência de promoção.

## Resultado do gap

**Bloqueado.** Nenhum gap D1/D2/D3 material foi demonstrado, mas o critério não permite declarar `inspection-only`: a fonte canônica não pôde ser revalidada. O card foi corrigido para separar dívida operacional de gaps do leitor, e o fato de repo/status foi encaminhado ao workspace.

## Gates

- Nenhuma PR substantiva criada nesta rodada.
- `franklinbaldo/cobogo#407`: head `32b3897335df837e0b49035ecce77d4d59fa3738`, aberta, `mergeable: false`; sem merge.
- Letra Bonita: gate de leitura/repo bloqueado antes de qualquer mudança de superfície; nenhum required check atual nem deployment interno foi reivindicado.

## Devolução ao Cobogó

- `knowledge/consumers/letra-bonita.md` migrado para schema `ConsumerCard`, `last_reviewed: 2026-08-31`, `gap_score: 0`.
- Os antigos `unmet_needs` de captura e pre-merge evidence foram movidos conceitualmente para `Capacidades de superfície` como dívida de evidência/validação.
- Nenhum conceito novo registrado: a relação autoria → preview → artefato já estava documentada e não houve nova captura before/after para aumentar a proveniência.
- Nenhum pattern promovido; o gate de ≥2 consumers com before/after permanece intacto.
- `workspace#26` aberto para o fato fora da autoridade desta rotina.

## Bloqueios

- `franklinbaldo/letra_bonita` — repo canônico do `ProjectProfile` não recuperável pelo conector/buscas nesta rodada, embora a URL Pages histórica siga respondendo. Estado: identidade/status/pipeline atuais não revalidáveis. Condição de retomada: workspace #26 resolver repo canônico/visibilidade/status e a rotina recuperar README/workflows/deploy antes de intervenção.
- `franklinbaldo/cobogo#407` — SHA `32b3897335df837e0b49035ecce77d4d59fa3738`; causa: branch não mergeável e 65/152 falhas Vitest já medidas. Condição: rebase + reconciliação deliberada da suíte em novo head, com todos os required checks verdes.

## Pressão para a próxima rotação

`franklinbaldo/intuit` — card ainda legado, `last_verified: 2026-08-16`, com `unmet_needs` de pressure-test de editor/preview e theme override que precisam ser separados entre gaps reais D1/D2/D3 e necessidades do design system antes de qualquer intervenção visual.
