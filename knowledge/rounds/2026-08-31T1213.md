---
type: RoundReport
routine: cobogo
date: 2026-08-31T12:13:00-04:00
focus: franklinbaldo/intuit
candidates: [franklinbaldo/intuit, franklinbaldo/cobogo]
outcome: closed
prs: [franklinbaldo/intuit#26]
merged: [franklinbaldo/intuit#26]
captures: [intuit-home-0df3ad1db891bc3f95e017b7464ef7cafe68ba7d, intuit-home-2bbf3376e2b7f461ddd7e90e1a1a8cb791ac0d06]
blocked: [franklinbaldo/cobogo#407]
---

## Foco e alternativas

As três skills operacionais e os RoundReports `2026-08-31T1108`, `T1037` e `T0837` foram relidos. Não apareceu urgência nova comprovada. `franklinbaldo/cobogo#407` permaneceu follow-up próprio aberto porém bloqueado no head `32b3897335df837e0b49035ecce77d4d59fa3738`, sem estado verde/mergeável que pudesse decidir a rodada. Pela regra de maior gap, `franklinbaldo/intuit` permaneceu foco com `gap_score: 1`. A rodada anterior abriu `intuit#25` como condição causal para produzir captura reproduzível antes de tocar no residual visual de `#14`.

## Leitor de fora — antes

### D1 — o que a pessoa pode fazer diretamente

A pessoa pode editar/colar HTML, renderizar no preview, copiar link, atualizar a URL, limpar o editor, alternar scripts no sandbox e alternar tema. Preview, erros e grupos operacionais já têm semântica acessível após #17/#24.

Gap D1: os controles principais ainda não têm prova de foco visual explícito equivalente para navegação por teclado e as transições/escala atuais não têm tratamento explícito para `prefers-reduced-motion`. Esse residual está registrado em `intuit#14`. A rotina ainda não possui uma captura reproduzível da home para estabelecer o before visual exigido.

### D2 — o que o projeto faz por trás e por que existe

Intuit continua sendo um renderer HTML/visual-testing client-side cuja relação principal é fonte/edit → preview renderizado. O default permanece `sandbox=""`, scripts são opt-in, o runtime usa `srcdoc` e estado pode ser transportado por `?data=`, `?b64=` e Gist.

Gap D2: nenhum material estabelecido.

### D3 — o que a pessoa pode fazer sozinha com os artefatos publicados

A pessoa pode transportar/reproduzir estado por URL/Base64/Gist e executar o source checkout como aplicação estática. Não há release/dataset/API autônoma adicional escondida pela superfície.

Gap D3: nenhum material estabelecido.

## Gap escolhido e critério

Gap escolhido: D1 — tornar fechável, com evidência causal, o residual de foco visual + reduced-motion por meio de uma capacidade reproduzível de captura da mesma rota, sem alterar a UI nesta fatia.

Critério verificável congelado: a rodada só fecha se o repo passar a produzir uma screenshot recuperável da home `/intuit/` em viewport explícito e estável, associada ao commit SHA, sem segredos e sem mudar sandbox/srcdoc/Base64/tema/comportamento; a mesma capacidade deve ser executável em PR futura para servir de `after`. O Test e o Pages existentes devem continuar verdes. A implementação de foco/reduced-motion de #14 permanece fora de escopo desta rodada.

## Capturas

Antes operacional — a ausência de captura no início da rodada foi o bloqueio factual. A #26 foi criada sem alterar `index.html`/runtime e produziu, no head `0df3ad1db891bc3f95e017b7464ef7cafe68ba7d`, a primeira captura determinística da home em `1440x900`: Visual capture run `33413051418`, artifact `intuit-home-0df3ad1db891bc3f95e017b7464ef7cafe68ba7d`, id `9765912419`, com metadata de SHA/rota/viewport/timestamp.

Baseline pós-merge — URL original `https://franklinbaldo.github.io/intuit/`; rota `/intuit/`; merge/deploy `2bbf3376e2b7f461ddd7e90e1a1a8cb791ac0d06`; Visual capture run `33413210167`; artifact `intuit-home-2bbf3376e2b7f461ddd7e90e1a1a8cb791ac0d06`, id `9765969339`; viewport `1440x900`; método Playwright versionado + artifact de GitHub Actions. Como #26 não alterou a UI, esta captura é o baseline `before` válido para a futura implementação de #14, e não um `after` de mudança visual nesta rodada.

Save Page Now: nenhum.

## Ações

- Skills `brazilian-web-design`, `cobogo-design-review` e `cobogo-consumer-synergy` relidas.
- RoundReports `2026-08-31T1108`, `T1037` e `T0837` relidos.
- `cobogo#407` revalidada: aberta, head `32b3897335df837e0b49035ecce77d4d59fa3738`, `mergeable: false`; sem merge ou bypass.
- Card `knowledge/consumers/intuit.md`, `workspace/knowledge/projects/intuit.md`, `intuit#25`, Playwright e workflows atuais confrontados.
- Pré-registro deste RoundReport criado antes de qualquer mudança no consumer.
- Branch `surface/reproducible-visual-capture` criada a partir de `main` `65f0decbb225d78a1277265023634133abf98322`.
- PR `franklinbaldo/intuit#26` aberta no head `0df3ad1db891bc3f95e017b7464ef7cafe68ba7d`, adicionando somente `tests/playwright/visual-capture.spec.js` e `.github/workflows/visual-capture.yml`.
- Test run `33413051136` e Visual capture run `33413051418` concluíram verdes no head exato; o segundo publicou artifact recuperável e provenance.
- #26 só então foi squash-mergeada como `2bbf3376e2b7f461ddd7e90e1a1a8cb791ac0d06`; #25 fechou automaticamente.
- Pós-merge no mesmo SHA: Pages run `33413210139` verde, Test run `33413210147` verde e Visual capture run `33413210167` verde com novo artifact de baseline.
- `intuit#14` recebeu comentário com o baseline exato para a futura intervenção de foco/reduced-motion.
- Aberta `franklinbaldo/workspace#27` para o dono do inventário reconciliar a nova capacidade no `ProjectProfile`, sem esta rotina escrevê-lo.

## Fatos observados

- `intuit#25` estava aberta no início e definia a ausência de captura reproduzível como condição de evidência, não como gap adicional do leitor.
- `main` do Intuit no início da rodada era `65f0decbb225d78a1277265023634133abf98322`.
- O repo já tinha Playwright/Test/Pages, mas não produzia artifact de screenshot recuperável com SHA/viewport/rota.
- A #26 não alterou `index.html`, sandbox, `srcdoc`, Base64, tema nem runtime da aplicação.
- O head `0df3ad1db891bc3f95e017b7464ef7cafe68ba7d` ficou mergeável e teve Test + Visual capture verdes antes do merge.
- O merge `2bbf3376e2b7f461ddd7e90e1a1a8cb791ac0d06` teve Pages, Test e Visual capture verdes; o artifact de main é `intuit-home-2bbf3376e2b7f461ddd7e90e1a1a8cb791ac0d06`.
- `intuit#14` continua aberto exclusivamente para o residual visual/motion; agora possui baseline before reproduzível.
- O `ProjectProfile` ainda afirma ausência de captura browser versionada; workspace #27 registra a reconciliação factual necessária.

## Inferências

- Resolver a captura primeiro não foi trabalho cosmético: removeu o bloqueio de evidência que impedia modificar foco/motion sem quebrar o método before/after.
- A nova capacidade é infraestrutura local de validação, não um pattern visual e não aumenta `gap_score` por si só.
- A captura do merge #26 é adequada como baseline causal para #14 porque a PR não alterou a superfície renderizada e o workflow agora pode ser repetido nas mesmas condições na futura PR visual.
- Uma única adoção de captura no Intuit não satisfaz o gate de promoção do Cobogó; não há justificativa para generalizar o workflow como pattern compartilhado nesta rodada.

## Resultado do gap

**Fechado para o critério desta rodada.** O repo agora produz screenshot recuperável da mesma home em viewport `1440x900`, com SHA/rota/viewport/timestamp, tanto em PR quanto em `main`; Test e Pages permaneceram verdes e nenhum comportamento da aplicação foi alterado. O gap D1 substantivo de foco visível + `prefers-reduced-motion` continua aberto em #14, mas deixou de estar bloqueado pela ausência de baseline visual.

## Gates

- `franklinbaldo/intuit#26` — head `0df3ad1db891bc3f95e017b7464ef7cafe68ba7d`; mergeable; Test run `33413051136`: success; Visual capture run `33413051418`: success + artifact `9765912419`; squash merge somente após ambos verdes.
- merge #26 — `2bbf3376e2b7f461ddd7e90e1a1a8cb791ac0d06`; Pages run `33413210139`: success; Test run `33413210147`: success; Visual capture run `33413210167`: success + artifact `9765969339`.
- `franklinbaldo/cobogo#407` — head `32b3897335df837e0b49035ecce77d4d59fa3738`; `mergeable: false`; dívida conhecida de 65/152 testes Vitest; sem merge.

## Devolução ao Cobogó

- Consumer card do Intuit atualizado em 2026-08-31; `gap_score` permanece `1`, mas `Captura visual versionada` passa de ausente para presente com evidence run/artifact/SHA.
- Nenhum conceito novo ou pattern promovido: a capacidade é local e ainda sem evidência convergente em ≥2 consumers.
- `workspace#27` aberto para reconciliar a capacidade no `ProjectProfile`, preservando a fronteira de autoridade.

## Bloqueios

- `franklinbaldo/cobogo#407` — SHA `32b3897335df837e0b49035ecce77d4d59fa3738`; causa: não mergeável e dívida ampla de self-conformance já medida em 65/152 falhas Vitest. Estado: aberta. Condição de retomada: classificar contratos atuais vs expectativas fossilizadas e obter todos os required checks verdes no head atual.

## Pressão para a próxima rotação

`franklinbaldo/intuit` — #14 agora está causalmente desbloqueada: usar o artifact de `main` `9765969339` como before, implementar apenas foco visual + `prefers-reduced-motion`, executar a mesma captura na PR como after, e então fechar checks → merge → Pages → recaptura pós-merge.
