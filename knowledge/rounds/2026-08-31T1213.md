---
type: RoundReport
routine: cobogo
date: 2026-08-31T12:13:00-04:00
focus: franklinbaldo/intuit
candidates: [franklinbaldo/intuit, franklinbaldo/cobogo]
outcome: blocked
prs: []
merged: []
captures: []
blocked: [franklinbaldo/intuit#14, franklinbaldo/cobogo#407]
---

## Foco e alternativas

As três skills operacionais e os RoundReports `2026-08-31T1108`, `T1037` e `T0837` foram relidos. Não há urgência nova comprovada. `franklinbaldo/cobogo#407` permanece follow-up próprio aberto porém bloqueado no head conhecido, sem estado verde/mergeável que possa decidir a rodada. Pela regra de maior gap, `franklinbaldo/intuit` permanece foco com `gap_score: 1`. A rodada anterior abriu `intuit#25` como condição causal para produzir captura reproduzível antes de tocar no residual visual de `#14`.

## Leitor de fora — antes

### D1 — o que a pessoa pode fazer diretamente

A pessoa pode editar/colar HTML, renderizar no preview, copiar link, atualizar a URL, limpar o editor, alternar scripts no sandbox e alternar tema. Preview, erros e grupos operacionais já têm semântica acessível após #17/#24.

Gap D1: os controles principais ainda não têm prova de foco visual explícito equivalente para navegação por teclado e as transições/escala atuais não têm tratamento explícito para `prefers-reduced-motion`. Esse residual está registrado em `intuit#14`. A rotina ainda não possui uma captura reproduzível da home para estabelecer o before visual exigido.

### D2 — o que o projeto faz por trás e por que existe

Intuit continua sendo um renderer HTML/visual-testing client-side cuja relação principal é fonte/edit → preview renderizado. O default permanece `sandbox=""`, scripts são opt-in, o runtime usa `srcdoc` e estado pode ser transportado por `?data=`, `?b64=` e Gist.

Gap D2: nenhum material estabelecido.

### D3 — o que a pessoa pode fazer sozinha com os artefatos publicados

A pessoa pode transportar/reproduzir estado por URL/Base64/Gist e executar o source checkout como aplicação estática. Não há release/dataset/API autônoma adicional escondida pela superfície.

Gap D3: nenhum material estabelecido.

## Gap escolhido e critério

Gap escolhido: D1 — tornar fechável, com evidência causal, o residual de foco visual + reduced-motion por meio de uma capacidade reproduzível de captura da mesma rota, sem alterar a UI nesta fatia.

Critério verificável congelado: a rodada só fecha se o repo passar a produzir uma screenshot recuperável da home `/intuit/` em viewport explícito e estável, associada ao commit SHA, sem segredos e sem mudar sandbox/srcdoc/Base64/tema/comportamento; a mesma capacidade deve ser executável em PR futura para servir de `after`. O Test e o Pages existentes devem continuar verdes. A implementação de foco/reduced-motion de #14 permanece fora de escopo desta rodada.

## Capturas

Antes: ainda indisponível no início da rodada. A primeira captura válida desta rodada deve representar o `main` sem mudanças visuais e servirá como baseline causal para #14.

Depois: pendente.

Save Page Now: nenhum.

## Ações

Pré-registro criado antes de qualquer mudança no consumer.

## Fatos observados

- `intuit#25` está aberta e define exatamente a infraestrutura de captura necessária.
- `main` do Intuit no início da rodada: `65f0decbb225d78a1277265023634133abf98322`.
- O repo já possui Playwright e workflows Test/Pages, mas ainda não um artifact de screenshot versionado/reproduzível.

## Inferências

- Resolver a captura primeiro não é trabalho cosmético: é a condição de evidência para a mudança visual já pré-registrada em #14.
- A captura não aumenta `gap_score`; ela apenas torna o gap D1 verificável sem violar o método.

## Resultado do gap

Pendente.

## Gates

Pendente.

## Devolução ao Cobogó

Pendente.

## Bloqueios

- `franklinbaldo/intuit#14` — bloqueado até existir baseline visual reproduzível.
- `franklinbaldo/cobogo#407` — bloqueio conhecido da suíte Vitest; revalidar nesta rodada.

## Pressão para a próxima rotação

Pendente.
