---
type: RoundReport
routine: cobogo
date: 2026-09-01T18:57:51-04:00
focus: franklinbaldo/causaganha
candidates: [franklinbaldo/causaganha, franklinbaldo/cobogo, franklinbaldo/the-lab]
outcome: partial
prs: [franklinbaldo/causaganha#979]
merged: [franklinbaldo/causaganha#979]
captures: [https://github.com/franklinbaldo/causaganha/actions/runs/33568866419]
blocked: [franklinbaldo/causaganha#979]
---

# Rodada Cobogó — 2026-09-01T1857

## Foco e alternativas

A regra que decide é follow-up causal. A rodada anterior deixou `franklinbaldo/causaganha#979` aberta no head `ce11e1f26e8b4f23127408ad2c8d1d43cb4707fe`, com conteúdo de superfície pronto e um único bloqueio operacional: o workflow de evidência usava `python -m http.server`, que não resolvia a clean URL `/causaganha/agentes` para o arquivo estático `agentes.html`.

`franklinbaldo/cobogo#407` segue como follow-up próprio, mas o bloqueio conhecido de 65 falhas Vitest em 20 arquivos excede uma correção mecânica. `franklinbaldo/the-lab` permanece com deploy desabilitado, portanto não supera o follow-up diretamente recuperável de CausaGanha.

## Leitor de fora — antes

### D1 — o que pode fazer diretamente no site

O leitor consegue consultar processos, pesquisar publicações e explorar cobertura/estado das fontes na superfície pública.

**Gap D1 [fato]:** nenhum gap material novo nesta rodada.

### D2 — o que o projeto faz por trás e por que existe

A superfície já explica a separação entre arquivo, estado, teor, proveniência, freshness e limites de cobertura; o `ProjectProfile` vigente confirma essa tese e registra MCP read-only, Internet Archive e Parquet/DuckDB entre as capacidades operacionais.

**Gap D2 [fato]:** nenhum gap material novo nesta rodada.

### D3 — o que pode fazer sozinho com os artefatos publicados

O repositório prova um servidor MCP read-only local, executável por stdio, mas a superfície pública ainda não expunha uma rota dedicada que ensinasse esse uso sem inventar endpoint HTTP remoto.

**Gap D3 [fato]:** `/agentes` ainda não estava publicada no início da rodada; #979 existia, mas não podia ser mergeada sem evidência visual válida no head atual.

## Gap escolhido e critério

Fechar o gap D3 de autoatendimento do MCP local, preservando o critério pré-registrado na rodada anterior.

**Critério verificável:** depois da mudança, a navegação pública expõe `Agentes` e `/agentes` mostra (1) Arquivo / Estado / Teor ligados às tools MCP provadas pelo repo, (2) configuração local com `uv run causaganha-mcp`, e (3) transporte remoto como indisponível; a evidência before/after deve usar a própria rota `/agentes`, e a rodada só fecha após deploy verificado e recaptura publicada.

## Capturas

- Before/after de PR: workflow `Agents Surface Visual Capture`, run `33568866419`, head `b270a88ee5bda10e259832d3e7b1f18a7aa221f3`.
- Método: builds base/head com fixtures idênticas; serving local em portas distintas; screenshots Playwright desktop 1280×900 e mobile 390×844 sobre `http://127.0.0.1:<porta>/causaganha/agentes`.
- Correção do método: no estado `after`, quando `agentes.html` existe, o workflow materializa `agentes/index.html` somente para reproduzir a resolução da clean URL do Pages; o base continua sem a rota, preservando a prova de ausência antes da PR.
- Resultado do run: `success`, concluído em 2026-09-01T22:59:59Z.
- URL pública alvo: `https://franklinbaldo.github.io/causaganha/agentes`.
- Captura pós-deploy: ainda não disponível nesta rodada; os workflows de push do merge ainda estavam em execução e `Deploy Web` não tinha conclusão verificável no momento do encerramento.
- URL arquivada: nenhuma; Save Page Now não é capacidade registrada para esta superfície.

## Ações

- `b270a88ee5bda10e259832d3e7b1f18a7aa221f3` — correção mecânica do workflow para reproduzir a clean URL sem alterar conteúdo da página.
- Run `33568866419` — capture before/after verde no head atual da PR.
- No mesmo head, 11 check-runs foram observados sem estado `queued`/`in_progress` e sem conclusão `failure`; PR mergeável.
- `franklinbaldo/causaganha#979` — mergeada por squash com `expected_head_sha=b270a88ee5bda10e259832d3e7b1f18a7aa221f3`.
- Landing em `main`: `333308303bcabf681a6513960fa6d53299ffc65b`.
- Após o merge, os workflows de push `Agents Surface Visual Capture` e `Product Surface Visual Capture` começaram para o landing SHA; a recaptura publicada depende do deploy do Pages.
- Nenhuma mudança no Cobogó compartilhado: o gate de promoção não foi satisfeito nem era necessário para este gap local.
- Nenhuma issue de capacidade/sinergia no workspace: o MCP read-only já consta no `ProjectProfile` vigente.

## Fatos observados

- O `ProjectProfile` de CausaGanha está `active`, prioridade alta e registra MCP read-only como capacidade operacional.
- #979 não inventa transporte remoto; a página declara explicitamente que o endpoint HTTP público ainda não está disponível.
- A falha da rodada anterior era do método de captura, não do conteúdo nem do build.
- O run `33568866419` ficou verde depois da correção da clean URL.
- #979 foi mergeada no landing `333308303bcabf681a6513960fa6d53299ffc65b` somente após o head atual ficar sem checks pendentes/vermelhos observados.
- O workflow `Deploy Web` é disparado por push em `main` quando `web/**` muda; a PR mergeada contém mudanças em `web/**`.
- No encerramento desta rodada, ainda não havia deploy Pages concluído e recaptura pública verificável para `/agentes`.

## Inferências

- O gap está tecnicamente implementado em `main`, mas não pode ser classificado como fechado enquanto a superfície pública e a captura pós-deploy não forem verificadas.
- A correção do workflow é infraestrutura de evidência específica deste consumer; não há base para promoção ao Cobogó.
- A próxima rodada deve começar pelo landing SHA `333308303bcabf681a6513960fa6d53299ffc65b`, verificar `Deploy Web`, abrir `/agentes` publicada e obter a captura pós-deploy antes de considerar qualquer novo trabalho substantivo.

## Resultado do gap

**Parcial.** O critério de implementação e o before/after de PR foram satisfeitos, e #979 chegou a `main`. Falta a parte final e obrigatória do critério: deploy Pages verificado e recaptura da rota pública `/agentes`.

## Gates

- #979 head `b270a88ee5bda10e259832d3e7b1f18a7aa221f3`: `capture-agents-surface` verde no run `33568866419`; check-runs do head sem `queued`/`in_progress` e sem `failure` no momento do merge; `mergeable: true`.
- Merge: squash aceito com proteção por `expected_head_sha`; landing `333308303bcabf681a6513960fa6d53299ffc65b`.
- Pós-merge: workflows de push em execução; deploy/Pages ainda não verificado.

## Devolução ao Cobogó

- ConsumerCard de CausaGanha permanece factualmente correto com `last_reviewed: 2026-09-01` e `gap_score: 1` enquanto a rota pública não for verificada; nenhuma edição adicional necessária nesta rodada.
- Conceitos: nenhum novo; sem captura pós-deploy ainda não há nova proveniência fechada a registrar.
- Pattern promovido: nenhum; gate de ≥2 consumers não satisfeito.
- Workspace: nenhuma issue nova; nenhuma capacidade ou sinergia nova foi descoberta.

## Bloqueios

- `franklinbaldo/causaganha#979` / landing `333308303bcabf681a6513960fa6d53299ffc65b` — estado: mergeado, aguardando verificação do deploy Pages e recaptura pública. Condição de retomada: workflow `Deploy Web` do landing concluir com sucesso; então verificar `https://franklinbaldo.github.io/causaganha/agentes` e registrar captura pós-deploy da mesma rota. Até isso ocorrer, o loop visual não está fechado.

## Pressão para a próxima rotação

`franklinbaldo/causaganha` — follow-up causal imediato do landing `333308303bcabf681a6513960fa6d53299ffc65b`: fechar deploy + recaptura de `/agentes`; não iniciar um segundo PR substantivo antes disso.
