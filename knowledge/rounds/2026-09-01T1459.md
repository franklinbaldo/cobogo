---
type: RoundReport
routine: cobogo
date: 2026-09-01T14:59:44-04:00
focus: franklinbaldo/the-lab
candidates: [franklinbaldo/the-lab, franklinbaldo/cobogo]
outcome: inspection-only
prs: []
merged: []
captures: []
blocked: [franklinbaldo/the-lab#128, franklinbaldo/cobogo#407]
---

# Rodada — The Lab

## Foco e alternativas

As três skills operacionais e a memória recente da rotina foram relidas antes da seleção. Não apareceu urgência nova. O follow-up próprio `franklinbaldo/cobogo#407` está agora `mergeable: true`, mas o head exato `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e` continua com o check `build` em `failure`; `capture` e GitGuardian estão verdes, mas isso não satisfaz o gate de todos os required checks verdes. Portanto não houve merge nem bypass.

Pela regra de maior gap, `franklinbaldo/the-lab` decide o foco: é o ConsumerCard ativo corrente com `gap_score: 1`. O gap já conhecido é D2 e depende de publicação, não de uma nova solução visual. Não foi aberto segundo PR de UI para contornar um deploy bloqueado.

## Leitor de fora — antes

- **D1 — no site:** a home pública permite navegar por Articles, Lab, Team e About, ler o latest dispatch e entrar no arquivo. **Gap:** nenhum D1 material novo.
- **D2 — por trás:** o repo prova que `main` contém a inscrição `Source snapshot` com provenance do snapshot Rosencrantz, mas a superfície pública reaberta nesta rodada ainda não a mostra. **Gap:** o leitor externo não consegue perceber qual snapshot upstream gerou a publicação, embora a solução já esteja implementada em `main`.
- **D3 — por conta própria:** GitHub, Papers e Lab State continuam acessíveis para inspeção independente. **Gap:** nenhum artefato autônomo adicional foi provado pelo repo e ocultado pela superfície.

## Gap escolhido e critério

Gap escolhido: D2, materializar na publicação a provenance já implementada em `main`.

Critério verificável preservado de `the-lab#128`: `Deploy Site` ativo; execução válida do `main` com install/build/deploy verdes; superfície publicada correspondente ao commit implantado; `Source snapshot` verificável na página publicada. Se houver mudança visualmente material para fechar esse ciclo, before/after pelo mesmo método continua obrigatório.

## Capturas

- Antes: nenhuma nova screenshot canônica foi produzida. A rota pública `https://franklinbaldo.github.io/the-lab/` foi reaberta em 2026-09-01 e continua sem `Source snapshot`; inspeção HTML não foi promovida a captura visual.
- Depois: nenhum, pois nenhuma intervenção foi executada.
- Save Page Now: nenhum verificado.

## Ações

- Revalidada `franklinbaldo/the-lab#128`: permanece aberta e continua sendo a autoridade do bloqueio de deploy.
- Revalidado `franklinbaldo/workspace#30`: permanece aberto; o ProjectProfile de The Lab ainda não foi restaurado pela autoridade dona.
- Revalidado `franklinbaldo/cobogo#407`: `mergeable: true`, head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`, porém `build=failure`; nenhum merge.
- Reaberta a home pública de The Lab: ainda não expõe `Source snapshot`.
- ConsumerCard `knowledge/consumers/the-lab.md` atualizado apenas com fatos revalidados em 2026-09-01; `gap_score` permanece 1.
- Nenhum PR substantivo aberto: o gap não é fechável dentro da autoridade desta rotina enquanto o deploy permanece desabilitado/bloqueado.

## Fatos observados

- O último commit de `franklinbaldo/the-lab` continua sendo `468b14f1e1c627eafbc4fc91513a28e6e2521aa3`, merge da #127 em 2026-08-14.
- A home pública responde em 2026-09-01 e não contém `Source snapshot` no conteúdo servido.
- `the-lab#128` permanece aberta e documenta `Deploy Site` em `disabled_manually` como causa confirmada.
- `workspace#30` permanece aberta.
- `cobogo#407` está mergeável no nível Git, mas seu check `build` no head atual permanece vermelho; `capture` está verde.

## Inferências

- O gap de The Lab continua sendo publicação divergente de `main`, não falta de design nem falta de implementação da provenance.
- Criar outra alteração visual no consumer não reduziria o gap e violaria a fronteira da rotina ao mascarar um bloqueio operacional já corretamente atribuído.
- A mudança de `mergeable: false` para `mergeable: true` em #407 não é causalmente suficiente para follow-up: o gate de checks continua falhando.

## Resultado do gap

**Não fechado; rodada sem intervenção.** O gap continua material e factual, mas não é fechável no orçamento/autoridade desta rotina sem o desbloqueio operacional descrito em #128. Nenhuma atividade foi fabricada para produzir movimento aparente.

## Gates

- `franklinbaldo/cobogo#407` — head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`: `build=failure`, `capture=success`, GitGuardian=`success`, automerge=`skipped`; não mergeado.
- The Lab — nenhuma PR própria aberta nesta rodada; deploy continua sem execução nova comprovada do `main`.

## Devolução ao Cobogó

- ConsumerCard de The Lab atualizado para `last_reviewed: 2026-09-01`, mantendo `gap_score: 1` e avançando `last_verified` apenas onde houve revalidação factual.
- Nenhum conceito novo: a solução local de provenance continua evidência de um único consumer sem before/after fechado.
- Nenhum pattern promovido; o gate de ≥2 consumers permanece não satisfeito.
- Nenhuma nova issue de capacidade/sinergia: `workspace#30` já é a autoridade aberta para o ProjectProfile ausente.

## Bloqueios

- `franklinbaldo/the-lab#128` — causa: `Deploy Site` documentado como `disabled_manually`; estado: aberto; condição de retomada: workflow ativo + execução válida do `main` + deploy verde + verificação publicada de `Source snapshot`.
- `franklinbaldo/cobogo#407` — SHA `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; causa: check `build` vermelho; estado: aberta e mergeável no Git; condição de retomada: todos os required checks verdes no mesmo head atual.

## Pressão para a próxima rotação

`franklinbaldo/the-lab` somente se #128 apresentar mudança causal (workflow reabilitado/novo run); sem isso, não repetir o mesmo bloqueio como intervenção e voltar ao desempate normal entre os demais consumers ativos.
