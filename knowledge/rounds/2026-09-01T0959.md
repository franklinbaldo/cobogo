---
type: RoundReport
routine: cobogo
date: 2026-09-01T09:59:03-04:00
focus: franklinbaldo/franklinbaldo.github.io
candidates: [franklinbaldo/franklinbaldo.github.io, franklinbaldo/cobogo, franklinbaldo/the-lab]
outcome: blocked
prs: [franklinbaldo/franklinbaldo.github.io#1640]
merged: []
captures: []
blocked: [franklinbaldo/franklinbaldo.github.io#1640, franklinbaldo/cobogo#407, franklinbaldo/the-lab#128]
---

## Foco e alternativas

A pressão da rodada anterior foi aplicada primeiro. O gap D1 do Franklin blog continua sendo o único gap material já pré-registrado e #1637 permanece aberto. Nesta execução surgiu uma forma causal de remover o bloqueio anterior: transformar `scripts/screenshots.mjs` em evidência de CI, sem depender de checkout local. Cobogó #407 e The Lab #128 continuam bloqueios históricos sem evidência de mudança causal observada nesta rodada.

## Leitor de fora — antes

**D1 — o que a pessoa pode fazer diretamente no site.** A navegação global não oferece entrada para `/audiobooks/`, embora o catálogo exista e esteja publicado. **Gap [fato]:** a capacidade pública continua pouco descobrível para quem entra pelo Header/Footer.

**D2 — o que o projeto faz por trás e por que existe.** A Audiobook Factory continua incorporada e condiciona publicação de feed ao estado do manifest. **Gap:** nenhum material separado de D1.

**D3 — o que a pessoa pode fazer sozinha com os artefatos publicados.** RSS/source e a estrutura reproduzível do repo continuam disponíveis; não há base para anunciar podcast HPMOR ativo. **Gap:** nenhum material separado de D1.

## Gap escolhido e critério

Gap escolhido: **D1 — tornar `/audiobooks/` descobrível pela navegação global sem alterar a prioridade editorial do blog.**

Critério verificável e imutável: **Header e Footer oferecem `Audiobooks`/`Audiolivros` para `/audiobooks/`; a home desktop before/after é capturada pelo mesmo `scripts/screenshots.mjs`; a nova entrada permanece secundária e nenhum feed/podcast desabilitado é anunciado.**

## Capturas

- Before: ainda não materializada; a PR #1640 foi aberta com um primeiro commit que adiciona workflow de evidência visual sem tocar na navegação, justamente para produzir esse before em CI.
- After: pendente de after no `main` pelo mesmo workflow após eventual merge.
- Método: `scripts/screenshots.mjs`, home desktop 1440×900, artifact do GitHub Actions.
- Save Page Now: não verificado.

## Ações

- Releitura das três skills e do RoundReport anterior.
- Confirmada #1637 ainda aberta com o mesmo critério.
- Criada branch `cobogo/audiobooks-nav-evidence` a partir do `main` corrente do consumer.
- Primeiro commit `f6556eea1116939582bb6a36f5df1437d162b1ba` adiciona `.github/workflows/visual-evidence.yml`; ele instala Chromium, constrói o site, sobe `astro preview`, executa `scripts/screenshots.mjs` e publica `/tmp/screenshots/*.png` como artifact, tanto em PR quanto em push para `main`.
- Aberta PR `franklinbaldo/franklinbaldo.github.io#1640` com esse primeiro commit e sem mudança de navegação.
- Na última consulta imediata, o head ainda não expunha runs/statuses; portanto o artifact before não podia ser afirmado nem a segunda etapa aplicada com segurança.
- A tentativa de converter a PR para draft falhou por erro do conector GraphQL (`fullDatabaseId` inexistente); a PR permanece aberta e deve ser tratada como parcial até o gate de evidência existir.

## Fatos observados

- #1637 segue aberta.
- `scripts/screenshots.mjs` captura `/` em desktop 1440×900 e mobile, além de rotas adicionais.
- O repo já usa `actions/upload-artifact` pinado em outros workflows; o novo workflow reutiliza a mesma versão pinada.
- PR #1640: head `f6556eea1116939582bb6a36f5df1437d162b1ba`; 1 arquivo alterado no momento desta rodada; navegação ainda intacta.
- Consulta imediata de workflow/status no head retornou vazio, compatível com run ainda não iniciado/indexado.

## Inferências

- O bloqueio anterior não precisa mais depender de checkout local: CI pode virar a capacidade canônica de captura para a rotina.
- O primeiro run da #1640, por não alterar Header/Footer, é um before causalmente limpo.
- A mudança de navegação só deve entrar depois que esse artifact existir; depois do merge, o push em `main` deve produzir o after pelo mesmo método.

## Resultado do gap

**não fechado / blocked.** Houve avanço causal: a infraestrutura que pode materializar before/after foi adicionada em uma única PR substantiva, mas o before ainda não estava disponível na última consulta e, por isso, a navegação não foi alterada nem mergeada nesta rodada.

## Gates

- `franklinbaldo/franklinbaldo.github.io#1640` — head `f6556eea1116939582bb6a36f5df1437d162b1ba`; estado: aberta; workflow/status ainda não indexado na última consulta; sem merge.
- Condição de retomada: confirmar run verde + artifact `visual-evidence-*` no primeiro head; então adicionar Header/Footer na mesma PR, exigir required checks verdes no novo head, merge protegido pelo SHA, Pages verde e artifact after no merge SHA.
- `franklinbaldo/cobogo#407` — bloqueio histórico não reaberto substantivamente nesta rodada.

## Devolução ao Cobogó

- ConsumerCard permanece factual em `last_reviewed: 2026-09-01`, `gap_score: 1`; não foi reduzido enquanto D1 continua aberto.
- Nenhum conceito/pattern promovido: ainda não existe par before/after fechado neste consumer.
- Nenhuma escrita no ProjectProfile do workspace.

## Bloqueios

- `franklinbaldo/franklinbaldo.github.io#1640` — causa: primeiro run/artifact de evidência ainda não disponível na consulta imediata; estado: PR parcial aberta no head `f6556eea1116939582bb6a36f5df1437d162b1ba`; condição de retomada: artifact before verde e acessível.
- Conversão para draft — tentativa falhou por erro do conector GraphQL; não altera o gate de evidência nem autoriza merge.
- `franklinbaldo/cobogo#407` e `franklinbaldo/the-lab#128` — sem mudança causal observada nesta rodada.

## Pressão para a próxima rotação

**franklinbaldo/franklinbaldo.github.io#1640** — retomar somente se o primeiro run já tiver materializado o before; nesse caso, concluir Header/Footer → checks → merge → deploy → after na mesma PR.
