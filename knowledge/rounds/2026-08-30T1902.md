---
type: RoundReport
routine: cobogo
date: 2026-08-30T19:02:59-04:00
focus: franklinbaldo/causaganha
candidates: [franklinbaldo/causaganha]
outcome: inspection-only
prs: []
merged: []
captures: []
blocked: [franklinbaldo/causaganha#941, franklinbaldo/cobogo#407, ovigialocal/ovigialocal.github.io#8]
---

# Rodada Cobogó — 2026-08-30 19:02 -04

## Foco e alternativas

A regra de urgência decide a rodada antes de maior-gap ou pressão: CausaGanha permanece consumer ativo com deploy público quebrado no SHA atual de `main`. O merge `d8c5102e6de42f8391cdedbf3fa83306b41c8142` continua sendo o head de `main`, e o último `Deploy Web` associado a esse SHA permanece o run `33338217183`, concluído com `failure`, com o job `deploy` pulado. A issue `franklinbaldo/causaganha#941` continua aberta. Como o defeito pertence ao pipeline e a rotina de superfície já abriu a issue dona com evidência e critério de conclusão, não há intervenção de UI admissível antes de o bloqueio cair. Nenhum segundo consumer foi aprofundado.

## Leitor de fora — antes

### D1 — o que a pessoa pode fazer diretamente no site

A última superfície publicamente comprovada é a versão implantada após #901: consultar por CNJ, pesquisar DJEN, explorar cobertura e navegar pelos jobs reais. A home multi-fonte está comprovadamente publicada.

**Gap D1:** nenhum gap material novo observado nesta rodada.

### D2 — o que o projeto faz por trás e por que existe

O repositório prova no `main` atual a nova `/sobre` introduzida por #902: Arquivo / Estado / Teor, fronteiras de prova por fonte, fluxo fonte → artefato → índice → superfície e acesso reproduzível. A captura pós-merge do próprio SHA também existe.

**Gap D2 [fato]:** o leitor público ainda não está comprovado recebendo essa explicação porque o deploy canônico do SHA falhou antes do build. O gap permanece exatamente o registrado no consumer card e em #941.

### D3 — o que a pessoa pode fazer sozinha com os artefatos publicados

Continuam comprovados ZIPs no Internet Archive, `sync-manifest.parquet`, `catalog.sql`, Parquets por fonte conforme disponibilidade e receita DuckDB para materialização/consulta.

**Gap D3:** nenhum gap material novo observado nesta rodada.

## Gap escolhido e critério

**Gap escolhido: D2 — publicação efetiva da `/sobre` já implementada e capturada.**

**Critério verificável:** um SHA de `main` contendo `d8c5102e...` deve concluir `Deploy Web` com build e deploy verdes; a rota pública `/sobre` deve então ser verificável no Pages. Não vale substituir esse critério por captura local/Actions já existente, nem alterar superfície para contornar o pipeline.

## Capturas

### Antes

Nenhuma captura nova. Evidência herdada e ainda válida: `Product Surface Visual Capture` run `33338217168`, artifact `9739713589`, no merge SHA `d8c5102e...`, com `/sobre` desktop/mobile capturada com sucesso.

### Depois

Nenhuma. O deploy público permanece bloqueado, portanto não há nova superfície publicada a recapturar.

## Ações

- skills `brazilian-web-design`, `cobogo-design-review` e `cobogo-consumer-synergy` relidas;
- RoundReports `2026-08-30T1759`, `2026-08-30T1558` e `2026-08-30T1502` relidos;
- lista canônica de consumer cards relida; `knowledge/consumers/causaganha.md` permanece `active`, `last_reviewed: 2026-08-30`, `gap_score: 1`;
- `main` de CausaGanha verificado: continua em `d8c5102e6de42f8391cdedbf3fa83306b41c8142`;
- histórico recente de Actions verificado: não há novo `Deploy Web` posterior ao run falho `33338217183` para o SHA atual;
- `causaganha#941` verificada aberta, com o mesmo escopo e critério de conclusão;
- PRs próprias abertas foram inspecionadas apenas como sinal. Nenhuma substitui a urgência de deploy; nenhuma mudança de superfície foi iniciada.

## Fatos observados

- o SHA atual de `main` permanece `d8c5102e...`;
- `Deploy Web` run `33338217183` permanece `failure`; `deploy` foi `skipped`;
- outros checks do mesmo SHA, inclusive CI/CodeQL e a captura de superfície, permanecem verdes; o defeito continua isolado ao pipeline de deploy já registrado;
- não houve commit descendente em `main` nem retry bem-sucedido que satisfizesse #941;
- o consumer card já descreve corretamente este estado e não mudou factual ou semanticamente.

## Inferências

- abrir/mergear #898 ou outra mudança visual enquanto #941 continua ativo criaria nova superfície não publicável e pioraria o estado causal;
- a ação correta desta rodada é não fabricar trabalho: preservar o critério de evidência, manter a issue no repo dono e reavaliar quando surgir novo deploy/commit relevante;
- não há base para alterar `gap_score`, promover pattern ou abrir nova issue no workspace.

## Resultado do gap

**não fechado.** Nenhum fato novo satisfaz o critério de deploy público. A implementação e a evidência visual de #902 continuam válidas, mas o leitor externo não está comprovado recebendo a nova `/sobre`.

## Gates

- `franklinbaldo/causaganha#941` — aberta; `main@d8c5102e...`; último `Deploy Web` `33338217183=failure`; condição de retomada: build + deploy verdes em SHA contendo #902, seguido de verificação pública de `/sobre`.
- `franklinbaldo/cobogo#407` — bloqueio previamente registrado; nenhum merge nesta rodada.
- `ovigialocal/ovigialocal.github.io#8` — bloqueio previamente registrado; nenhum merge nesta rodada.

## Devolução ao Cobogó

- consumer card do CausaGanha mantido sem edição porque os fatos, `last_reviewed` e `gap_score: 1` permanecem corretos;
- conceito `source-proof-boundaries` permanece abaixo do gate de promoção: um consumer observado e a implantação pública correspondente ainda bloqueada;
- nenhum novo conceito, pattern ou issue de capacidade/sinergia foi criado.

## Bloqueios

- `franklinbaldo/causaganha#941` — causa: `Deploy Web` falha no `Setup Python` antes do build; estado: aberto, sem retry/commit descendente bem-sucedido; condição de retomada: deploy verde e verificação de `/sobre` pública.
- `franklinbaldo/cobogo#407` — condição de retomada conforme RoundReports anteriores.
- `ovigialocal/ovigialocal.github.io#8` — condição de retomada conforme RoundReports anteriores.

## Pressão para a próxima rotação

`franklinbaldo/causaganha#941` — verificar primeiro se surgiu commit/retry com deploy verde; somente depois retomar a revisão visual ampla de #898 ou outro gap de superfície.
