---
type: RoundReport
routine: cobogo
date: 2026-09-02T15:05:00-04:00
focus: franklinbaldo/baliza
candidates: [franklinbaldo/baliza, franklinbaldo/ficha, franklinbaldo/astronauta]
outcome: partial
prs: [franklinbaldo/baliza#693]
merged: [franklinbaldo/baliza#693]
captures: [run:33664587760, run:33670722002]
blocked: [franklinbaldo/baliza#692, franklinbaldo/baliza#694]
---

# Rodada Cobogó — remediação de dados responsivos no Baliza

## Método e seleção do foco

A rodada releu as skills `brazilian-web-design`, `cobogo-design-review` e `cobogo-consumer-synergy`, a governança opinionada atual do Cobogó, os três RoundReports mais recentes, o ranking e o assessment/card do Baliza. O `ProjectProfile` vigente no workspace confirma `franklinbaldo/baliza` como projeto ativo e de alta prioridade; nenhuma capacidade ou sinergia nova foi inferida além dessa fonte factual.

Baliza venceu por follow-up causal próprio: a #693, aberta na rodada anterior para a dívida concreta registrada em #692, agora possuía checks e captura no head exato e podia ser reconciliada sem escolher novo consumer.

## Pré-registro factual

### D1 / clareza

**4/4, sem mudança.** O leitor entende as tarefas e a cadeia PCA → Publicações → Atas → Contratos. A remediação escolhida não altera capability ou copy.

### D2 / explicabilidade

**4/4, sem mudança.** A relação PNCP → preservação → análise continua explícita e sustentada por artefatos públicos.

### D3 / autonomia

**4/4, sem mudança.** Snapshots, manifests, explorador e caminhos de reutilização permanecem disponíveis; `gap_score` continua 0.

### Assessment relevante

O assessment publicado continua **21/24 — boa**: D1 4, D2 4, D3 4, UX 3, UI 2, Confiança 4. A maior dívida continua UI 2 em `/publicacoes` até existir AFTER do landing efetivamente publicado. O ranking permanece inalterado.

### Problema e critério imutável

A #692 já havia congelado o problema: no narrow, tabela e esquema não podiam comprimir informação a ponto de perder separação semântica. O critério permaneceu:

1. esquema sem colisão perceptível nome/tipo/descrição em `390×844`;
2. tabela com estratégia responsiva intencional, sem headers/valores espremidos até a ilegibilidade;
3. desktop legível;
4. captura ligada ao SHA avaliado;
5. para fechamento material, merge → deploy → AFTER no estado publicado.

## Opinião Cobogó aplicada

`visual-evidence-as-quality-gate` permanece **stable** e é a regra causal desta rodada: captura no head permite avaliar a implementação, mas não autoriza inferir o landing publicado quando o deploy falha.

`rendered-evidence-manifest` permanece **opinionated**. O run de PR é causalmente ligado ao head real `97fed00aed829f432e39ac6bb91fcf64037a14e3`, mas o nome do artifact ainda carrega identidade de merge-ref sintético; não houve adoção do manifest compartilhado e nenhuma maturidade foi inflada.

## Follow-up da PR #693

Head exato: `97fed00aed829f432e39ac6bb91fcf64037a14e3`.

No head passaram:

- required check `GitGuardian Security Checks`;
- `Test`;
- `Visual capture — data surfaces`, run `33664587760`.

O job de deploy observado na PR estava `skipped`, não era required check e não representava falha do head. Com os gates aplicáveis verdes e a PR mergeável, #693 foi squash-mergeada.

Landing: `8e324011bdc46a27b5c0ea2826288d4673b9faed`.

A implementação preserva a identidade analítica do Baliza: o esquema reflui em narrow, enquanto a tabela genuinamente tabular conserva scroll horizontal deliberado com largura mínima e região focável.

## Evidência pós-merge e blocker

No landing, `Visual capture — data surfaces` run `33670722002` terminou **success** no SHA exato. Isso prova que o harness ainda consegue renderizar/capturar o landing localmente.

Porém `Deploy to GitHub Pages` run `33670722066` terminou **failure antes do build/deploy**. O gate `npm audit --omit=dev --audit-level=high` encontrou vulnerabilidade `high` em `fast-uri` e abortou o job. Portanto não existe prova de que `8e324011...` tenha sido publicado no Pages; uma captura local de `main` não substitui o AFTER da superfície implantada.

Esse problema é operacional/de dependência e pertence ao repo dono, não à autoridade de implementação desta rotina. Foi aberta `franklinbaldo/baliza#694` com SHA, run, causa e condição objetiva de retomada.

A #692 havia sido fechada automaticamente pelo `Closes` da #693. Foi reaberta porque o contrato visual exige AFTER pós-deploy; código mergeado não equivale a dívida pública resolvida.

## Resultado

**Parcial / blocker objetivo.** A implementação visual passou no head e está em `main`, mas o gap não é considerado fechado porque o deploy do landing falhou. Por isso:

- assessment permanece 21/24;
- UI permanece 2/4 e UX 3/4;
- ranking não muda;
- `gap_score` D1–D3 permanece 0;
- `visual-evidence-as-quality-gate` permanece `stable`;
- `rendered-evidence-manifest` permanece `opinionated`.

A ConsumerCard foi reconciliada com a diferença explícita entre implementação em `main`, captura local pós-merge e estado efetivamente publicado.

## Condição de retomada

Retomar Baliza quando #694 tiver mudança causal: um `main` contendo #693 deve passar `Deploy to GitHub Pages`, ser efetivamente publicado e então `/publicacoes` deve ser recapturado/verificado no estado implantado. Só nessa sequência #692 pode fechar e UI/UX podem ser reavaliadas.

## Pressão seguinte

Se #694 continuar imóvel na próxima rodada, não repetir Baliza. A pressão volta para dívida dimensional elegível em outro consumer, com `franklinbaldo/ficha` (UI 1/4) à frente quando houver follow-up causal fechável; consumers bloqueados apenas por ausência de captura continuam fora até mudança causal.
