---
type: RoundReport
routine: cobogo
date: 2026-09-01T05:02:30-04:00
focus: franklinbaldo/baliza
candidates: [franklinbaldo/baliza, franklinbaldo/the-lab, franklinbaldo/ficha]
outcome: inspection-only
prs: []
merged: []
captures: []
blocked: [franklinbaldo/cobogo#407, franklinbaldo/the-lab#128]
---

## Foco e alternativas

As três skills operacionais e os três RoundReports mais recentes foram relidos antes da seleção. `franklinbaldo/cobogo#407` continua aberta no head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; embora a API agora reporte `mergeable: true`, o check `build` desse mesmo head continua `failure`, portanto não vence por follow-up causal e não pode ser mergeada. `franklinbaldo/the-lab#128` continua aberta com a mesma causa documentada (`Deploy Site` desabilitado manualmente), sem mudança causal que justifique repetir a intervenção.

Baliza foi escolhido pela pressão explícita da rodada anterior e pela antiguidade do ConsumerCard (`last_reviewed: 2026-08-31`, `gap_score: 0`). O foco foi inspeção: verificar se `main`, Pages/sync, README e ProjectProfile continuam sustentando D1/D2/D3 e se surgiu alguma capacidade publicada invisível ao leitor.

## Leitor de fora — antes

**D1 — o que a pessoa pode fazer diretamente no site.** A home permite escolher/continuar uma cidade, monitorar contratações, entrar em publicações, PCA, atas, busca/exploração, status e arquivo; `/comparar` distingue explicitamente referências simuladas de municípios observados. **Gap:** nenhum material observado no estado incorporado.

**D2 — o que o projeto faz por trás e por que existe.** README, ConsumerCard e ProjectProfile convergem na tese de preservar o acervo do PNCP como dado público reproduzível/citável e construir análise sobre a mesma autoridade; o Internet Archive é parte da arquitetura. **Gap de superfície:** nenhum material observado. A dívida do PNCP Doctor permanece operacional e pertence ao repo dono.

**D3 — o que a pessoa pode fazer sozinha com os artefatos publicados.** A superfície/card expõem arquivo de snapshots, manifesto de Parquets com URL/hash, área de desenvolvedores e caminhos de consulta/reuso independentes do frontend. **Gap:** nenhum material observado.

## Gap escolhido e critério

**Nenhum gap fechável pré-registrado.** Critério verificável: se `main` continuar sem mudança de superfície desde #688, se o deploy/PNCP Sync no SHA atual estiver saudável, se README/ProjectProfile não provarem capacidade incorporada nova omitida da superfície e se os PRs abertos forem apenas dependências/orphans sem mudança publicada, terminar `inspection-only`, não abrir PR de UI e preservar `gap_score: 0`. Correções factuais do ConsumerCard são permitidas sem converter manutenção do corpus em gap visual.

## Capturas

- Before novo: nenhum; não houve mudança visual proposta.
- Evidência visual vigente: harness de #671/#674 para `/publicacoes` e `/comparar`, já registrado no ConsumerCard.
- After: não aplicável; nenhuma byte da superfície do consumer foi alterada.
- Save Page Now: nenhum mecanismo novo verificado.

## Ações

- Pré-registro criado em `knowledge/rounds/2026-09-01T0502.md` antes de alterar o ConsumerCard.
- Relidos `brazilian-web-design`, `cobogo-design-review` e `cobogo-consumer-synergy`, mais os RoundReports `T0158`, `T0259` e `T0357`.
- Revalidado `baliza/main` em `3468dc1a0bb8f7b6a4382171192379c65295bb3b`; o último commit continua sendo #688, explicitamente behavior-neutral para a superfície e com 190 testes declarados.
- Revalidado PNCP Sync run `33489816812` = `success` em 2026-09-01 no mesmo SHA.
- README atual confrontado com o ConsumerCard e o ProjectProfile; nenhuma capacidade web já incorporada ficou invisível em D1/D2/D3.
- PRs abertas observadas continuam concentradas em Dependabot e branches órfãs/auxiliares; nenhuma é uma PR própria de superfície pronta para follow-up.
- Revalidado `cobogo#407`: API agora reporta `mergeable: true`, mas `build` continua `failure` no head exato `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; nenhum merge.
- Revalidado `the-lab#128`: aberta, sem mudança causal; Deploy Site continua documentado como `disabled_manually`.
- Corrigido o ConsumerCard de Baliza para `last_reviewed: 2026-09-01`, `gap_score: 0`, `last_verified` atual e PNCP Sync fresco.
- Removida do card a referência fossilizada a `Proveniência/freshness`: `knowledge/patterns/provenance-freshness.md` já havia sido removido do corpus corrente na limpeza de consumers 404, portanto não podia continuar listado em `Padrões do Cobogó em uso`.
- Achado fora da autoridade de superfície: README anuncia Python 3.11+, `pyproject.toml` exige `>=3.12` e `Dockerfile` usa `python:3.11-slim`. Aberta `franklinbaldo/baliza#689` com evidência, escopo e critério de conclusão; nenhuma implementação feita nesta rotina.
- Nenhuma PR substantiva, nenhum conceito novo e nenhum pattern promovido.

## Fatos observados

- `baliza/main` permanece `3468dc1a0bb8f7b6a4382171192379c65295bb3b`, merge da #688 em 2026-08-31; não houve mudança de superfície posterior.
- PNCP Sync `33489816812` concluiu `success` em 2026-09-01 no mesmo SHA.
- O README continua descrevendo as três superfícies do mesmo acervo: site público, pipeline de preservação e CLI; a tese pública segue alinhada ao ProjectProfile e ao ConsumerCard.
- O card continha uma entrada chamada `Proveniência/freshness` sob `Padrões do Cobogó em uso`, enquanto o arquivo compartilhado `knowledge/patterns/provenance-freshness.md` já não existe no corpus corrente por ter perdido o gate de evidência após a remoção de Tambaqui.
- O README anuncia Python 3.11+, `pyproject.toml` declara `requires-python = ">=3.12"` e o Dockerfile parte de `python:3.11-slim`; a inconsistência foi registrada em `baliza#689`.
- `cobogo#407` permanece no mesmo head; o check `build` continua vermelho apesar de `mergeable: true`.
- `the-lab#128` continua aberta com a mesma causa documentada e sem novo deploy.

## Inferências

- Não há regressão de superfície nem capacidade incorporada invisível ao leitor; abrir uma PR de UI seria fabricar atividade.
- O PNCP Sync fresco reforça a evidência operacional da preservação sem transformar um workflow verde em claim visual adicional.
- A referência ao pattern removido era sujeira de corpus, não um novo gap D1/D2/D3; removê-la mantém a memória corrente fail-closed em relação a conhecimento que perdeu evidência viva.
- A divergência Python 3.11/3.12 é contrato de instalação/empacotamento e não deve ser “corrigida” pela rotina de superfície; a issue #689 é o destino correto.
- `mergeable: true` não substitui o gate de checks: #407 continua proibida de merge enquanto o build do head atual estiver vermelho.

## Resultado do gap

**inspection-only.** O critério pré-registrado foi satisfeito por não intervenção de superfície: D1, D2 e D3 permanecem coerentes, `gap_score: 0` continua correto e nenhuma PR visual foi fabricada. Houve somente manutenção factual do corpus e abertura de issue no repo dono para um contrato técnico inconsistente.

## Gates

- Nenhuma PR criada ou mergeada nesta rodada.
- `franklinbaldo/baliza`: `main` `3468dc1a0bb8f7b6a4382171192379c65295bb3b`; PNCP Sync `33489816812` = success; nenhuma mudança de superfície nova.
- `franklinbaldo/cobogo#407`: head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; `mergeable: true`; `build` = failure; `capture` = success; GitGuardian = success; sem merge.

## Devolução ao Cobogó

- `knowledge/consumers/baliza.md` atualizado para `last_reviewed: 2026-09-01`, preservando `gap_score: 0` e registrando PNCP Sync atual.
- Removida a atribuição a um pattern compartilhado que já não existe no corpus corrente.
- Nenhum conceito novo: não houve solução nova de superfície nem evidence pair before/after.
- Nenhuma contagem incrementada e nenhum pattern promovido.
- `baliza#689` registra o único achado técnico fora da autoridade desta rotina; nenhuma escrita foi feita no ProjectProfile.

## Bloqueios

- `franklinbaldo/cobogo#407` — head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; causa: check `build` vermelho; condição de retomada: todos os required checks verdes no head corrente.
- `franklinbaldo/the-lab#128` — causa inalterada: Deploy Site desabilitado manualmente; condição de retomada: workflow ativo + deploy verde + recaptura pública da provenance.

## Pressão para a próxima rotação

**nenhuma.** Aplicar novamente urgência → follow-up causal → maior gap/antiguidade entre os ConsumerCards ativos. The Lab só deve voltar com mudança causal no deploy; #407 só vence por follow-up quando os checks do head atual estiverem verdes.
