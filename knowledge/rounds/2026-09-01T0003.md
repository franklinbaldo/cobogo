---
type: RoundReport
routine: cobogo
date: 2026-09-01T00:03:00-04:00
focus: franklinbaldo/cobogo
candidates: [franklinbaldo/cobogo, franklinbaldo/the-lab, franklinbaldo/sisprev]
outcome: closed
prs: [franklinbaldo/cobogo#414]
merged: [franklinbaldo/cobogo#414]
captures: []
blocked: [franklinbaldo/cobogo#407]
---

## Foco e alternativas

A rodada começou pelo follow-up causal explicitamente deixado pela rodada anterior: `franklinbaldo/cobogo#414`. O head corrente era `0f1267c81543a18f88bd96bf70badfd120dc7b32`; CI e Greenfield já haviam passado, mas Knowledge seguia vermelho porque faltavam specs normativas para `Concept`, `ConsumerCard` e `RoundReport`. Isso decide a rodada pela regra 2 antes de qualquer novo consumer.

The Lab e Sisprev permaneceram alternativas. The Lab continua com urgência histórica ligada ao deploy, mas não houve mudança causal observada antes de o follow-up próprio decidir a rodada; não foi aberto segundo PR substantivo.

As skills `brazilian-web-design`, `cobogo-design-review` e `cobogo-consumer-synergy` foram relidas. A memória recente relevante inclui o RoundReport da #414 (`2026-08-31T2209`) e as duas rodadas imediatamente anteriores de The Lab (`2026-08-31T2058` e `2026-08-31T2000`).

## Leitor de fora — antes

**D1 — o que a pessoa pode fazer diretamente no site.** Nenhum gap visual novo foi pré-registrado. A intervenção de follow-up é semântica/normativa no corpus do próprio Cobogó, não mudança de navegação ou composição da superfície pública.

**D2 — o que o projeto faz por trás e por que existe.** Antes da correção, a #414 já havia reconciliado `ConsumerCard(repo)` no schema relacional, mas o próprio gate de knowledge mostrava que três tipos correntes do corpus não possuíam contrato normativo: `Concept`, `ConsumerCard` e `RoundReport`. **Gap:** o corpus usava tipos vivos sem specs explícitas, deixando membership, promoção e relatórios dependentes de convenção implícita mesmo após a limpeza de repositórios inexistentes.

**D3 — o que a pessoa pode fazer sozinha com os artefatos publicados.** Nenhum gap D3 material novo foi observado; CSS, componentes, documentação e entry points permanecem publicados e essa rodada não altera seu contrato de consumo.

## Gap escolhido e critério

Gap escolhido: **D2 — tornar explícitos e verificáveis os contratos normativos dos três tipos correntes faltantes.**

Critério verificável congelado: **`Concept`, `ConsumerCard` e `RoundReport` devem possuir specs normativas no formato canônico do bundle; o `okf-parser check` e a etapa posterior de evidência semântica devem passar no mesmo head da PR; CI e Greenfield visual preview também devem estar verdes; somente então a #414 pode ser mergeada, seguida de deploy Pages verde no commit de merge.**

## Capturas

Nenhuma captura before/after nova. A mudança é exclusivamente de knowledge/schema e não altera uma rota visual de consumer. Greenfield visual preview foi usado como gate de não-regressão no head da PR, não como evidência de fechamento de gap visual.

- Preview de não-regressão: run `33468183598`, head `36861c12a82da1f4b13021136d2496ce37ef197d`, success.
- Deploy after: run `33468305623`, commit `0ca302f25d9760f0fd223eefd879b31c0202f2d7`, success.
- Save Page Now: nenhum mecanismo utilizado nesta rodada.

## Ações

- Relidas as três skills operacionais e a memória recente da rotina.
- Revalidada #414: head anterior à intervenção `0f1267c81543a18f88bd96bf70badfd120dc7b32`, Knowledge vermelho; CI e preview verdes.
- Lido o formato normativo já existente em `knowledge/.okf/specs/consumer.md` e amostras atuais de `ConsumerCard`, `Concept` e `RoundReport`.
- Criadas, na própria branch da #414, `knowledge/.okf/specs/concept.md`, `consumercard.md` e `roundreport.md`.
- Novo head da #414: `36861c12a82da1f4b13021136d2496ce37ef197d`.
- Gates nesse head: CI `33468183582` = success; Knowledge `33468183576` = success; Greenfield visual preview `33468183598` = success. A etapa `Validate Cobogó knowledge with okf-parser v0.42.8` e a etapa posterior de evidência semântica passaram no mesmo run.
- #414 revalidada como `mergeable: true` no mesmo head e mergeada por squash com `expected_head_sha` fixado; commit de merge `0ca302f25d9760f0fd223eefd879b31c0202f2d7`.
- Deploy to GitHub Pages run `33468305623` concluiu success no commit de merge.
- Consumer card do Cobogó atualizado para `last_reviewed: 2026-09-01`, `gap_score: 0`, registrando #414 e os novos gates.
- #407 revalidada: segue aberta, head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`, `mergeable: false`; nenhuma tentativa adicional nesta rodada.

## Fatos observados

- O formato das specs normativas existentes usa `type: Spec`, `specifies_type` e `status: normative`, seguido de campos e invariantes em Markdown.
- Os `ConsumerCard` atuais usam `repo`, `site`, `status`, `last_reviewed` e `gap_score`.
- Os `Concept` atuais usam `status`, `observed_in` e `provenance`.
- Os `RoundReport` atuais usam `routine`, `date`, `focus`, `candidates`, `outcome`, `prs`, `merged`, `captures` e `blocked`.
- No head `36861c12...`, todos os gates requeridos da #414 ficaram verdes.
- O deploy Pages do merge `0ca302f2...` terminou com sucesso.
- #407 permanece independente e não mergeável.

## Inferências

- A ausência das três specs era exatamente a falha normativa restante da #414; não havia necessidade de alterar o schema relacional novamente.
- A limpeza de repositórios inexistentes revelou um princípio operacional durável: membership corrente, relações tipadas e specs normativas precisam evoluir juntas para que documentação morta não continue exercendo autoridade indireta.
- Como nenhuma composição pública mudou, fabricar screenshots before/after seria evidência falsa; o preview verde serve apenas como não-regressão.

## Resultado do gap

**fechado.** O critério congelado foi satisfeito integralmente: os três tipos possuem specs normativas, Knowledge passou inclusive na etapa de evidência semântica, CI e preview ficaram verdes no mesmo head, #414 foi mergeada com proteção pelo SHA e Pages concluiu success no commit de merge.

## Gates

- `franklinbaldo/cobogo#414` — head `36861c12a82da1f4b13021136d2496ce37ef197d`; CI `33468183582` = success; Knowledge `33468183576` = success; Greenfield visual preview `33468183598` = success; mergeable = true; squash merge `0ca302f25d9760f0fd223eefd879b31c0202f2d7`; Pages `33468305623` = success. Estado final: **merged / deployed**.
- `franklinbaldo/cobogo#407` — head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; segue aberta e `mergeable: false`. Nenhum bypass.

## Devolução ao Cobogó

- `knowledge/consumers/cobogo.md` atualizado com `last_reviewed: 2026-09-01` e `gap_score: 0`.
- Specs normativas de `Concept`, `ConsumerCard` e `RoundReport` agora fazem parte do bundle canônico.
- Nenhum pattern novo foi promovido e nenhuma contagem de conceito foi incrementada: a mudança fecha governança do corpus, não fornece evidência visual multi-consumer.
- Nenhum achado novo de capacidade/sinergia exigiu issue no workspace nesta rodada.

## Bloqueios

- `franklinbaldo/cobogo#407` — SHA `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; estado: aberta e não mergeável; causa conhecida das rodadas anteriores: suíte Vitest legada ainda contém ampla divergência; condição de retomada: correção causal dentro do orçamento e todos os required checks verdes no head corrente, sem enfraquecer o gate.

## Pressão para a próxima rotação

**franklinbaldo/the-lab** — somente se a urgência de deploy continuar factual na próxima discovery e houver mudança causal desde o último bloqueio; caso contrário, aplicar a regra de não fabricar relatório redundante e escolher pelo maior gap atual entre os demais consumers.
