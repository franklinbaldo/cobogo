---
type: RoundReport
routine: cobogo
date: 2026-08-31T18:59:27-04:00
focus: franklinbaldo/undogmatic
candidates: [franklinbaldo/undogmatic, franklinbaldo/funes-memories, franklinbaldo/cobogo]
outcome: blocked
prs: []
merged: []
captures: []
blocked: [franklinbaldo/undogmatic#20, franklinbaldo/cobogo#407]
---

## Foco e alternativas

Não houve urgência comprovada de Pages/deploy quebrado nem superfície publicada sustentando fato removido. O follow-up próprio `franklinbaldo/cobogo#407` continua aberto no head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; não há novo estado verde/mergeável que autorize merge e a tentativa causal da rodada anterior já demonstrou a dívida ampla da suíte.

Pela regra de maior gap/nunca avaliado, `franklinbaldo/undogmatic` foi escolhido: seu card ainda estava no schema legado, com `last_verified: 2026-08-16`, e o `ProjectProfile` continua marcando o projeto como `active-research`. Há atividade recente de governança na issue #20 (2026-08-17), portanto a exclusão automática por >60 dias sem sinal de retomada não foi aplicada. `funes-memories` também permanece em card legado, mas não registra unmet need e já possui browser proof controlado para seu boundary editorial. Cobogó não tem gap D1/D2/D3 registrado e a #407 é dívida de self-conformance, não gap de leitor.

## Leitor de fora — antes

### D1 — o que a pessoa pode fazer diretamente no site

Não há deploy público verificado da UI humana. O repositório contém `apps/shame_heatmap/`, um app Gradio local com texto, idioma, pesos α/δ, clipping e heatmap token-level.

**Gap D1:** nenhum gap público verificável. A ausência de deploy é estado de distribuição/capacidade, não capacidade provada escondida por uma superfície publicada.

### D2 — o que o projeto faz por trás e por que existe

O README raiz declara explicitamente a direção atual LLM-first e `undogmatic.llm_scorer` como fonte de verdade do ShameScore, com score, confidence e rationale. A UI local implementa outro mecanismo: `xlm-roberta-base`, bancos vetoriais placeholder, pesos α/δ e similaridade token-level. Na própria UI, a apresentação ainda é “ShameFeeling — Token Heatmap” e a explicação visível diz apenas que a intensidade codifica similaridade à “shame direction”.

**Gap D2:** material. O repo prova que o heatmap não é o scorer canônico, mas a própria superfície local não torna esse boundary perceptível. Isso permite a leitura equivocada `token similarity heatmap == current ShameScore` já registrada em `undogmatic#20`.

### D3 — o que a pessoa pode fazer sozinha com os artefatos publicados

O repositório fornece código, dataset semente/controle, geração A/B, relatórios Markdown/CSV e comandos reproduzíveis para executar o caminho LLM-first. O README também liga a documentação de visualização de direções como material complementar.

**Gap D3:** nenhum material adicional observado. Não há API/release externa que o repo prove e a superfície esconda.

## Gap escolhido e critério

Gap escolhido: **D2 — distinguir na própria UI o diagnóstico vetorial experimental do ShameScore canônico LLM-first**.

Critério verificável, pré-registrado antes de qualquer intervenção no consumer: depois da mudança, a superfície Gradio informa explicitamente, sem depender do README raiz, que o heatmap é um diagnóstico vetorial experimental baseado em `xlm-roberta-base`/banks e **não** o ShameScore canônico LLM-first; a captura after mostra esse aviso no mesmo estado/viewport da captura before.

## Capturas

- Before: **indisponível**. Não foi localizada capacidade de captura visual reproduzível para a UI Gradio local no repo; não há deploy público verificável a capturar.
- After: não produzido, porque nenhuma mudança visualmente material foi iniciada sem o before obrigatório.
- URL original: não há rota pública confirmada; execução documentada é local via `python apps/shame_heatmap/app.py`.
- URL arquivada: nenhuma; Save Page Now não se aplica sem rota publicada.
- Commit observado do consumer: `f252a93e5ee4f64cfa1a53fc49f4e9ddb0864467`.

## Ações

- Lidas as skills `brazilian-web-design`, `cobogo-design-review` e `cobogo-consumer-synergy`.
- Reavaliados README raiz, `apps/shame_heatmap/README.md`, `apps/shame_heatmap/app.py`, `ProjectProfile` e issue `undogmatic#20`.
- Atualizado `knowledge/consumers/undogmatic.md` para o schema `ConsumerCard`, `last_reviewed: 2026-08-31`, `gap_score: 1`.
- Nenhuma PR foi aberta no consumer: a captura before obrigatória não existe e iniciar a alteração produziria um loop visual impossível de fechar nesta rodada.
- `franklinbaldo/cobogo#407` permaneceu bloqueada; nenhum gate foi contornado.

## Fatos observados

- O README raiz afirma que `undogmatic.llm_scorer` é a fonte de verdade do ShameScore.
- `apps/shame_heatmap/app.py` calcula direção vetorial a partir de quatro bancos, `xlm-roberta-base` e pesos α/δ e colore tokens por similaridade.
- `apps/shame_heatmap/README.md` afirma que os bancos versionados são placeholders.
- A UI visível não contém aviso de que o mecanismo é experimental/não-canônico em relação ao ShameScore LLM-first.
- `undogmatic#20` continua aberta e define exatamente essa divergência de boundary.
- Não foi localizado deploy público nem capacidade canônica de screenshot da UI local.

## Inferências

- O defeito é de informação/semântica de superfície, não de aparência: o leitor que executa a UI recebe uma explicação incompleta sobre o estatuto do mecanismo.
- A correção mínima provável é copy/inscrição de boundary na própria UI, preservando a identidade local e sem redesenhar a ferramenta.
- Como a correção muda conteúdo renderizado, a regra da rotina exige captura before/after; sem before reproduzível, implementar agora violaria o gate de evidência.
- A falta de captura é dívida de infraestrutura visual, não justificativa para declarar o gap fechado por leitura de código.

## Resultado do gap

**blocked.** O gap D2 foi identificado e o critério ficou congelado, mas nenhuma mudança foi iniciada porque a capacidade de captura necessária para evidência before/after não está disponível para a superfície local. O gap permanece `gap_score: 1`.

## Gates

- `franklinbaldo/undogmatic`: nenhuma PR nesta rodada; portanto nenhum head/check a promover.
- `franklinbaldo/cobogo#407`: head observado `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; continua aberto sem novo estado verde/mergeável. Sem merge.

## Devolução ao Cobogó

- Card `knowledge/consumers/undogmatic.md` migrado para `ConsumerCard` e atualizado com D1/D2/D3, capacidades de superfície e histórico.
- Nenhum conceito/pattern promovido: existe um único consumer e nenhuma captura before/after desta solução.
- Nenhum achado novo de capacidade/sinergia exigiu alteração do `ProjectProfile`; a divergência factual e a decisão de produto já estão registradas em `undogmatic#20` e no profile vigente.

## Bloqueios

- Consumer SHA: `f252a93e5ee4f64cfa1a53fc49f4e9ddb0864467`.
- Causa: capacidade de captura visual indisponível para a UI Gradio local; não há rota pública confirmada.
- Estado: gap D2 identificado, critério congelado, consumer intocado.
- Condição de retomada: existir captura before reproduzível da UI atual (local ou deployment controlado) identificada por SHA/viewport/estado; então aplicar somente o boundary copy da #20, rodar gates, mergear e produzir a mesma captura after.
- Cobogó #407: dívida de suíte já classificada em rodada anterior; retomar apenas com nova evidência/escopo próprio para reconciliar as falhas, sem enfraquecer o gate.

## Pressão para a próxima rotação

`franklinbaldo/funes-memories` — card ainda está no schema legado e é o próximo consumer ativo não migrado; a próxima rodada deve primeiro verificar se houve fato novo desde o browser proof de 2026-08-18 e, se não houver gap, apenas reconciliar o card sem fabricar intervenção.
