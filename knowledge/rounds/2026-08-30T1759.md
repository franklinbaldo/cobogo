---
type: RoundReport
routine: cobogo
date: 2026-08-30T17:59:18-04:00
focus: franklinbaldo/causaganha
candidates: [franklinbaldo/causaganha, ovigialocal/ovigialocal.github.io]
outcome: blocked
prs: [franklinbaldo/causaganha#902]
merged: [franklinbaldo/causaganha#902]
captures: [github-actions:33338105717/artifact:9739678514, github-actions:33338217168/artifact:9739713589]
blocked: [franklinbaldo/causaganha#941, franklinbaldo/cobogo#407, ovigialocal/ovigialocal.github.io#8]
---

# Rodada Cobogó — 2026-08-30 17:59 -04

## Foco e alternativas

Não há urgência de Pages/deploy conhecida. O follow-up causal anterior #901 já está mergeado em `1e608dcbd7f8c5a3d4c25bd323d42df5bab66262`, e o merge SHA agora tem deploy verde. O próximo PR próprio da pilha, #902, é o primeiro follow-up substantivo aberto e portanto decide o foco. Seu head atual `46f8c91da228fe93ae92c3387c97676c47ce2bc8` está não mergeável após o squash de #901; a rodada admite uma única tentativa de reconciliação, preservando apenas o delta factual de #902 sobre o antigo head de #901. O Vigia permanece alternativa com `gap_score: 0` na última reconciliação conhecida.

`franklinbaldo/cobogo#407` e `ovigialocal/ovigialocal.github.io#8` permanecem bloqueios já registrados e não receberão PR substantivo nesta rodada.

## Leitor de fora — antes

### D1 — o que a pessoa pode fazer diretamente no site

A home publicada já apresenta CausaGanha como produto multi-fonte e mantém as rotas orientadas a tarefa incorporadas pela #900. O leitor pode consultar por CNJ, buscar publicações, explorar cobertura e chegar à explicação/reuso.

**Gap D1 [fato]:** nenhum gap material escolhido na home nesta rodada. A mudança de #901 está em `main` e o deploy do merge SHA foi observado verde; a recaptura pós-merge daquela rodada continua uma dívida de evidência, não um motivo para reabrir a arquitetura da home.

### D2 — o que o projeto faz por trás e por que existe

O `ProjectProfile` define CausaGanha como camada cívica verificável sobre rastros judiciais públicos, separando arquivo, estado, teor, significado, proveniência, freshness e limites de cobertura. `/sobre` contém fatos corretos sobre fontes, dados públicos e governança, mas sua sequência ainda é institucional: projeto → metodologia → fontes → dados → licença/governança.

**Gap D2 [fato] — `/sobre`:** o leitor precisa reconstruir sozinho o modelo mental que o repo já usa. A superfície não ensina primeiro `Arquivo / Estado / Teor`, nem explicita cedo o que cada fonte sustenta e o que não sustenta. #902 contém exatamente um commit de delta sobre o antigo head de #901 e modifica apenas `web/src/pages/sobre.astro` para fechar esse gap.

### D3 — o que a pessoa pode fazer sozinha com os artefatos publicados

O repo prova ZIPs no Internet Archive, `sync-manifest.parquet`, `catalog.sql`, `indice_processual.parquet` e Parquets por fonte conforme disponibilidade, além de receita DuckDB reproduzível e MCP read-only como outra interface do mesmo sistema.

**Gap D3 [fato]:** nenhum gap material contabilizado nesta dimensão. A rodada deve melhorar a explicação e descoberta desses artefatos sem prometer cobertura uniforme ou criar novo artefato.

## Gap escolhido e critério

**Gap escolhido: D2 — transformar `/sobre` em explicação progressiva do sistema factual.**

**Critério pré-registrado e congelado:** depois da mudança, a mesma rota `/sobre` deve, em captura desktop e mobile, (1) abrir pela tese do produto antes da governança institucional, (2) ensinar `Arquivo / Estado / Teor` como modelo mental visível, (3) tornar recuperável para cada fonte o que ela sustenta e o que não sustenta, (4) manter um caminho reproduzível para os artefatos públicos e a receita DuckDB, e (5) preservar a identidade verde/dourada, leitura confortável e navegação por teclado/semântica existente. Não pode afirmar cobertura completa, equiparar movimento DataJud a teor de decisão nem esconder limites de fonte. O head final precisa estar mergeável e com todos os required checks verdes; após merge, deploy/Pages e recaptura da mesma rota devem ser verificáveis.

## Capturas

### Antes

- método: `Product Surface Visual Capture`, Chromium, fixtures idênticas para base/head;
- run PR: `33338105717`, artifact `9739678514`, digest `sha256:1c278e0e20826da93122c522cc264c0e1743b507b55e6b8760d3450ba70d4958`;
- base efetiva: `main@709d438a67fa1980189fcc138ab0a8379d55cf1b`; a diferença desse SHA para #901 é documental/RFC, sem alteração de superfície;
- `/sobre` desktop e mobile preservados como `before`; home desktop/mobile e `/stats` desktop também capturados como controle.

### Depois

- head pré-merge `2dba1070c35c8ea351643692a10fe57df9cc0151`: mesmo run/artifact `33338105717/9739678514`; `/sobre` desktop/mobile diferentes, home e `/stats` pixel-idênticos;
- merge `d8c5102e6de42f8391cdedbf3fa83306b41c8142`: recaptura automática por `push` no run `33338217168`, artifact `9739713589`, digest `sha256:fa5d9f3b3b8dab2bfaada171d1a447e09f610998608f03711537a49f3e54d670`;
- a recaptura pós-merge construiu e fotografou o mesmo SHA com sucesso, mas não substitui o gate de deploy público.

## Ações

- skills `brazilian-web-design`, `cobogo-design-review` e `cobogo-consumer-synergy` relidas;
- RoundReports `T1558`, `T1428` e `T1407` relidos;
- consumer card do CausaGanha e `ProjectProfile` do workspace relidos;
- merge/deploy de #901 reconciliado como fato: `1e608dcb...` possui deploy verde;
- #902 reconstruída mecanicamente sobre o `main` pós-#901, preservando somente seu delta de `/sobre` e incorporando o harness visual multi-superfície com trigger pós-merge;
- novo head `2dba1070...` ficou mergeável e com `lint`, `web`, `tests (tjro)`, CodeQL e `compare-product-surfaces` verdes;
- captura pré-merge comparou base/head: somente `/sobre` mudou;
- #902 mergeada por squash em `d8c5102e...`;
- recaptura pós-merge `33338217168` verde e artifact `9739713589` publicado;
- `Deploy Web` pós-merge falhou no passo `Setup Python` antes do build; deploy pulado;
- issue `franklinbaldo/causaganha#941` aberta com evidência, escopo e critério de conclusão; nenhuma correção de pipeline implementada por esta rotina;
- PR #903 fechada como superseded: sua capacidade de captura foi absorvida e ampliada em #902; #898 permanece aberta porque tem escopo de revisão visual mais amplo.

## Fatos observados

- #901 está mergeada e seu merge SHA possui `deploy=success`;
- #902 tinha ancestry de stack quebrada por squash, mas seu delta próprio era um commit limitado a `web/src/pages/sobre.astro`;
- a reconstrução resultou em PR com dois arquivos: `/sobre` e o workflow de evidência visual;
- o before/after mostra home e `/stats` idênticos e mudança apenas em `/sobre`;
- a nova `/sobre` abre pela tese do produto, ensina Arquivo / Estado / Teor, lista para cada fonte “o que sustenta” e “limite”, preserva a receita DuckDB e explicita limites de cobertura;
- o merge SHA tem captura reproduzível verde, mas o workflow canônico de deploy falhou em `actions/setup-python` antes do build;
- o ProjectProfile permanece autoridade factual para tese/capacidades; esta rotina não o editou.

## Inferências

- a mudança visual/explicativa satisfaz o critério congelado em ambiente construído do próprio merge SHA;
- como o deploy público não concluiu, não é válido afirmar que o leitor externo já recebe a nova `/sobre`;
- `source-proof-boundaries` é um conceito reutilizável, mas ainda não um pattern: há somente um consumer observado e seu deploy público está bloqueado.

## Resultado do gap

**parcial / não fechado publicamente.** A evidência before/after e a recaptura do merge SHA satisfazem o critério de composição, mas o gate final exige deploy/Pages verificável. `Deploy Web` run `33338217183` falhou antes do build; portanto a superfície pública não foi comprovada no novo SHA.

## Gates

- #902 — head final `2dba1070c35c8ea351643692a10fe57df9cc0151`; mergeable; todos os checks observados no head concluíram verdes; merge `d8c5102e6de42f8391cdedbf3fa83306b41c8142`.
- recaptura pós-merge — `compare-product-surfaces` run `33338217168`: success; artifact `9739713589`.
- deploy pós-merge — `Deploy Web` run `33338217183`: `build=failure` em `Setup Python`; `deploy=skipped`.
- `franklinbaldo/cobogo#407` — bloqueio já conhecido; nenhum merge.

## Devolução ao Cobogó

- consumer card atualizado, `gap_score: 1`: D1 fechado pela #901; D2 permanece material apenas porque #902 ainda não foi implantada; D3 sem gap material;
- conceito `knowledge/concepts/source-proof-boundaries.md` registrado com proveniência do merge e da captura pós-merge;
- nenhuma promoção: apenas um consumer observado e deploy ainda bloqueado;
- nenhuma edição no `ProjectProfile` do workspace.

## Bloqueios

- `franklinbaldo/causaganha#941` — `main@d8c5102e...`; `Deploy Web` falha em `Setup Python` antes do build; estado: aberto; condição de retomada: build Astro e deploy Pages verdes no SHA atual/descendente contendo #902, seguidos de verificação da rota pública.
- `franklinbaldo/cobogo#407` — gate Vitest/CI vermelho; condição de retomada já registrada.
- `ovigialocal/ovigialocal.github.io#8` — ancestry/conflito; condição de retomada já registrada.

## Pressão para a próxima rotação

`franklinbaldo/causaganha` — fechar causalmente `#941`, verificar Pages e a `/sobre` pública; só depois retomar a revisão visual mais ampla de `#898`.
