---
type: RoundReport
routine: cobogo
date: 2026-08-31T20:00:00-04:00
focus: franklinbaldo/the-lab
candidates: [franklinbaldo/the-lab, franklinbaldo/sisprev, franklinbaldo/causaganha]
outcome: blocked
prs: []
merged: []
captures: []
blocked: [franklinbaldo/the-lab#128, franklinbaldo/cobogo#407]
---

## Foco e alternativas

A rotação foi reconstruída com o gate fail-closed: card/relatório histórico não prova membership; o repositório precisa continuar relacionado pelo corpus atual e resolver ao vivo no GitHub. Os cards removidos na correção imediatamente anterior não participaram da seleção.

`franklinbaldo/the-lab` ganhou por urgência: é um consumer ainda presente no corpus, o repositório público resolve e teve push em 2026-08-14, mas a issue `franklinbaldo/the-lab#128` confirma que `Deploy Site` está `disabled_manually`. A superfície Pages ainda responde, porém pode divergir de `main`. `franklinbaldo/sisprev` permanece candidato com evidência renderizada anterior, mas sem urgência equivalente observada. `franklinbaldo/causaganha` mantém dívida de prova operacional do reuso público, não uma quebra de Pages nem claim público falso já demonstrado nesta passagem.

A pressão do RoundReport anterior para Funes Memories foi descartada antes da seleção: Funes não pertence mais ao corpus ativo após a correção de membership.

## Leitor de fora — antes

**D1 — o que pode fazer no site.** A home publicada permite navegar por Articles, Lab, Team e About, ler o latest dispatch e entrar no arquivo de pesquisa. **Gap:** nenhum D1 material demonstrado nesta pré-leitura.

**D2 — o que o projeto faz por trás e por que existe.** A superfície publicada explica o Rosencrantz Substrate Invariance Lab e pergunta se um agente pode detectar que vive em um mundo gerado; o repositório documenta um site Astro alimentado por conteúdo sincronizado de `franklinbaldo/rosencrantz-coin`. O `main` já contém no footer a inscrição condicional `Source snapshot:` com repositório, SHA e data do snapshot upstream. **Gap:** a home atualmente publicada não mostra essa inscrição, porque a mudança de proveniência está em `main` mas o deploy correspondente não foi executado. Assim, o leitor vê o laboratório e seus textos, mas não consegue identificar na própria superfície qual snapshot upstream materializou aquele estado.

**D3 — o que pode fazer por conta própria.** O leitor pode seguir GitHub, Papers e Lab State e ler o arquivo público independentemente da maquinaria de sincronização. **Gap:** nenhum artefato reutilizável adicional foi provado pelo repo e ocultado pela superfície nesta pré-leitura.

O workspace não contém `knowledge/projects/the-lab.md` no inventário atual. A ausência do `ProjectProfile` será devolvida ao workspace; nesta rodada, afirmações D2 ficam limitadas ao que o próprio repo e a superfície provam, sem inventar capacidades/sinergias.

## Gap escolhido e critério

Gap escolhido: D2 — provenance do snapshot upstream está implementada em `main`, mas ainda não é perceptível na superfície publicada.

Critério verificável e imutável: **depois do fechamento, a home pública deve renderizar no footer `Source snapshot: franklinbaldo/rosencrantz-coin@<short-sha>` acompanhado da data do snapshot; o deploy deve corresponder ao `main` que contém essa implementação e uma captura da home deve mostrar a inscrição.**

## Capturas

- **Before textual público** — `https://franklinbaldo.github.io/the-lab/`, home/footer, observado em 2026-08-31T20:00:00-04:00. A superfície responde e o footer termina em GitHub · Papers · Lab State, sem `Source snapshot`.
- **Before visual novo** — nenhum. A busca no repo não localizou uma capacidade canônica/reproduzível de screenshot para o estado atual; a evidência browser registrada no card antigo é histórica e não foi promovida artificialmente a captura desta rodada.
- **After** — nenhum. O deploy necessário para materializar o `main` está indisponível; sem deploy e sem captura after, o loop não pode ser declarado fechado.
- **Save Page Now** — nenhum mecanismo disponível/verificado nesta rodada.

## Ações

- `franklinbaldo/the-lab#128` — blocker existente do deploy, confirmado ainda aberto; não duplicado e não implementado porque é dívida operacional do repo dono.
- `franklinbaldo/workspace#30` — aberto para criar/reconciliar o `ProjectProfile` ausente de The Lab e restaurar autoridade factual de tese/estado/capacidades/sinergias.
- `knowledge/consumers/the-lab.md` — migrado para `ConsumerCard`, `status: active`, `last_reviewed: 2026-08-31`, `gap_score: 1`.
- `franklinbaldo/cobogo#407` — follow-up próprio reconfirmado no head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; CI run `33433669418` continua `failure`. Nenhum gate foi contornado.
- Nenhum PR de superfície foi aberto: a solução local do gap já está em `main`; duplicá-la não teria causalidade e o deploy é o blocker real.

## Fatos observados

- `franklinbaldo/the-lab` resolve no GitHub, não está arquivado e teve push em 2026-08-14T20:07:30Z.
- A superfície Pages responde e o footer publicado observado termina em GitHub · Papers · Lab State, sem `Source snapshot`.
- `site/src/layouts/Layout.astro` em `main` já renderiza `Source snapshot` quando `content/provenance.json` existe.
- `.github/workflows/deploy.yml` possui `workflow_dispatch` e push em `main`, com build `npm ci` + `npm run build` e `actions/deploy-pages@v4`.
- `the-lab#128` permanece aberta e registra `Deploy Site` como `disabled_manually`.
- A busca no workspace não localizou `knowledge/projects/the-lab.md`; `workspace#30` passou a rastrear essa ausência.
- A busca no consumer não localizou capacidade atual canônica de screenshot/capture reproduzível para a home.
- `cobogo#407` permanece não mergeável no head atual e seu CI atual continua vermelho.

## Inferências

- O gap não exige nova solução visual: a solução local já existe no código e preserva a identidade do consumer.
- Reescrever ou duplicar a solução seria atividade sem causalidade; o fechamento depende primeiro de restabelecer o caminho de deploy já rastreado no repo dono.
- Pages responder não prova que Pages corresponde ao `main` atual; este consumer volta a fornecer evidência negativa útil para separar disponibilidade, deploy e provenance.
- A ausência de `ProjectProfile` impede esta rotina de transformar leitura do repo em inventário institucional de capacidades ou sinergias; por isso o achado foi devolvido ao workspace, não incorporado como nova autoridade no card.

## Resultado do gap

**Não fechado — bloqueado.** O critério pré-registrado permanece intacto. `Source snapshot` continua ausente da superfície pública observada; não há deploy verde do `main` que contém a implementação e não existe captura after. A rodada não declara sucesso parcial por mera presença do código no repositório.

## Gates

- `franklinbaldo/cobogo#407` — head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; PR aberta, não mergeável; CI run `33433669418`: `failure`; Auto-merge Jules run `33433669641`: `skipped`. Estado final: bloqueada, sem merge.
- The Lab — nenhum PR de superfície na rodada. Gate de publicação indisponível porque `Deploy Site` segue `disabled_manually`; nenhum merge/deploy/capture foi fabricado para satisfazer o checklist.

## Devolução ao Cobogó

- Card `knowledge/consumers/the-lab.md` migrado para o schema atual com `last_reviewed: 2026-08-31` e `gap_score: 1`.
- A solução local `publicação gerada -> Source snapshot upstream exato` continua registrada como algo que The Lab faz bem, mas **não** foi promovida: não há before/after atual nem segundo consumer convergente no gate desta rodada.
- Nenhum conceito novo foi criado e nenhuma contagem foi inflada a partir de evidência histórica.
- `workspace#30` recebe o achado de autoridade factual/capacidade, respeitando a fronteira entre workspace e Cobogó.

## Bloqueios

- `franklinbaldo/the-lab#128` — causa: `Deploy Site` desabilitado manualmente. Estado: `main` contém a provenance; publicação observada não a materializa. Condição de retomada: workflow `Deploy Site` ativo + build/deploy verde do `main` + captura pública do footer com `Source snapshot`.
- Captura atual da home — causa: nenhuma capacidade canônica/reproduzível localizada no repo para gerar a evidência visual nova exigida pelo fechamento. Condição de retomada: usar uma capacidade de captura existente/reestabelecida e registrar SHA/viewport/rota; não reutilizar screenshot histórico como se fosse atual.
- `franklinbaldo/cobogo#407` — causa: CI vermelho no head atual e dívida ampla da suíte já medida em rodada anterior. Estado: aberta/não mergeável. Condição de retomada: reconciliar as falhas por contrato atual vs expectativa fossilizada vs regressão real sem enfraquecer o gate.

## Pressão para a próxima rotação

**nenhuma.** The Lab continuará ganhando por urgência enquanto `#128` mantiver o deploy indisponível; qualquer RoundReport futuro deve revalidar esse fato antes de repetir o bloqueio. Se a urgência desaparecer, a seleção volta ao maior gap/consumer ativo mais antigo, sem ressuscitar cards removidos.
