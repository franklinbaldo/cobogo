---
type: RoundReport
routine: cobogo
date: 2026-09-04T03:57:10-04:00
focus: franklinbaldo/quem-sao-eles
mode: REMEDIAÇÃO
status: in-progress
---

# Rodada — Quem São Eles?

## O que estava errado ou faltando

O projeto está em 23 de 24 pontos na avaliação de qualidade vigente: Clareza 4/4, Explicabilidade 4/4, Autonomia 4/4, UX 4/4, UI 3/4 e Confiança 4/4. A dimensão visual ainda não é referência. Ao reler a implementação atual, encontrei um defeito concreto no rodapé compartilhado: `Footer.astro` usa `--gray-gradient`, `--gray` e `--gray-dark`, mas as páginas atuais não carregam o CSS legado que define essas variáveis. Assim, cor e fundo do rodapé dependem de valores CSS inexistentes na própria superfície.

A issue #13 também está desatualizada: ela ainda descreve o URL incorreto `/quem-sao-elesdata/...`, mas `main` já normaliza `BASE_URL` com barra final desde a #12. Isso será reconciliado como manutenção mínima, sem consumir o orçamento de implementação.

## O que será feito

A correção escolhida é tornar o rodapé visualmente autossuficiente, usando valores locais coerentes com a identidade jornalística já presente nas páginas, sem reintroduzir o CSS do template Astro nem impor aparência do Cobogó. A mudança deve preservar texto, links e hierarquia atuais.

## Critério verificável que não muda durante a rodada

A mudança só será considerada concluída se:

1. o rodapé não depender de variáveis CSS ausentes;
2. `npm run build` e os checks exigidos pelo repositório passarem no commit atual da branch;
3. a PR for incorporada;
4. a observação visual existente rodar novamente no commit que realmente entrou em `main`;
5. nenhuma regressão de largura estreita aparecer na rota `/pep`.

## Opinião Cobogó aplicável

Aplica-se `visual-evidence-as-quality-gate`, atualmente `stable`: em linguagem comum, é uma recomendação que já funcionou de forma convergente em pelo menos dois projetos reais e exige que uma mudança visual seja julgada no navegador, não apenas pelo CSS ou pelo build.

## O que já está comprovado

O `ProjectProfile` do workspace confirma que `franklinbaldo/quem-sao-eles` está ativo e que a falha de base path da #13 foi corrigida dentro da #12. O `main` atual está em `f9d662964abdecfad1b329f3a65856b47cfcc32b`; a última observação visual da busca PEP nesse commit terminou verde e inclui desktop e celular.

## O que ainda falta

Implementar a correção do rodapé, abrir e levar a PR até o fim, repetir a observação em `main`, reconciliar a avaliação/ConsumerCard/ranking apenas se os fatos justificarem mudança e fechar a #13 como já resolvida.

## Por que a próxima rodada deve ou não voltar ao mesmo projeto

Ainda não é possível decidir. Esta rodada só estará fechada depois da implementação e da nova observação do estado incorporado em `main`.