---
type: RoundReport
routine: cobogo
date: 2026-08-31T11:08:00-04:00
focus: franklinbaldo/intuit
candidates: [franklinbaldo/intuit, franklinbaldo/cobogo]
outcome: blocked
prs: []
merged: []
captures: []
blocked: [franklinbaldo/intuit#14, franklinbaldo/cobogo#407]
---

## Foco e alternativas

As três skills operacionais e os RoundReports `2026-08-31T1037`, `T0837` e `T0701` foram relidos. Não apareceu urgência nova. O único follow-up próprio conhecido no Cobogó continua sendo `franklinbaldo/cobogo#407`, head `32b3897335df837e0b49035ecce77d4d59fa3738`, ainda `mergeable: false`; portanto não há head verde/mergeável que possa decidir a rodada por follow-up causal.

Pela regra de maior gap, `franklinbaldo/intuit` decide o foco: card ativo, `gap_score: 1`, com residual D1 explicitamente restrito a foco visual de teclado e `prefers-reduced-motion`. Cobogó permanece com `gap_score: 0` e #407 bloqueada.

## Leitor de fora — antes

### D1 — o que a pessoa pode fazer diretamente

A pessoa pode editar/colar HTML, renderizar o preview, copiar link, atualizar a URL, limpar o editor, alternar scripts no sandbox e alternar tema. Preview, erros e grupos operacionais já têm semântica acessível após #17/#24.

Gap D1: os controles principais têm affordance visual de hover/transform, mas o repo ainda não prova foco visual explícito equivalente para navegação por teclado; as transições/escala atuais também não têm tratamento explícito para `prefers-reduced-motion`. Esse residual está factual e diretamente registrado em `intuit#14`.

### D2 — o que o projeto faz por trás e por que existe

O `ProjectProfile` continua definindo Intuit como renderer HTML/visual-testing client-side cuja relação principal é fonte/edit → preview renderizado. O default permanece `sandbox=""`, scripts são opt-in e o runtime usa `srcdoc`; estado pode ser transportado por `?data=`, `?b64=` e Gist.

Gap D2: nenhum material estabelecido. README, ProjectProfile, card e estado recente do repo continuam coerentes quanto à finalidade e à fronteira de segurança.

### D3 — o que a pessoa pode fazer sozinha com os artefatos publicados

A pessoa pode transportar/reproduzir estado por URL/Base64/Gist e executar o source checkout como aplicação estática. Não há release/dataset/API autônoma adicional cuja existência esteja escondida pela superfície.

Gap D3: nenhum material estabelecido.

## Gap escolhido e critério

Gap escolhido: D1 — foco visual verificável por teclado + respeito a `prefers-reduced-motion`, sem alterar a identidade Apple-inspired local.

Critério verificável congelado: depois da mudança, os controles principais terão foco visual explícito observável por teclado; com `prefers-reduced-motion: reduce`, as transições/hover scaling relevantes serão removidas ou reduzidas; Playwright provará os dois comportamentos; o default continuará `sandbox=""` com render por `srcdoc`. A mesma rota deverá possuir captura before/after nas mesmas condições de viewport. Não alterar este critério após implementação.

## Capturas

Antes: **indisponível**. O consumer card ainda registra `Captura visual versionada — ausente`, e o repo atual contém Playwright/gate mas nenhuma capacidade registrada que produza e preserve screenshot reproduzível da rota pública/home com commit SHA. CI e Pages verdes não são pixel-proof e não substituem a captura exigida pela rotina.

Rota pretendida: `https://franklinbaldo.github.io/intuit/` (`/intuit/`).

Depois: nenhum, porque a falta da captura before bloqueou qualquer mudança visualmente material antes de tocar em código.

Save Page Now: nenhum.

## Ações

- Skills `brazilian-web-design`, `cobogo-design-review` e `cobogo-consumer-synergy` relidas.
- RoundReports `2026-08-31T1037`, `T0837` e `T0701` relidos.
- `cobogo#407` revalidada: aberta, head `32b3897335df837e0b49035ecce77d4d59fa3738`, não mergeável; sem merge.
- Card `knowledge/consumers/intuit.md`, `workspace/knowledge/projects/intuit.md`, issue `intuit#14` e árvore atual do repo confrontados.
- Busca de PRs abertas em `franklinbaldo/intuit`: nenhuma.
- Confirmado que o repo possui `playwright.config.js` e testes, mas o card permanece factual ao registrar ausência de captura visual versionada/reproduzível.
- Aberta `franklinbaldo/intuit#25` — `surface: add reproducible visual capture for UI reviews` — com escopo apenas de infraestrutura de captura, sem alterar UI nem absorver o residual de #14.
- Nenhuma branch/PR de implementação visual criada, porque isso violaria o gate de captura-before da rotina.

## Fatos observados

- `intuit#14` foi atualizado em 2026-08-31 para conter somente o residual visual/motion: foco visível e reduced-motion; alert semantics e grupos nomeados já foram fechados na #24.
- O card do Intuit está ativo, revisto em 2026-08-31 e tem `gap_score: 1` exclusivamente por esse residual D1.
- O `ProjectProfile` preserva como invariantes o sandbox estrito default, `srcdoc`, scripts opt-in e a identidade visual local.
- Não há PR aberta atualmente em `franklinbaldo/intuit`.
- A árvore do repo contém Playwright e gate, mas a memória canônica registra corretamente que não existe captura visual versionada usada como before/after.
- `intuit#25` agora possui critério objetivo para tornar a captura reproduzível e comparável em `main` e PR.

## Inferências

- Foco de teclado e reduced-motion são mudança visualmente material: mesmo que pequena, altera pixels/estado/motion e portanto exige captura before/after pela rotina.
- O gate Playwright atual prova comportamento, não fornece automaticamente a proveniência visual exigida para comparação/promoção.
- Implementar #14 antes de obter o before produziria uma melhoria plausível, mas quebraria o método e tornaria impossível demonstrar causalmente a mudança nas mesmas condições.
- A capacidade de captura é local ao consumer e pode ser pequena; ela não é gap D1/D2/D3 e não deve aumentar `gap_score` nem virar pattern Cobogó sem evidência multi-consumer.

## Resultado do gap

**Não fechado / bloqueado antes da implementação.** O gap D1 permanece verdadeiro e verificável, mas a captura obrigatória `before` não está disponível. Nenhum pixel, estilo ou motion foi alterado nesta rodada. `intuit#25` torna explícita a condição de retomada.

## Gates

- Nenhuma PR substantiva criada nesta rodada.
- `franklinbaldo/intuit#14` — issue de gap, sem branch/PR nesta rodada; implementação bloqueada pelo gate de captura-before.
- `franklinbaldo/cobogo#407` — head `32b3897335df837e0b49035ecce77d4d59fa3738`; `mergeable: false`; sem merge.

## Devolução ao Cobogó

- Consumer card do Intuit já tem `last_reviewed: 2026-08-31` e `gap_score: 1`; foi revalidado e não recebeu alteração factual adicional nesta rodada.
- Nenhum conceito novo: foco/reduced-motion é correção local de acessibilidade e ainda não possui before/after.
- Nenhum pattern promovido ou contagem incrementada; gate de ≥2 consumers com proveniência visual permanece intacto.
- Nenhum achado novo de capacidade/sinergia exige issue no workspace; o `ProjectProfile` já representa corretamente o residual de #14.

## Bloqueios

- `franklinbaldo/intuit#14` — causa: mudança visual/motion exige captura before, mas a capacidade de captura reproduzível da rota está ausente. Estado: gap D1 aberto, nenhuma implementação iniciada. Condição de retomada: `intuit#25` produzir e preservar uma captura do `main` atual com viewport/commit identificáveis; só então implementar #14, obter head verde, merge, Pages e recaptura nas mesmas condições.
- `franklinbaldo/cobogo#407` — SHA `32b3897335df837e0b49035ecce77d4d59fa3738`; causa: não mergeável e dívida ampla já medida de 65/152 testes Vitest. Estado: aberta. Condição de retomada: reconciliar deliberadamente contratos vigentes vs expectativas fossilizadas e obter todos os required checks verdes no head atual.

## Pressão para a próxima rotação

`franklinbaldo/intuit` somente se `#25` tiver fechado a capacidade de captura e permitido retomar causalmente #14; caso contrário, aplicar de novo urgência → follow-up → maior `gap_score` entre os demais consumers ativos, sem repetir Intuit por inércia.
