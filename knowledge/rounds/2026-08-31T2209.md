---
type: RoundReport
routine: cobogo
date: 2026-08-31T22:09:07-04:00
focus: franklinbaldo/cobogo
candidates: [franklinbaldo/cobogo, franklinbaldo/the-lab, franklinbaldo/sisprev]
outcome: blocked
prs: [franklinbaldo/cobogo#414]
merged: []
captures: []
blocked: [franklinbaldo/cobogo#414, franklinbaldo/cobogo#407]
---

## Foco e alternativas

A integridade do próprio corpus tornou-se a pré-condição da rotação. Resolução direta no GitHub confirmou 404 para `franklinbaldo/funes-memories`, `franklinbaldo/tambaqui`, `franklinbaldo/formatolegal`, `franklinbaldo/ecos-do-pampa`, `franklinbaldo/letra_bonita`, `franklinbaldo/travessia`, `franklinbaldo/the-theater` e `franklinbaldo/rationality-club-latam`. `franklinbaldo/sites_prefeituras` não foi excluído: o GitHub resolve o nome para `franklinbaldo/rossio`, evidenciando continuidade por rename. `franklinbaldo/undogmatic` também existe, embora esteja excluído da rotação por política separada.

O foco ficou em `franklinbaldo/cobogo` como self-consumer porque documentação corrente ainda permitia que repositórios inexistentes sustentassem inventário e patterns. The Lab e Sisprev permaneceram candidatos, mas nenhum segundo PR substantivo foi iniciado.

## Leitor de fora — antes

D1 — não aplicável a uma mudança exclusivamente semântica/documental do knowledge do Cobogó.

D2 — o corpus corrente ainda atribuía autoridade viva a repositórios inexistentes: `automation-observable-freshness` dependia de três consumers 404; `provenance-freshness` dependia de Ficha + Tambaqui; `editorial-reading` ainda contava The Theater; o scan de 2026-08-10 mantinha vários repositórios apagados como candidatos vivos. Gap: a memória histórica estava contaminando a autoridade corrente do portfólio.

D3 — não aplicável a artefatos publicados de um consumer nesta intervenção.

## Gap escolhido e critério

Gap escolhido: integridade de membership/evidência do corpus.

Critério verificável: nenhum pattern ou inventário corrente deve depender de repositório confirmado como inexistente; pattern que perde o mínimo de evidência viva deve ser removido; relações do schema devem apontar para a identidade canônica vigente dos ConsumerCards; a suíte normativa do knowledge deve ficar verde antes do merge.

## Capturas

Nenhuma. Não houve mudança visual de consumer; screenshots before/after não acrescentariam evidência causal.

## Ações

- Confirmados oito repositórios 404 e um rename `sites_prefeituras` → `rossio`.
- Removidos do `main` `knowledge/patterns/automation-observable-freshness.md`, `knowledge/patterns/provenance-freshness.md` e o scan obsoleto `knowledge/consumers/portfolio-scan-2026-08-10.md`.
- `knowledge/patterns/editorial-reading.md` foi reconciliado para O Vigia + Franklin blog, retirando The Theater.
- RoundReports cujo foco era um repositório agora confirmado inexistente (`T0501`, `T0602`, `T0701`, `T1500`) foram removidos do corpus corrente.
- A PR #413, criada sobre base anterior e tornada conflitante pelas mudanças já aterrissadas, foi fechada sem merge.
- Aberta #414 sobre o `main` atual para o delta restante: remover link morto em O Vigia e reconciliar a relação relacional antiga `consumer(title)` com `ConsumerCard(repo)`.
- A única tentativa de correção da #414 atualizou `knowledge/okf.schema.sql` e quatro referências de specimen/design-decision para repos canônicos.
- No head `de448d850e7c90d2c7f636d591202189c7b6d098`, CI e Greenfield visual preview passaram. O `check --classify --relational-schema` dentro de Knowledge passou com `conformant: true`, 113 conceitos e zero diagnósticos.
- O passo normativo posterior falhou por ausência de specs para `Concept`, `ConsumerCard` e `RoundReport`. Nenhuma segunda correção foi feita nesta rodada.

## Fatos observados

- Os oito nomes listados acima retornam 404 por resolução direta do GitHub.
- `sites_prefeituras` resolve para `franklinbaldo/rossio`; exclusão automática seria incorreta.
- O schema relacional anterior declarava tabela `consumer(title, repository)`, mas os cards atuais são `type: ConsumerCard` e usam `repo` como identidade.
- Após a correção da #414, a validação relacional fica conforme e sem diagnósticos.
- `spec-check.json` do mesmo run acusa apenas três `OKF010`: faltam `.okf/specs/concept.md`, `.okf/specs/consumercard.md` e `.okf/specs/roundreport.md`.
- `cobogo#407` permanece dívida independente e não foi contornada.

## Inferências

- A limpeza de repositórios mortos revelou duas camadas diferentes de fossilização: primeiro referências a consumers apagados; depois contratos OKF ainda modelados pelo schema legado.
- A correção relacional está validada pelo próprio gate que antes falhava; a falha restante é de governança normativa de tipos, não da relação `ConsumerCard(repo)`.
- Criar as três specs é trabalho coerente, mas seria uma segunda tentativa de correção no mesmo follow-up e excede o orçamento desta rodada.

## Resultado do gap

**parcial / blocked.** A documentação de repositórios inexistentes foi removida do corpus corrente e o contrato relacional foi reconciliado com sucesso. O loop não fecha porque #414 ainda tem `Knowledge` vermelho no head testado devido às três specs normativas ausentes; portanto não houve merge.

## Gates

- `franklinbaldo/cobogo#414` — head testado `de448d850e7c90d2c7f636d591202189c7b6d098`; CI `33462059809` = success; Greenfield visual preview `33462059806` = success; Knowledge `33462059811` = failure. Dentro de Knowledge, validação base e relacional = success/conformant; falha normativa = `OKF010` para `Concept`, `ConsumerCard`, `RoundReport`. Estado final: aberta/bloqueada, sem merge.
- `franklinbaldo/cobogo#407` — permanece aberta e fora do escopo da correção desta rodada; sem bypass.

## Devolução ao Cobogó

- Dois patterns sem suporte vivo suficiente foram removidos.
- `editorial-reading` permanece experimental apoiado apenas por consumers vivos.
- O scan amplo fossilizado foi removido em vez de ser mantido como pseudo-registry corrente.
- O link de O Vigia ao pattern removido está corrigido na #414.
- O contrato relacional agora usa `ConsumerCard(repo)` na #414; nenhuma promoção de pattern foi feita.

## Bloqueios

- `franklinbaldo/cobogo#414` — head testado `de448d850e7c90d2c7f636d591202189c7b6d098`; causa: specs normativas ausentes para `Concept`, `ConsumerCard`, `RoundReport`; estado: relações OKF conformes, gate normativo vermelho; condição de retomada: criar/reconciliar as três specs no formato canônico e obter todos os required checks verdes no novo head.
- `franklinbaldo/cobogo#407` — dívida independente da suíte Vitest; condição de retomada permanece required checks verdes no head corrente sem enfraquecer o gate.

## Pressão para a próxima rotação

`franklinbaldo/cobogo` — follow-up causal #414: completar as três specs normativas e, somente com todos os checks verdes no head exato, mergear. Depois disso, voltar à regra normal; The Lab só deve voltar ao foco se houver mudança causal no blocker de deploy ou a urgência continuar comprovada sem produzir relatório redundante.
