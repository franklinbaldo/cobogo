---
type: RoundReport
routine: cobogo
date: 2026-09-02T03:59:00-04:00
focus: franklinbaldo/ficha
candidates: [franklinbaldo/ficha, franklinbaldo/intuit, franklinbaldo/aleph-the-game]
outcome: partial
prs: [franklinbaldo/ficha#229]
merged: [franklinbaldo/ficha#229]
captures: [artifact:9833746642, artifact:9837261361]
blocked: [franklinbaldo/ficha#230, franklinbaldo/cobogo#407]
---

# Rodada Cobogó — 2026-09-02T0359

## Foco e alternativas

Follow-up/dívida causal decidiu o foco. A rodada anterior havia transformado em issue concreta a pior dimensão conhecida do corpus: `UI: 1/4` no Ficha por clipping do CTA `Buscar` em `390×844`. #227 ainda estava aberta no início desta rodada e o mecanismo permanecia presente em `main`, portanto havia trabalho fechável e sustentado por evidência. Intuit já tem baseline 20/24 sem dívida 0/1 conhecida; Aleph permanece bloqueado por #6. `cobogo#407` foi revalidada e continua não mergeável no mesmo blocker, sem mudança causal.

## Leitor de fora — antes

D1 — a home deixa claro que o leitor pode entender uma empresa e conexões públicas por busca de CNPJ/Razão Social. Gap factual: nenhum.

D2 — origem Receita Federal, competência, preservação, verificação e estado tornam o mecanismo/proveniência perceptíveis. Gap factual: nenhum.

D3 — `Levar a base` e `manifest.json` permitem reutilizar os artefatos públicos fora da interface. Gap factual: nenhum.

Assessment vigente antes da mudança: D1 4, D2 4, D3 4, UX 2, UI 1, Confiança 3 = 18/24. A maior dívida é UI: a captura narrow canônica mostra o botão `Buscar` parcialmente fora do viewport.

## Gap escolhido e critério

Gap escolhido: **UI/UX — containment da ação primária em viewport estreito.**

Critério congelado, herdado de #227 e repetido na #229: a captura canônica da rota `/ficha/` em `390×844` deve mostrar campo e botão `Buscar` integralmente dentro do viewport, sem clipping/overflow horizontal, com alvo legível; a captura `1280×900` deve permanecer sem regressão.

Recomendação Cobogó aplicada: `primary-action-viewport-containment`, maturidade `opinionated` no início da rodada.

## Capturas

- BEFORE — artifact `9833746642`, rota `/ficha/`, viewport `390×844`, método Playwright/Chromium do workflow canônico. Evidência: `Buscar` parcialmente cortado na borda direita. A captura larga `1280×900` cabe corretamente.
- AFTER NO HEAD DA PR — run `33606568537`, artifact `9837261361`, head real `b394d98daa33c1d9fb3c86c37e8c3e269030e3c0`, mesma rota e mesmo método, viewports `1280×900` e `390×844`. A captura narrow mostra input e `Buscar` integralmente contidos; desktop permanece em uma linha e sem regressão observável.
- AFTER PÓS-MERGE — inexistente. O landing é `2e2adfa7c57cc5b80f504d1a9d0aa657abf94317` e Deploy site `33606723991` concluiu `success`, porém `.github/workflows/visual-capture.yml` dispara apenas em `pull_request` ou `workflow_dispatch`; não há recaptura automática de `main`. Issue #230 aberta com critério para fechar essa lacuna. Como a mudança é visualmente material, a ausência de after pós-merge impede declarar o loop visual fechado.

Nota causal adicional: o artifact da PR ainda usa merge-ref sintético no nome/JSON por causa de #228; o run metadata identifica o head real `b394d98…`, mas a cadeia deve ser corrigida para não depender dessa reconciliação manual.

## Ações

- Relidas as três skills operacionais, README opinionado e `surface-quality-model` atuais.
- Revalidado `ProjectProfile` do Ficha como fonte factual, sem escrevê-lo.
- Inspecionado `SearchCNPJ.svelte`: `.input-group` em flex sem regra estreita; input `flex: 1`; botão com `min-width: 120px` e padding horizontal fixo — mecanismo compatível com o clipping observado.
- Criada branch `cobogo/fix-search-mobile-overflow` a partir de `ca67d1c…`.
- Alterado somente `web/src/styles/ficha-core-map.css`: reflow local abaixo de 480 px, input com `min-width: 0`, CTA com largura/padding compatíveis e UF em linha própria no modo endereço.
- Aberta `franklinbaldo/ficha#229`; head `b394d98…`.
- No head, CI `33606568572` = success e Visual Capture `33606568537` = success; artifact `9837261361` inspecionado em desktop+narrow.
- #229 mergeada por squash somente após gates verdes; landing `2e2adfa7c57cc5b80f504d1a9d0aa657abf94317`.
- CI pós-merge `33606724153` = success; Deploy site `33606723991` = success.
- #227 fechada automaticamente como `completed` pela #229.
- Aberta `franklinbaldo/ficha#230` para tornar captura pós-merge de `main` uma capacidade normal do consumer; #228 continua aberta para corrigir identidade head vs merge-ref.

## Fatos observados

- O defeito narrow tinha mecanismo local reproduzível: competição entre input flexível e CTA com largura mínima fixa sem reflow.
- O artifact do head da #229 mostra `Buscar` inteiro em `390×844`; desktop continua sem regressão visual aparente.
- A correção preserva a identidade do Ficha e vive no mapeamento CSS local, não num componente visual universal.
- O landing da #229 foi publicado com CI e Deploy site verdes.
- O workflow de captura não executa em push para `main`; logo não existe artifact pós-merge do landing nesta rodada.

## Inferências

- A opinião `primary-action-viewport-containment` parece adequada à primeira aplicação: o consumer resolveu o problema por reflow local sem exigir composição uniforme do Cobogó.
- A maturidade ainda não deve subir para `validated`: não por falta de segundo consumer, mas porque o contrato da própria rotina exige after pós-merge para mudança visualmente material.
- Head, merge-ref e landing são identidades diferentes de evidência. O corpus deve preservar essa distinção em vez de chamar qualquer screenshot de “causal”.

## Resultado do gap

**parcial.** O critério foi satisfeito na evidência do head da #229 e a implementação foi mergeada/publicada com gates verdes. Porém o loop visual não está fechado porque falta a captura after do landing `2e2adfa…` pelo mesmo método. O assessment canônico permanece 18/24 (UI 1, UX 2) até essa recaptura; não se reescreve nota por equivalência presumida entre head e conteúdo publicado.

## Gates

- `franklinbaldo/ficha#229` — head `b394d98daa33c1d9fb3c86c37e8c3e269030e3c0`; CI `33606568572` success; Visual Capture `33606568537` success; mergeável; squash merge → `2e2adfa7c57cc5b80f504d1a9d0aa657abf94317`.
- landing #229 — CI `33606724153` success; Deploy site `33606723991` success.
- `franklinbaldo/cobogo#407` — head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; continua aberta/não mergeável e com dívida ampla de testes já registrada; sem mudança causal nesta rodada.
- PR de memória do Cobogó — deve passar os gates do head atual antes do merge; registrar o resultado final nesta mesma rodada.

## Devolução ao Cobogó

- ConsumerCard do Ficha atualizado com landing #229, evidência head e blocker pós-merge.
- Assessment de Ficha mantido em 18/24 como baseline canônico, acrescido do estado da remediação; ranking não muda artificialmente antes do after.
- `primary-action-viewport-containment` registra a primeira aplicação, mas permanece `opinionated` até a recaptura do landing; depois disso pode subir para `validated` com apenas este consumer.
- #230 torna explícita a dívida de capacidade de validação visual pós-merge; #228 permanece dono da identidade causal de PR.

## Bloqueios

- `franklinbaldo/ficha#230` — causa: Visual Capture não dispara automaticamente em `main`; estado: aberto; condição objetiva de retomada: landing SHA ser recapturado em `1280×900` e `390×844` pelo mesmo Playwright/Chromium, com artifact/JSON ligados inequivocamente a `main`.
- `franklinbaldo/ficha#228` — causa: eventos de PR registram merge-ref em vez do head real dentro do artifact/JSON; não bloqueou a inspeção desta rodada porque o run metadata fornece `head_sha`, mas continua dívida de causalidade.
- `franklinbaldo/cobogo#407` — blocker pré-existente sem mudança causal.

## Pressão para a próxima rotação

`franklinbaldo/ficha` somente se #230 produzir a recaptura pós-merge (ou se #228/#230 ganhar PR pronta). Nesse caso, fechar o loop da #229, atualizar UI/UX com a evidência real e promover `primary-action-viewport-containment` para `validated` se o critério permanecer satisfeito. Sem mudança causal, rotacionar para outro consumer ativo sem baseline.
