---
type: RoundReport
routine: cobogo
date: 2026-09-01T12:57:38-04:00
focus: franklinbaldo/franklinbaldo.github.io
candidates: [franklinbaldo/franklinbaldo.github.io]
outcome: partial
prs: [franklinbaldo/franklinbaldo.github.io#1640]
merged: [franklinbaldo/franklinbaldo.github.io#1640]
captures:
  - https://api.github.com/repos/franklinbaldo/franklinbaldo.github.io/actions/artifacts/9806697863
  - https://api.github.com/repos/franklinbaldo/franklinbaldo.github.io/actions/artifacts/9811726761
blocked: [franklinbaldo/franklinbaldo.github.io@eebbe97af4e3aaf71b8a45b5752056dcf158122d]
---

# Rodada — Franklin blog

## Foco e alternativas

Foco mantido em `franklinbaldo/franklinbaldo.github.io` por follow-up causal da #1640: o head anterior já estava verde e havia materializado o `before`, portanto a ação de maior valor era completar a mudança de navegação sem abrir um segundo consumer substantivo.

Alternativas: nenhum segundo consumer aprofundado nesta rodada. A rotina consumiu o orçamento substantivo no fechamento causal da #1640.

## Leitor de fora — antes

Pré-registro herdado, já persistido antes desta execução no RoundReport `2026-09-01T1059.md`; não foi reescrito após a intervenção.

- **D1 — no site:** o leitor consegue navegar ensaios, projetos, arquivo, busca, música, livros, ranking e demais superfícies expostas pela navegação; `/audiobooks/` existe e funciona quando acessado diretamente. **Gap:** Header e Footer não oferecem caminho global para `/audiobooks/`, então a superfície fica escondida para quem chega sem contexto.
- **D2 — por trás:** o repo prova Audiobook Factory, catálogo derivado dos manifests e feed condicionado ao estado de cada obra. **Gap:** nenhum gap D2 separado; a superfície não pode transformar infraestrutura condicional em claim de podcast ativo, especialmente para HPMOR com `podcast.enabled: false`.
- **D3 — por conta própria:** source, RSS, arquivo/busca e estrutura reproduzível da fábrica estão disponíveis. **Gap:** nenhum gap D3 material independente do problema D1.

## Gap escolhido e critério

Gap escolhido: D1, descoberta global de `/audiobooks/`.

Critério verificável, preservado da rodada anterior: Header e Footer devem expor `Audiobooks` / `Audiolivros` para `/audiobooks/` dentro da hierarquia secundária existente, sem promover a fábrica a CTA principal nem anunciar podcast/feed desabilitado; checks do head exato devem ficar verdes; merge somente nesse head; Pages deve ficar verde no merge SHA; captura before/after da home pelo mesmo método deve mostrar a nova entrada.

## Capturas

### Antes

- URL original: `https://franklinbaldo.github.io/`
- URL arquivada: nenhum Save Page Now verificado.
- artifact: `https://api.github.com/repos/franklinbaldo/franklinbaldo.github.io/actions/artifacts/9806697863`
- timestamp: `2026-09-01T15:05:33Z`
- rota: `/`
- commit/head: `6503538c187bd6cb860252dd1865ef7bdb2199f5`
- método: workflow `Visual evidence` executando `scripts/screenshots.mjs`; home desktop 1440x900.
- observação: Header mostra a hierarquia primária e o grupo `More`; Footer não contém `Audiobooks`.

### Depois

- URL original: `https://franklinbaldo.github.io/`
- URL arquivada: nenhum Save Page Now verificado.
- artifact: `https://api.github.com/repos/franklinbaldo/franklinbaldo.github.io/actions/artifacts/9811726761`
- timestamp: `2026-09-01T17:10:59Z`
- rota: `/`
- commit/merge: `eebbe97af4e3aaf71b8a45b5752056dcf158122d`
- método: o mesmo workflow `Visual evidence` executando `scripts/screenshots.mjs`; home desktop 1440x900.
- observação: Footer mostra `Audiobooks` dentro da navegação secundária; o Header mantém as superfícies editoriais primárias no primeiro nível e o source do mesmo SHA inclui Audiobooks no grupo secundário `More`.

## Ações

- PR: `franklinbaldo/franklinbaldo.github.io#1640`.
- head final da PR: `a03c3670ca95100ae9bc638e7633a0efa13f7088`.
- commits de implementação na branch: `fa4602fef3fa4ee9d311e230570c968224944cf4` (Header), `050a3ac711b098cb8005dde8383d9f3b0c12705e` (Footer), `a03c3670ca95100ae9bc638e7633a0efa13f7088` (change card/head final).
- workflows no head final: Change cards `33535276185`, Visual evidence `33535276198`, Check `33535276367` — todos `success`.
- merge squash protegido pelo head SHA: `eebbe97af4e3aaf71b8a45b5752056dcf158122d`.
- recaptura pós-merge: Visual evidence `33535970280`, `success`, artifact `9811726761`.
- deploy pós-merge: Deploy to GitHub Pages `33535970297`; build `success`, job `deploy` ainda `in_progress` na última verificação desta rodada.
- issue de origem: `franklinbaldo/franklinbaldo.github.io#1637`, ainda aberta enquanto o critério de Pages não está confirmado.
- Cobogó: conceito `knowledge/concepts/commit-addressed-visual-evidence.md` registrado com proveniência do consumer.
- workspace: nenhuma issue aberta; não houve achado novo sobre capacidade operacional ou sinergia de projeto fora da autoridade de superfície.

## Fatos observados

- O artifact de before pertence ao head `6503538c...`, anterior à alteração de Header/Footer.
- No head final `a03c3670...`, Change cards, Visual evidence e Check concluíram `success`.
- A #1640 estava mergeável e foi squash-mergeada somente depois desses resultados, usando `expected_head_sha` igual a `a03c3670...`.
- O merge SHA é `eebbe97a...`.
- A recaptura de `main` no merge SHA concluiu `success` e produziu artifact `9811726761`.
- A captura desktop pós-merge mostra `Audiobooks` no Footer; o código no merge SHA contém a mesma entrada no `secondaryLinks` do Header, dentro de `More`.
- O build do Pages no merge SHA concluiu `success`; o passo `actions/deploy-pages` continuava `in_progress` na última leitura.

## Inferências

- A solução fecha a parte de composição/navegação do gap sem deslocar a identidade do blog: audiolivros entram junto de música/livros/ranking/tags/changelog, não junto das superfícies editoriais primárias.
- A evidência visual agora é causalmente mais forte do que nas rodadas anteriores porque before e after são artifacts do mesmo harness e carregam o SHA observado.
- Não é possível declarar o gap integralmente fechado enquanto o Pages do merge SHA não concluir verde, porque esse item fazia parte do critério pré-registrado.

## Resultado do gap

**Parcial.** A mudança, o merge e o before/after satisfazem a parte visual e estrutural do critério: a nova entrada aparece na captura pós-merge sem promoção a CTA principal, e Header/Footer apontam para `/audiobooks/`. O único item ainda não satisfeito na última leitura é `Pages verde no merge SHA`; o deploy permanecia em andamento.

O critério não foi reescrito para acomodar esse estado.

## Gates

- PR `franklinbaldo/franklinbaldo.github.io#1640`
  - head: `a03c3670ca95100ae9bc638e7633a0efa13f7088`
  - Change cards `33535276185`: `success`
  - Visual evidence `33535276198`: `success`
  - Check `33535276367`: `success`
  - mergeable antes do merge: `true`
  - estado final: mergeada por squash em `eebbe97af4e3aaf71b8a45b5752056dcf158122d`
- Pós-merge `eebbe97af4e3aaf71b8a45b5752056dcf158122d`
  - Visual evidence `33535970280`: `success`
  - Deploy to GitHub Pages `33535970297`: build `success`; deploy `in_progress` na última leitura

## Devolução ao Cobogó

- Consumer card `knowledge/consumers/franklin-blog.md` atualizado com a nova evidência e capacidade de captura; `last_reviewed: 2026-09-01` mantido.
- `gap_score` permanece `1` enquanto Pages do merge SHA não for verificado verde.
- Conceito `commit-addressed-visual-evidence` criado com `observed_in` contendo apenas este consumer e proveniência no merge SHA.
- Pattern não promovido: contagem de consumers = 1, abaixo do gate de promoção.

## Bloqueios

- SHA: `eebbe97af4e3aaf71b8a45b5752056dcf158122d`.
- causa: o workflow `Deploy to GitHub Pages` `33535970297` tinha build verde, mas o job `deploy`/`actions/deploy-pages` ainda permanecia `in_progress` após repetidas verificações.
- estado: PR já mergeada; after já materializado; issue #1637 mantida aberta; card mantém gap 1.
- condição de retomada: o run `33535970297` concluir. Se `success`, verificar o site no merge SHA, fechar #1637 e reduzir `gap_score` para 0; se falhar, tratar o deploy como urgência na próxima rodada.

## Pressão para a próxima rotação

`franklinbaldo/franklinbaldo.github.io` somente para concluir o estado causal do deploy `33535970297`; depois disso, voltar à rotação normal e não consumir nova rodada substantiva no mesmo consumer.
