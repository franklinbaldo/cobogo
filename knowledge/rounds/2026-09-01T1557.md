---
type: RoundReport
routine: cobogo
date: 2026-09-01T15:57:40-04:00
focus: franklinbaldo/quem-sao-eles
candidates: [franklinbaldo/quem-sao-eles, franklinbaldo/the-lab, franklinbaldo/cobogo]
outcome: blocked
prs: []
merged: []
captures: []
blocked: [franklinbaldo/quem-sao-eles#17, franklinbaldo/the-lab#128, franklinbaldo/cobogo#407]
---

# Rodada — Quem São Eles?

## Foco e alternativas

As três skills operacionais e a memória recente foram relidas. `franklinbaldo/the-lab` permanece com `gap_score: 1`, mas a rodada anterior determinou não repetir o bloqueio sem mudança causal em #128. `franklinbaldo/cobogo#407` permanece própria e aberta, porém o último estado conhecido continua com `build` vermelho no head atual, portanto não satisfaz follow-up causal verde.

No discovery leve, `franklinbaldo/quem-sao-eles` apresentou um fato novo e urgente de superfície: o workflow mensal gerou em 2026-09-01 o commit `356071a66aeab08052de03c3c671f3e6e7f14870`, adicionando `public/data/202607_pep.parquet`, mas a consulta de Actions para esse head retornou zero runs. Como `src/pages/pep.astro` escolhe o maior `YYYYMM_pep.parquet` no momento do build, o snapshot novo em `main` não tem build/deploy/capture correspondente. Isso vence a rotação por urgência de publicação/freshness e não por antiguidade simples.

## Leitor de fora — antes

- **D1 — no site:** a rota `/pep` permite consultar PEP no navegador e mostra origem, competência e arquivo exato. **Gap:** `main` já contém o snapshot `202607_pep.parquet`, mas não há Pages/capture associado ao SHA que o adicionou; portanto o leitor publicado não tem prova de receber a competência nova.
- **D2 — por trás:** README/ProjectProfile e o código provam refresh mensal, snapshot versionado, Astro + Svelte + DuckDB-WASM e provenance derivada do arquivo selecionado no build. **Gap:** nenhum gap explicativo independente; o mecanismo existe, mas seu último refresh não fechou o loop até publicação.
- **D3 — por conta própria:** a pessoa pode baixar o mesmo Parquet que a rota consulta. **Gap:** o repo contém um artifact mais novo que o estado publicado comprovado, então o contrato `usar aqui ↔ levar o artefato` está causalmente incompleto para a competência 202607.

## Gap escolhido e critério

Gap escolhido: materializar na superfície publicada o snapshot mensal novo que já existe em `main`.

Critério verificável, fixado antes de qualquer intervenção: o refresh mensal deve causar build/deploy do Pages no SHA que contém o novo Parquet; esse Pages deve terminar verde; `/pep` deve mostrar a competência e o arquivo correspondentes ao snapshot novo; o capture runtime deve rodar contra esse mesmo estado e continuar provando `search-ready` + consulta real. Não basta apenas o arquivo existir em Git.

## Capturas

- Antes: nenhuma nova captura. O gap foi estabelecido causalmente pela ausência de qualquer workflow run no head `356071a66aeab08052de03c3c671f3e6e7f14870` e pelo fato de `pep.astro` resolver o snapshot em build time. O último capture conhecido permanece `PEP visual capture` run `32075299012` no estado anterior `1fb2c8eb55e577de07e274386b06ceec7a2bb50f`.
- Depois: nenhum; nenhuma mudança visual foi implementada e o pipeline/deploy é autoridade do repo consumer.
- URL original: `https://franklinbaldo.github.io/quem-sao-eles/pep`.
- URL arquivada: nenhum Save Page Now verificado.

## Ações

- Revalidado o ConsumerCard de Quem São Eles? e o `ProjectProfile` do workspace.
- Verificado workflow `Update PEP Data` run `33494134838`: `success`, origem do commit novo.
- Verificado commit `356071a66aeab08052de03c3c671f3e6e7f14870`: adiciona `public/data/202607_pep.parquet`.
- Verificado `actions/runs?head_sha=356071a...`: `total_count: 0`.
- Lido `src/pages/pep.astro`: `readdirSync(public/data)`, sort e `pepFiles.at(-1)` definem competência, source URL e Parquet no build.
- Aberta `franklinbaldo/quem-sao-eles#17` com evidência, autoridade, escopo e critério de conclusão. Não implementado porque a falha é de pipeline/deploy do repo dono.
- ConsumerCard atualizado para `last_reviewed: 2026-09-01`, `gap_score: 1`.
- Nenhum PR substantivo aberto nesta rotina.

## Fatos observados

- O refresh mensal executou com sucesso em 2026-09-01 e produziu um commit de dados em `main`.
- O commit novo contém `public/data/202607_pep.parquet`.
- Não há workflow run associado ao SHA novo.
- A rota `/pep` determina o snapshot mais recente durante o build, a partir dos arquivos existentes em `public/data`.
- O último estado visual/runtime comprovado pelo card é anterior ao refresh de setembro.

## Inferências

- O refresh atualiza o repositório, mas não fecha automaticamente o loop de publicação; o novo snapshot não pode ser tratado como publicado apenas porque existe em `main`.
- O problema é operacional/pipeline, não uma lacuna de hierarquia visual. Alterar UI seria mascarar a causa e violar a fronteira desta rotina.
- O padrão de provenance da página continua correto em desenho: ele liga origem, competência e arquivo. O defeito está na causalidade refresh → build/deploy/capture.

## Resultado do gap

**Não fechado; bloqueado na autoridade do consumer.** O gap foi diagnosticado e atribuído a #17, mas não há mudança de design legítima a executar no orçamento desta rotina. O `gap_score` sobe para 1 até Pages + capture provarem a competência nova.

## Gates

- `franklinbaldo/quem-sao-eles#17` — issue operacional, sem PR nesta rodada; condição de fechamento: Pages e capture verdes no SHA com o novo snapshot e `/pep` correspondente.
- `franklinbaldo/cobogo#407` — continua não elegível a merge enquanto `build` não estiver verde no head atual; nenhum bypass.
- `franklinbaldo/the-lab#128` — sem mudança causal aprofundada nesta rodada.

## Devolução ao Cobogó

- ConsumerCard `knowledge/consumers/quem-sao-eles.md` atualizado para refletir o drift factual e `gap_score: 1`.
- Nenhum conceito novo: `Provenance and freshness` e `Public artifact reuse` já cobrem a relação; o achado desta rodada é falha operacional de publicação, não novo pattern visual.
- Nenhum pattern promovido; não há nova evidência before/after em ≥2 consumers.
- Nenhuma issue no workspace: não surgiu capacidade/sinergia nova; o ProjectProfile permanece factual quanto ao mecanismo e já alerta que snapshots futuros precisam ser observados.

## Bloqueios

- `franklinbaldo/quem-sao-eles#17` — SHA `356071a66aeab08052de03c3c671f3e6e7f14870`; causa: refresh mensal gerou snapshot sem run de Pages/capture no novo head; estado: aberto; condição de retomada: pipeline provocar Pages + capture no SHA contendo 202607 e superfície publicada provar competência/arquivo.
- `franklinbaldo/the-lab#128` — bloqueio operacional pré-existente; não aprofundado por ausência de mudança causal conhecida.
- `franklinbaldo/cobogo#407` — bloqueio pré-existente por check `build` vermelho no head atual; não mergeado.

## Pressão para a próxima rotação

`franklinbaldo/quem-sao-eles` se #17 apresentar mudança causal (PR/run/deploy); se não, não repetir o bloqueio e voltar ao desempate normal dos consumers ativos.
