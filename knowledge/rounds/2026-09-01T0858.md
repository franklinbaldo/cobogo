---
type: RoundReport
routine: cobogo
date: 2026-09-01T08:58:31-04:00
focus: franklinbaldo/franklinbaldo.github.io
candidates: [franklinbaldo/franklinbaldo.github.io, franklinbaldo/quem-sao-eles, franklinbaldo/the-lab, franklinbaldo/cobogo]
outcome: blocked
prs: []
merged: []
captures: []
blocked: [franklinbaldo/franklinbaldo.github.io#1637, franklinbaldo/cobogo#407, franklinbaldo/the-lab#128]
---

## Foco e alternativas

As três skills operacionais e os três RoundReports mais recentes foram relidos antes da seleção. `cobogo#407` segue aberta e não mergeável no mesmo head histórico, portanto não vence por follow-up causal. The Lab permanece um bloqueio conhecido sem mudança causal: a issue #128 continua aberta e registra `Deploy Site` como `disabled_manually`. Entre os cards ativos ainda em `last_reviewed: 2026-08-31`, Franklin blog e Quem São Eles? empatavam em `gap_score: 0`; o blog foi aprofundado porque discovery encontrou uma capacidade pública incorporada depois da última revisão — a camada `/audiobooks/` — e Pages verde no `main` corrente.

## Leitor de fora — antes

**D1 — o que a pessoa pode fazer diretamente no site.** A navegação global oferece ensaios/projetos/sobre/busca e, no grupo secundário, música, livros, ranking, tags e changelog. O repo também prova um catálogo público `/audiobooks/`, páginas por obra/capítulo e estados `podcast`/`em preparação`, mas essa entrada não aparece no Header/Footer e o ConsumerCard ainda não a registra. **Gap [fato]:** um leitor que entra pela navegação global não recebe caminho para descobrir a superfície de audiolivros já publicada.

**D2 — o que o projeto faz por trás e por que existe.** O ProjectProfile e o repo provam uma Audiobook Factory multi-work, com catálogo derivado de manifests e publicação de feed condicionada a `podcast.enabled`. O card atual ainda descreve a maquinaria anterior à incorporação dessa camada. **Gap [fato]:** há drift documental no card, mas a superfície `/audiobooks/` já explica seu próprio estado como catálogo em preparação/publicação; o gap escolhido permanece D1.

**D3 — o que a pessoa pode fazer sozinha com os artefatos publicados.** RSS EN/PT e source permanecem reutilizáveis; a fábrica possui rota de feed por obra somente quando `podcast.enabled: true`. HPMOR está explicitamente com podcast desabilitado, então não há base para anunciar feed HPMOR ativo. **Gap:** nenhum D3 independente do gap de descoberta D1.

## Gap escolhido e critério

Gap escolhido: **D1 — tornar a superfície `/audiobooks/` descobrível pela navegação global sem alterar a identidade editorial do blog.**

Critério verificável e imutável: **depois da mudança, Header e Footer devem oferecer um link rotulado `Audiobooks`/`Audiolivros` que leve a `/audiobooks/`; a captura da home desktop deve mostrar a nova entrada dentro da hierarquia existente, sem transformar audiolivros em CTA principal e sem anunciar podcast/feed de obra que esteja desabilitado.**

## Capturas

- **Before visual:** não materializada. A capacidade versionada existe em `scripts/screenshots.mjs` e cobre `/` em desktop 1440×900, mas nesta execução não foi possível obter checkout executável para rodá-la. Como a mudança pretendida é visivelmente material na navegação, a ausência da captura antes bloqueia a PR por regra da rotina.
- **After visual:** não aplicável enquanto não houver intervenção/merge; deve usar a mesma rota, viewport e script quando o bloqueio de captura for removido.
- **Deploy observado:** `Deploy to GitHub Pages` run `33498944159`, `success`, head `b5492e69a2bc4298b2f1886521d234fb6b7f5f16`.
- **Save Page Now:** não verificado nesta rodada.

## Ações

- Pré-registro criado antes de qualquer alteração no consumer.
- Lidos ConsumerCard, ProjectProfile, `Header.astro`, `Footer.astro`, rotas/contratos da Audiobook Factory e estado do manifest HPMOR.
- Discovery confirmou `franklinbaldo.github.io/main` em `b5492e69a2bc4298b2f1886521d234fb6b7f5f16` e Pages verde nesse mesmo SHA.
- Confirmado que a busca no source encontra `/audiobooks/` nas rotas internas da fábrica, mas não como entrada global de Header/Footer; o próprio Footer documenta a lista compartilhada com Header sem audiolivros.
- A tentativa de executar a capacidade versionada de screenshots não conseguiu obter checkout executável nesta execução. Não foi aberta PR sem a captura before obrigatória.
- Aberta `franklinbaldo/franklinbaldo.github.io#1637` com evidência, escopo, gate visual e critério de conclusão; nenhuma implementação foi feita.
- Revalidada `franklinbaldo/the-lab#128`: permanece aberta e registra `Deploy Site` como `disabled_manually`; nenhuma mudança causal.
- Revalidada `franklinbaldo/cobogo#407`: aberta, `mergeable: false`, head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; nenhum merge/bypass.
- ConsumerCard do Franklin blog atualizado para `last_reviewed: 2026-09-01`, `gap_score: 1`, incorporando a camada de audiolivros e o gap D1 rastreado em #1637.

## Fatos observados

- `franklinbaldo.github.io/main` observado em `b5492e69a2bc4298b2f1886521d234fb6b7f5f16`.
- Pages run `33498944159` concluiu `success` no mesmo SHA.
- `src/pages/audiobooks/index.astro` existe e apresenta o catálogo de Audiolivros; há páginas por obra/capítulo e feed por obra condicionado a `podcast.enabled`.
- `Header.astro`/`Footer.astro` organizam os links globais sem entrada para audiolivros; `Footer.astro` explicita que segue o mesmo agrupamento do Header.
- `data/audiobooks/hpmor/work.md` mantém `podcast.enabled: false`.
- `scripts/screenshots.mjs` existe e captura a home em desktop/mobile, mas não foi executável nesta sessão por indisponibilidade do checkout necessário.
- `franklinbaldo/the-lab#128` continua aberta com o diagnóstico `disabled_manually` para `Deploy Site`.
- `franklinbaldo/cobogo#407` permanece aberta e não mergeável no head histórico.

## Inferências

- O problema é descoberta/navegação de uma superfície já incorporada, não implementação da fábrica de áudio.
- A correção mínima deve entrar no grupo secundário existente, preservando a prioridade editorial de ensaios/projetos.
- A existência de rota/feed code não autoriza afirmar podcast ativo para HPMOR; o manifest é a fronteira factual e atualmente o desabilita.
- Como a correção adicionaria pixels à navegação global, source inspection não substitui before/after; bloquear a PR é mais correto do que fechar o gap sem evidência visual.

## Resultado do gap

**não fechado / blocked.** O gap D1 foi comprovado e ganhou issue dona (#1637), mas a intervenção não começou porque a captura before obrigatória não pôde ser materializada. O critério pré-registrado permanece intacto para retomada.

## Gates

- Nenhuma PR substantiva foi aberta no consumer; portanto não há head candidato a merge.
- `franklinbaldo/franklinbaldo.github.io#1637` — issue aberta; condição de retomada: produzir before da home via `scripts/screenshots.mjs`, então implementar Header/Footer, exigir Check verde no head exato, merge protegido pelo head, Pages verde e after pelo mesmo método.
- `franklinbaldo/cobogo#407` — head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; aberta e `mergeable: false`; nenhum merge.

## Devolução ao Cobogó

- `knowledge/consumers/franklin-blog.md` atualizado para `last_reviewed: 2026-09-01`, `gap_score: 1`.
- O card agora registra a Audiobook Factory como capacidade incorporada, sem inventar podcast HPMOR ativo.
- Nenhum conceito novo, nenhuma contagem incrementada e nenhum pattern promovido: não há solução local fechada nem before/after, muito menos segunda evidência convergente.
- Nenhuma escrita foi feita no ProjectProfile do workspace.

## Bloqueios

- `franklinbaldo/franklinbaldo.github.io#1637` — causa: captura visual before obrigatória não materializada apesar de `scripts/screenshots.mjs` existir; estado: issue aberta, sem branch/PR; condição de retomada: executar captura `/` desktop com o script versionado e então iniciar a mudança.
- `franklinbaldo/cobogo#407` — head `612f7f247cdbfc40b0cf8e0292fbb50d9f39999e`; estado: aberta, não mergeável; condição de retomada: head corrente mergeável com todos os gates verdes.
- `franklinbaldo/the-lab#128` — estado: aberta, `Deploy Site` continua documentado como `disabled_manually`; condição de retomada: workflow ativo + deploy verde do `main` + recaptura pública.

## Pressão para a próxima rotação

**franklinbaldo/franklinbaldo.github.io#1637**, se a capacidade de captura puder ser materializada; caso contrário, não repetir o bloqueio e voltar à seleção normal por urgência/follow-up/gap fechável.
