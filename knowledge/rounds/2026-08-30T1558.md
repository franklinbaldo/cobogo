---
type: RoundReport
routine: cobogo
date: 2026-08-30T15:58:32-04:00
focus: franklinbaldo/causaganha
candidates: [franklinbaldo/causaganha, ovigialocal/ovigialocal.github.io]
outcome: partial
prs: [franklinbaldo/causaganha#899, franklinbaldo/causaganha#900]
merged: [franklinbaldo/causaganha#899, franklinbaldo/causaganha#900]
captures: [github-actions:32532893041/artifact:9464477484]
blocked: [franklinbaldo/causaganha#900-post-merge-recapture, franklinbaldo/cobogo#407, ovigialocal/ovigialocal.github.io#8]
---

# Rodada Cobogó — 2026-08-30 15:58 -04

## Foco e alternativas

Não há urgência de Pages/deploy conhecida. O follow-up causal encontrou a base da pilha de superfície do CausaGanha: #899 estava mergeável no head `1821dd75f3dbe244737cc28b3162109a089e9711`, sem check com conclusão de falha, e foi mergeada por squash em `4d25935c5854223e63b625473f7f8177b3fa97a1`. Com a base incorporada, #900 passou a apontar diretamente para `main`, continua mergeável e é o primeiro PR substantivo da pilha que altera a superfície pública. Pela regra de follow-up causal, CausaGanha é o foco. O Vigia fica como alternativa; seu card terminou a rodada anterior com `gap_score: 0`.

`franklinbaldo/cobogo#407` continua bloqueada pelo gate Vitest já registrado. `ovigialocal/ovigialocal.github.io#8` continua bloqueada por ancestry/conflito e não é trabalho desta rodada.

## Leitor de fora — antes

### D1 — o que a pessoa pode fazer diretamente no site

O site já permite consultar um processo por CNJ, pesquisar publicações e explorar cobertura/dados. A superfície atual, porém, distribui descoberta entre headers locais, footer e outros pontos; não há uma navegação global compacta organizada pelos jobs reais do leitor.

**Gap D1 [fato]:** as capacidades já publicadas existem, mas o leitor de fora não recebe em todas as páginas uma orientação estável para consultar, buscar, explorar cobertura e entender/reutilizar o projeto.

### D2 — o que o projeto faz por trás e por que existe

O `ProjectProfile` define CausaGanha como uma camada cívica verificável sobre rastros judiciais públicos, separando arquivo, estado, teor, significado, proveniência, freshness e cobertura. O README incorporado pela #899 agora explica esse modelo e as três interfaces — site, dados e MCP — mas essa estrutura ainda não virou navegação pública global.

**Gap D2 [fato]:** a superfície não torna igualmente recuperável, de qualquer página, o caminho para entender a cobertura e a explicação/reuso do sistema; o modelo factual existe no repo, mas a orientação pública ainda é fragmentada.

### D3 — o que a pessoa pode fazer sozinha com os artefatos publicados

O repositório prova artefatos reutilizáveis: Internet Archive, `sync-manifest.parquet`, `catalog.sql`, `indice_processual.parquet` e Parquets por fonte conforme disponibilidade. O README oferece uma receita DuckDB reproduzível.

**Gap D3 [fato]:** não é necessário criar novo artefato nesta rodada; o gap é de descoberta: o caminho para dados/reuso precisa ficar diretamente alcançável pela navegação pública, sem transformar o site em portal técnico.

## Gap escolhido e critério

**Gap escolhido: D1/D2 — navegação global pelos jobs reais do CausaGanha.**

**Critério pré-registrado e congelado:** após a mudança, uma captura da mesma superfície funcional deve mostrar uma navegação global compacta com rotas orientadas a tarefa — consultar, buscar, explorar cobertura e entender/reutilizar — sem expor nomes de pipeline como arquitetura principal. A navegação deve preservar a identidade verde/dourada local, funcionar sem JavaScript como links normais, ter estado atual semanticamente marcado e não degradar a leitura/densidade da página funcional. O head corrente precisa estar mergeável e sem required check vermelho antes do merge; após o merge, o deploy/Pages deve ser verificado e a mesma capacidade de captura deve recapturar a superfície.

## Capturas

### Antes

- rota de controle: `/stats`;
- método: workflow de comparação determinística já existente no consumer;
- run `32532893041`, artifact `cobogo-core-stats-before-after` id `9464477484`, digest `sha256:31c4911fdcdfcefa06379969f7260c396277f57ec2c6166d209c8c34a71624a8`;
- o artifact contém base/head no mesmo viewport e fixture e é a evidência visual disponível para #900 antes do merge.

### Depois

- merge/deploy: `a76ef9a0ba44373d8e3e892c8fa2f5fdef0a6e03`;
- `Deploy Web` run `33332421721`: jobs `build` e `deploy` concluíram com `success` no merge SHA;
- recaptura pós-merge: **não produzida**. O workflow `Cobogo Core Adoption Capture` disponível em `main` tem apenas trigger `pull_request` para `web/src/**`, sem `push` ou `workflow_dispatch`. A integração disponível nesta rodada não expõe dispatch arbitrário de workflow, e o ambiente local não conseguiu resolver `github.com` para reproduzir a captura fora do Actions. Alterar o workflow agora exigiria um segundo PR de infraestrutura no mesmo consumer, excedendo o orçamento de um PR substantivo;
- por isso não existe after-capture admissível nesta rodada e o loop visual permanece parcial, embora o deploy esteja comprovadamente verde.

## Ações

- skills `brazilian-web-design`, `cobogo-design-review` e `cobogo-consumer-synergy` relidas;
- três RoundReports mais recentes consultados;
- consumer card legado do CausaGanha e `ProjectProfile` do workspace relidos;
- README e CLAUDE.md do consumer confrontados com o card;
- #899 mergeada como follow-up causal não visual, estabelecendo a linguagem factual de produto em `main`;
- RoundReport pré-registrado no commit Cobogó `79c887f0085cb7f991dbc84c08f02ac9403c8a50` antes do merge substantivo;
- #900 selecionada como único PR substantivo, reconferida mergeável no head `f8ebb148bc9378f41ebd3b06e4db2338197af397` e sem check observado com conclusão `failure`;
- #900 mergeada por squash em `a76ef9a0ba44373d8e3e892c8fa2f5fdef0a6e03`;
- deploy pós-merge verificado no run `33332421721` com `build=success` e `deploy=success`;
- workflow de captura pós-merge inspecionado e confirmado PR-only, impedindo o fechamento da recaptura sem nova mudança de infraestrutura;
- consumer card migrado para o schema `ConsumerCard` e reconciliado com `main`/ProjectProfile, com `gap_score: 2`.

## Fatos observados

- o card do CausaGanha usava o schema legado `type: consumer` e `last_verified: 2026-08-18`; foi migrado nesta rodada;
- o README em `main` após #899 descreve consulta por CNJ, busca DJEN, quatro fontes, três interfaces e artefatos públicos reutilizáveis;
- `CLAUDE.md` ainda abre com a formulação antiga de “archives DJEN and serves a public dashboard”, evidência de documentação operacional parcialmente atrasada, mas isso não é mudança de superfície desta rodada;
- a home atual já encaminha CNJ ao dossiê reconciliado, mas abaixo do hero continua centrada no arquivo DJEN e inclui calendário explicitamente demonstrativo; portanto #901 ainda representa gap D1 real, não mera preferência visual;
- `/sobre` contém fatos multi-fonte e receita DuckDB correta, mas ainda organiza a explicação de forma institucional e não pelo modelo arquivo / estado / teor; portanto #902 ainda representa gap D2 real;
- #900 foi mergeada com head `f8ebb148bc9378f41ebd3b06e4db2338197af397`; antes do merge, `lint`, `tests (tjro)`, `compare-stats` e GitGuardian observados estavam verdes;
- o merge SHA `a76ef9a0...` foi construído e implantado com sucesso no GitHub Pages;
- o workflow visual canônico do consumer não roda em `push` e não possui `workflow_dispatch`.

## Inferências

- uma navegação orientada a jobs é um ajuste de arquitetura de informação local, não um pattern a promover ao Cobogó por si só;
- tornar “dados/reuso” alcançável pela navegação fecha parte do gap D3 de descoberta sem inventar novos artefatos nem appificar a leitura;
- a mudança tem evidência pré-merge suficiente para ser plausivelmente correta e deploy pós-merge saudável, mas o critério congelado exigia recaptura pós-merge; não é legítimo rebaixar esse requisito depois do fato.

## Resultado do gap

**parcial.** A implementação e o deploy satisfazem a parte estrutural do critério: #900 adiciona a navegação global orientada a jobs, usa links normais, preserva a autoridade visual local e passou nos gates observados do head. O artifact pré-merge `9464477484` fornece a evidência visual da mudança. Porém o critério também exigia recaptura da mesma superfície após o merge, e a capacidade existente é PR-only. Sem after-capture do SHA implantado, o gap não pode ser classificado como fechado nesta rodada.

## Gates

- #899 — head `1821dd75f3dbe244737cc28b3162109a089e9711`; mergeável; checks publicados sem falha observada; squash merge `4d25935c5854223e63b625473f7f8177b3fa97a1`.
- #900 — head `f8ebb148bc9378f41ebd3b06e4db2338197af397`; mergeável imediatamente antes do merge; `lint`, `tests (tjro)`, `compare-stats` e GitGuardian verdes no head; squash merge `a76ef9a0ba44373d8e3e892c8fa2f5fdef0a6e03`.
- pós-merge #900 — `Deploy Web` run `33332421721`, `build=success`, `deploy=success` no merge SHA.
- recaptura #900 — bloqueada pela ausência de trigger pós-merge/dispatch no workflow visual atual.
- `franklinbaldo/cobogo#407` — aberto/vermelho; nenhum merge.

## Devolução ao Cobogó

- `knowledge/consumers/causaganha.md` migrado ao schema `ConsumerCard`, `last_reviewed: 2026-08-30`, `gap_score: 2`;
- D1 reconciliada com a home real em `main`: navegação fechada estruturalmente, mas home multi-fonte ainda é gap material rastreado por #901;
- D2 reconciliada com `/sobre`: artefatos e fontes estão corretos, mas explicação progressiva do sistema ainda é gap material rastreado por #902;
- D3 não contabiliza gap material: há artefatos e receita DuckDB reais e reproduzíveis;
- nenhum conceito novo foi promovido ou registrado como evidência suficiente nesta rodada: a navegação é solução local e o loop visual não foi fechado pós-merge;
- nenhum achado novo sobre capacidade/sinergia exigiu issue no workspace; o drift do ProjectProfile não foi alterado por esta rotina.

## Bloqueios

- `franklinbaldo/causaganha#900-post-merge-recapture` — merge SHA `a76ef9a0ba44373d8e3e892c8fa2f5fdef0a6e03`; causa: `Cobogo Core Adoption Capture` só dispara em `pull_request` e não possui `push`/`workflow_dispatch`; estado: deploy verde, evidence loop incompleto; condição de retomada: disponibilizar recaptura do SHA de `main` pela capacidade existente ou evoluir a infraestrutura visual em rodada própria e então recapturar `/stats`.
- `franklinbaldo/cobogo#407` — gate Vitest/CI vermelho; condição de retomada: reconciliar a suíte sem enfraquecer o gate.
- `ovigialocal/ovigialocal.github.io#8` — ancestry/conflito; condição de retomada: reconciliação dedicada preservando o contrato integral.

## Pressão para a próxima rotação

`franklinbaldo/causaganha#901` — a home em `main` ainda conta predominantemente a história do arquivo DJEN, apesar de o produto e a navegação já serem multi-fonte. Antes de outro merge substantivo, a próxima rodada deve também resolver/absorver o bloqueio de recaptura pós-merge para não repetir um loop visual incompleto.
