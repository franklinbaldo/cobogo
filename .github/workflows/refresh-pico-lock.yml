name: Refresh Pico removal lockfile

on:
  push:
    branches: [chore/remove-pico-dependency]

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Regenerate lockfile
        run: npm install --package-lock-only --ignore-scripts
      - name: Commit regenerated lockfile and remove helper
        run: |
          rm .github/workflows/refresh-pico-lock.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package-lock.json .github/workflows/refresh-pico-lock.yml
          git diff --cached --quiet && exit 0
          git commit -m "chore: refresh lockfile after Pico removal"
          git push
